<UserControl x:Class="LongBetterWindows.Host.Views.ToolCenterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LongBetterWindows.Host.Views"
             xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationCore"
             Focusable="True" PreviewKeyDown="UserControl_PreviewKeyDown">
    <UserControl.Resources>
        <Style x:Key="ManagementCard" TargetType="Border" BasedOn="{StaticResource LongCard}">
            <Setter Property="Effect" Value="{x:Null}" />
        </Style>
        <Style x:Key="OverviewCard" TargetType="Border" BasedOn="{StaticResource ManagementCard}" />
        <Style x:Key="OverviewTaskButton" TargetType="Button" BasedOn="{StaticResource LongButton}">
            <Setter Property="Height" Value="76" />
            <Setter Property="Padding" Value="14,11" />
            <Setter Property="Margin" Value="0,0,10,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                CornerRadius="{DynamicResource Long.Radius.Control}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource Long.Brush.Surface.Hover}" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource Long.Brush.Stroke.Strong}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource Long.Brush.Surface.Pressed}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource Long.Brush.Focus}" />
                                <Setter TargetName="Chrome" Property="BorderThickness" Value="2" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="OverviewUtilityButton" TargetType="Button" BasedOn="{StaticResource OverviewTaskButton}">
            <Setter Property="Height" Value="44" />
            <Setter Property="Padding" Value="14,0" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="104" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid x:Name="PageHeader" Grid.Row="0" Margin="32,20,32,16">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="PageTitle" Text="{DynamicResource i18n.page.overview.title}" FontSize="{DynamicResource Long.Type.Display}"
                               FontWeight="SemiBold" Foreground="{DynamicResource Long.Brush.Text.Primary}" />
                    <TextBlock x:Name="PageSubtitle" Text="{DynamicResource i18n.page.overview.subtitle}"
                               FontSize="{DynamicResource Long.Type.Body}" Margin="0,5,0,0"
                               Foreground="{DynamicResource Long.Brush.Text.Secondary}" />
                </StackPanel>
            </Grid>

            <ScrollViewer x:Name="ContentScrollViewer" Grid.Row="1" Style="{StaticResource DarkScrollViewerStyle}"
                          automation:AutomationProperties.AutomationId="Long.ToolCenter.ContentScroll"
                          automation:AutomationProperties.Name="{DynamicResource i18n.shell.pageContent}"
                          HorizontalContentAlignment="Stretch"
                          HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto">
                <Border x:Name="ContentBodyFrame" Padding="32,0,32,32"
                        HorizontalAlignment="Stretch">
                <Grid x:Name="ContentBody">
                    <!-- 概览 -->
                    <StackPanel x:Name="PanelOverview">
                        <Border x:Name="WelcomeBanner" Visibility="Collapsed" Margin="0,0,0,18"
                                CornerRadius="{DynamicResource Long.Radius.Card}"
                                Background="{DynamicResource Long.Brush.Accent.Gradient}" Padding="22,18">
                            <Grid x:Name="WelcomeGrid">
                                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition x:Name="WelcomeTextColumn" Width="*"/>
                                    <ColumnDefinition x:Name="WelcomeActionColumn" Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="48"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Image Width="38" Height="38" HorizontalAlignment="Left" VerticalAlignment="Top"
                                           Source="/LongBetterWindows.Host;component/Assets/app-icon.png"
                                           RenderOptions.BitmapScalingMode="HighQuality"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{DynamicResource i18n.overview.welcome.title}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource Long.Brush.Text.OnAccent}" />
                                        <TextBlock Text="{DynamicResource i18n.overview.welcome.desc}"
                                                   Margin="0,6,10,0" FontSize="12" TextWrapping="Wrap"
                                                   Foreground="{DynamicResource Long.Brush.Text.OnAccentMuted}" />
                                    </StackPanel>
                                </Grid>
                                <Button x:Name="WelcomeDismissButton" Grid.Column="1"
                                        Content="{DynamicResource i18n.overview.welcome.dismiss}" Style="{StaticResource LongButton}"
                                        Background="{DynamicResource Long.Brush.Surface.OnAccent}"
                                        BorderBrush="{DynamicResource Long.Brush.Stroke.OnAccent}" Foreground="{DynamicResource Long.Brush.Text.OnAccent}"
                                        Click="WelcomeDismiss_Click" />
                            </Grid>
                        </Border>

                        <Border x:Name="OverviewSummaryCard"
                                Style="{StaticResource OverviewCard}"
                                Margin="0,0,0,20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Margin="0,0,18,0">
                                        <TextBlock Text="{DynamicResource i18n.overview.status.title}"
                                                   FontSize="16" FontWeight="SemiBold" />
                                        <TextBlock Text="{DynamicResource i18n.overview.status.ready}"
                                                   Margin="0,7,0,0" TextWrapping="Wrap"
                                                   Foreground="{DynamicResource Long.Brush.State.Success}" />
                                        <TextBlock Text="{DynamicResource i18n.overview.status.releasePending}"
                                                   Margin="0,5,0,0" TextWrapping="Wrap"
                                                   Foreground="{DynamicResource Long.Brush.Text.Muted}" />
                                    </StackPanel>
                                    <Button Grid.Column="1"
                                            Content="{DynamicResource i18n.app.openCommandCenter}"
                                            automation:AutomationProperties.Name="{DynamicResource i18n.app.openCommandCenter}"
                                            Style="{StaticResource LongButton.Primary}"
                                            VerticalAlignment="Top"
                                            Click="OpenPalette_Click" />
                                </Grid>
                                <UniformGrid x:Name="OverviewMetricsGrid" Grid.Row="1"
                                             Columns="3" Margin="0,18,0,0">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource i18n.overview.metrics.plugins}"
                                                   TextWrapping="Wrap"
                                                   Foreground="{DynamicResource Long.Brush.Text.Muted}" />
                                        <TextBlock x:Name="OverviewPluginCount" Text="0"
                                                   FontSize="24" FontWeight="SemiBold" Margin="0,5,0,0" />
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource i18n.overview.metrics.commands}"
                                                   TextWrapping="Wrap"
                                                   Foreground="{DynamicResource Long.Brush.Text.Muted}" />
                                        <TextBlock x:Name="OverviewCommandCount" Text="0"
                                                   FontSize="24" FontWeight="SemiBold" Margin="0,5,0,0" />
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource i18n.overview.metrics.capabilities}"
                                                   TextWrapping="Wrap"
                                                   Foreground="{DynamicResource Long.Brush.Text.Muted}" />
                                        <TextBlock x:Name="OverviewCapabilityCount" Text="0"
                                                   FontSize="24" FontWeight="SemiBold" Margin="0,5,0,0" />
                                    </StackPanel>
                                </UniformGrid>
                            </Grid>
                        </Border>

                        <StackPanel x:Name="ManagementDestinationsPanel" Margin="0,0,0,20">
                            <TextBlock x:Name="ManagementPrimaryTitle"
                                       Text="{DynamicResource i18n.overview.management.primary}"
                                       Margin="0,0,0,10"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource Long.Brush.Text.Primary}" />
                            <UniformGrid x:Name="ManagementDestinationGrid"
                                         Columns="4"
                                         KeyboardNavigation.TabNavigation="Local">
                                <Button x:Name="DestinationPlugins"
                                        Tag="Plugins"
                                        Style="{StaticResource OverviewTaskButton}"
                                        automation:AutomationProperties.AutomationId="Long.Management.Destination.Plugins"
                                        automation:AutomationProperties.Name="{DynamicResource i18n.nav.plugins}"
                                        KeyboardNavigation.TabIndex="0"
                                        Click="ManagementDestination_Click">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource i18n.nav.plugins}" FontWeight="SemiBold" />
                                        <TextBlock Text="{DynamicResource i18n.overview.management.plugins}"
                                                   Margin="0,5,0,0" FontSize="11" TextWrapping="Wrap"
                                                   Foreground="{DynamicResource Long.Brush.Text.Secondary}" />
                                    </StackPanel>
                                </Button>
                                <Button x:Name="DestinationMarket"
                                        Tag="Market"
                                        Style="{StaticResource OverviewTaskButton}"
                                        automation:AutomationProperties.AutomationId="Long.Management.Destination.Market"
                                        automation:AutomationProperties.Name="{DynamicResource i18n.nav.market}"
                                        KeyboardNavigation.TabIndex="1"
                                        Click="ManagementDestination_Click">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource i18n.nav.market}" FontWeight="SemiBold" />
                                        <TextBlock Text="{DynamicResource i18n.overview.management.market}"
                                                   Margin="0,5,0,0" FontSize="11" TextWrapping="Wrap"
                                                   Foreground="{DynamicResource Long.Brush.Text.Secondary}" />
                                    </StackPanel>
                                </Button>
                                <Button x:Name="DestinationWorkflows"
                                        Tag="Workflows"
                                        Style="{StaticResource OverviewTaskButton}"
                                        automation:AutomationProperties.AutomationId="Long.Management.Destination.Workflows"
                                        automation:AutomationProperties.Name="{DynamicResource i18n.nav.workflows}"
                                        KeyboardNavigation.TabIndex="2"
                                        Click="ManagementDestination_Click">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource i18n.nav.workflows}" FontWeight="SemiBold" />
                                        <TextBlock Text="{DynamicResource i18n.overview.management.workflows}"
                                                   Margin="0,5,0,0" FontSize="11" TextWrapping="Wrap"
                                                   Foreground="{DynamicResource Long.Brush.Text.Secondary}" />
                                    </StackPanel>
                                </Button>
                                <Button x:Name="DestinationWidgets"
                                        Tag="Widgets"
                                        Style="{StaticResource OverviewTaskButton}"
                                        automation:AutomationProperties.AutomationId="Long.Management.Destination.Widgets"
                                        automation:AutomationProperties.Name="{DynamicResource i18n.nav.widgets}"
                                        KeyboardNavigation.TabIndex="3"
                                        Click="ManagementDestination_Click">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource i18n.nav.widgets}" FontWeight="SemiBold" />
                                        <TextBlock Text="{DynamicResource i18n.overview.management.widgets}"
                                                   Margin="0,5,0,0" FontSize="11" TextWrapping="Wrap"
                                                   Foreground="{DynamicResource Long.Brush.Text.Secondary}" />
                                    </StackPanel>
                                </Button>
                            </UniformGrid>
                            <TextBlock x:Name="ManagementSecondaryTitle"
                                       Text="{DynamicResource i18n.overview.management.secondary}"
                                       Margin="0,8,0,10"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource Long.Brush.Text.Primary}" />
                            <UniformGrid x:Name="ManagementUtilityGrid"
                                         Columns="4"
                                         KeyboardNavigation.TabNavigation="Local">
                                <Button x:Name="DestinationSystem"
                                        Content="{DynamicResource i18n.nav.system}"
                                        Tag="System"
                                        Style="{StaticResource OverviewUtilityButton}"
                                        automation:AutomationProperties.AutomationId="Long.Management.Destination.System"
                                        automation:AutomationProperties.Name="{DynamicResource i18n.nav.system}"
                                        KeyboardNavigation.TabIndex="4"
                                        Click="ManagementDestination_Click" />
                                <Button x:Name="DestinationDiagnostics"
                                        Content="{DynamicResource i18n.nav.diagnostics}"
                                        Tag="Diagnostics"
                                        Style="{StaticResource OverviewUtilityButton}"
                                        automation:AutomationProperties.AutomationId="Long.Management.Destination.Diagnostics"
                                        automation:AutomationProperties.Name="{DynamicResource i18n.nav.diagnostics}"
                                        KeyboardNavigation.TabIndex="5"
                                        Click="ManagementDestination_Click" />
                                <Button x:Name="DestinationDeveloper"
                                        Content="{DynamicResource i18n.nav.developer}"
                                        Tag="Developer"
                                        Style="{StaticResource OverviewUtilityButton}"
                                        automation:AutomationProperties.AutomationId="Long.Management.Destination.Developer"
                                        automation:AutomationProperties.Name="{DynamicResource i18n.nav.developer}"
                                        KeyboardNavigation.TabIndex="6"
                                        Click="ManagementDestination_Click" />
                                <Button x:Name="DestinationSettings"
                                        Content="{DynamicResource i18n.nav.settings}"
                                        Tag="Settings"
                                        Style="{StaticResource OverviewUtilityButton}"
                                        automation:AutomationProperties.AutomationId="Long.Management.Destination.Settings"
                                        automation:AutomationProperties.Name="{DynamicResource i18n.nav.settings}"
                                        KeyboardNavigation.TabIndex="7"
                                        Click="ManagementDestination_Click" />
                            </UniformGrid>
                            <TextBlock x:Name="ManagementNoResultsText"
                                       Text="{DynamicResource i18n.management.noResults}"
                                       Margin="0,8,0,0"
                                       Visibility="Collapsed"
                                       Foreground="{DynamicResource Long.Brush.Text.Muted}" />
                        </StackPanel>
                    </StackPanel>

                    <!-- 组合动作 -->
                    <Grid x:Name="PanelWorkflows" Visibility="Collapsed">
                        <ContentControl x:Name="WorkflowEditorHost"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Stretch"
                                        automation:AutomationProperties.AutomationId="Long.ToolCenter.WorkflowHost"
                                        automation:AutomationProperties.Name="组合动作工作台" />
                    </Grid>

                    <!-- 插件管理 -->
                    <Grid x:Name="PanelPlugins" Visibility="Collapsed">
                        <ContentControl x:Name="PluginManagementHost"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Stretch" />
                    </Grid>

                    <!-- Web Widgets -->
                    <Grid x:Name="PanelWidgets" Visibility="Collapsed">
                        <ContentControl x:Name="WidgetDashboardHost"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Stretch"
                                        automation:AutomationProperties.AutomationId="Long.ToolCenter.WidgetDashboardHost" />
                    </Grid>

                    <!-- 工作区插件设置模块 -->
                    <Grid x:Name="PanelPluginSettings" Visibility="Collapsed">
                        <ContentControl x:Name="PluginSettingsModuleHost"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Stretch"
                                        automation:AutomationProperties.AutomationId="Long.ToolCenter.PluginSettingsHost" />
                    </Grid>

                    <!-- Long 原生插件市场 -->
                    <Grid x:Name="PanelMarket" Visibility="Collapsed">
                        <ContentControl x:Name="MarketHost" />
                    </Grid>

                    <!-- 系统集成 -->
                    <Grid x:Name="PanelSystem" Visibility="Collapsed">
                        <ContentControl x:Name="SystemHost"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Stretch" />
                    </Grid>

                    <!-- 平台诊断 -->
                    <Grid x:Name="PanelDiagnostics" Visibility="Collapsed">
                        <ContentControl x:Name="DiagnosticsHost" />
                    </Grid>

                    <!-- 开发者 -->
                    <Grid x:Name="PanelDev" Visibility="Collapsed">
                        <ContentControl x:Name="DeveloperHost"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Stretch" />
                    </Grid>

                    <!-- 设置 -->
                    <Grid x:Name="PanelSettings" Visibility="Collapsed">
                        <ContentControl x:Name="SettingsHost"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Stretch" />
                    </Grid>
                </Grid>
                </Border>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
