# v1.11.0 S1C-F 真实睡眠/唤醒审计（2026-08-14）

## 结论

S1C-F 的生产事件合同、结构化质量探针和不可覆盖脚本已经完成；修复提交 `8d92725` 已通过自动化门禁和真实 Windows 睡眠、唤醒、解锁回执。本阶段正式关闭，下一步进入 S1C-G 显示器/DPI。

## 需求对齐

- 只接受宿主窗口真实收到的 `WM_POWERBROADCAST`：`PBT_APMSUSPEND` 后必须出现 `PBT_APMRESUMESUSPEND` 或 `PBT_APMRESUMEAUTOMATIC`。
- 锁屏/解锁和质量测试使用的可用性注入不能生成物理电源证据。
- 恢复后必须保持插件 session ID、WebView 内容身份和嵌入表面，并恢复宿主可见性与交互状态。
- 探针结束前必须停止插件会话并确认清理，报告同时保留挂起前与恢复后的资源样本。
- 脚本只允许从干净跟踪树启动，已有 ready/report 文件不可覆盖；超时后只终止本次启动的宿主进程树。

## 实现

- `HostEnvironmentStateService` 新增明确的物理电源转换事件，同时保留现有统一交互可用性合同。
- `PhysicalPowerRecoveryQualityProbe` 打开硬件监控 WebView，等待真实挂起/恢复序列并生成 schema 1 JSON。
- 新增启动参数 `--quality-power-recovery-report` 和 `verify-sleep-wake-recovery.ps1`。
- 脚本支持维护者从 Windows 电源菜单手动睡眠；显式传入 `-TriggerSleep` 时调用 `SetSuspendState` 触发真实系统睡眠，不提供模拟消息路径。

## 自动验证

- 电源事件、启动参数、脚本、解锁后恢复语义与防模拟回流定向测试：19 / 19 通过。
- `build_test.ps1` 首轮为 1427 / 1428，既有宏引擎 2 秒时序测试偶发超时；该测试独立复跑 1 / 1 通过。
- 探针修复后的 Release 全量门禁：1433 / 1433 通过。
- Release 解决方案构建：0 警告、0 错误。

## 物理验收

首次从 `c88d514` 运行真实睡眠/唤醒，宿主捕获 `PBT_APMSUSPEND` 和 `PBT_APMRESUMEAUTOMATIC`，两事件相隔约 38 秒。报告中挂起后不可用、最终交互恢复、会话身份、WebView 内容身份、嵌入表面和清理均通过；唯一失败项为 `restored_host_state=false`。

审计确认 Windows 在电源恢复消息到达时仍处于锁屏状态，用户解锁后才恢复交互。原探针把“恢复消息到达瞬间可交互”错误纳入宿主恢复条件，和要求维护者唤醒后解锁的验收流程冲突。修复后该瞬时状态只作为 `interaction_available_at_resume` 记录，门禁等待最终交互恢复后再验证 WebView 可见性。首次失败报告 SHA-256 为 `4b0b952d94ede29df8b1afb582f36bf2fcd7c3d3196d72d171c3b07d3a83221c`，原始文件保留在本机 `artifacts/quality/s1c-sleep-wake-c88d514/`。

修复提交 `8d927252ff487d0c78bb43d81dcf8e0d8551aeb3` 经 GitHub Repository CI `31764045338` 两个 Job 通过后完成第二次实机运行。宿主捕获 `PBT_APMSUSPEND` 和 `PBT_APMRESUMEAUTOMATIC`，UTC `02:36:43` 挂起、`02:37:03` 恢复；恢复消息到达时会话仍锁定，维护者解锁后最终交互与 WebView 宿主状态恢复。

最终报告 `passed=true`：挂起后不可用、最终恢复、会话身份、内容身份、嵌入表面和清理全部通过。句柄保持 1181，线程从 35 降至 30，私有内存从 310857728 降至 229511168 字节；探针结束后宿主残留进程为 0。ready 文件记录 session ID `305b5514bb2847ff9253c568f4a94573` 和内容身份 `57664488`。

- 最终报告 SHA-256：`949e0ece211360a0d667ab276da7c6b81a19f29d4e2a0b71db9e8ea6af3c3c81`
- ready 文件 SHA-256：`2dd4e069e2140687a141d34a9f0150025364127ff988a9f4fa60e8fc023def16`
- 本机原始证据：`artifacts/quality/s1c-sleep-wake-8d92725/`

代码合并且工作树干净后运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\verify-sleep-wake-recovery.ps1 -TriggerSleep
```

电脑进入睡眠后由维护者唤醒并解锁。只有最终报告满足 `physical_sleep_wake_recovery`、`event_source=WM_POWERBROADCAST`、身份保持、表面保持、交互恢复和清理全部通过，才能关闭 S1C-F。原始 `artifacts/**` 继续只保留本机，不提交仓库。

## 后续

1. S1C-F 已关闭，不再重复执行，除非后续电源生命周期代码发生变化。
2. 进入 S1C-G，先审计当前显示设备，再完成可执行的显示器拓扑和物理 DPI 验收。
