# Long BetterWindows 开发审计与后续计划（2026-07-23）

## 一、审计结论

当前主线与最初产品目标总体对齐，已经形成以 Windows 本地效率中枢为核心的完整产品骨架：统一命令中心、上下文搜索、Long 超级面板、插件生命周期、可信插件市场、系统能力接口和组合动作编排均已进入同一交互体系。项目参考 uTools 的是“统一入口、搜索即操作、插件承载和低打扰交互”逻辑，不实现 uTools 插件兼容层，也不复制其 API、Manifest 或包格式；这一边界保持正确。

本轮审计没有发现需要推翻产品方向或重写架构的 P0 代码缺陷。当前风险已经从“功能是否存在”转为“真实环境证据是否闭环、自动化是否稳定、发布外部条件是否完成”。因此后续不应继续无序增加工具数量，而应先完成组合动作真实交互验收、质量基础设施治理和 v1.9 外部门禁。

截至审计时：

- 已审计全部远端分支；原唯一领先提交 `90634c6` 已快进合入 `master`，其余远端分支均不领先主线。
- 本轮只纳入源码、测试、正式脚本、文档和一份最终通过证据；临时 dump、失败诊断和可再生成输出均已排除。
- Release 构建为 0 警告、0 错误。
- `LongBetterWindows.Tests` 为 437 / 437 通过。
- `git diff --check` 与 PowerShell 脚本语法检查通过。
- 长输出第三批已在当前多显示器 250% 缩放环境生成通过报告，批准、执行、3699 字符长度、限高滚动、清除和隔离报告均已闭环。

## 二、目标对齐矩阵

| 目标 | 当前状态 | 审计判断 | 仍需完成 |
| --- | --- | --- | --- |
| 优雅、华丽、扁平化、动效化 UI | Long Design System、统一卡片/按钮/圆角/色彩、宽屏与紧凑布局、减少动画和高对比模式均已接入 | 高度对齐 | 物理 DPI、读屏器和真实辅助功能人工签核；继续减少局部旧样式 |
| 参考 uTools 的统一交互逻辑 | 命令中心、超级面板、上下文 Provider、固定/最近使用排序、插件动作和升级失效处理已统一 | 高度对齐 | 继续优化搜索到执行的连续性 |
| 插件平台与内置插件 UI | 25 个插件、42 条命令、三类运行时、嵌入/分离窗口、可信市场和能力审计已形成闭环 | 高度对齐 | 真实 Registry/CDN 演练、干净机生命周期观察；逐步统一插件内部空态/错误态/加载态 |
| 组合动作编排 | Schema v3、输入/输出绑定、权限批准、顺序执行、补偿、报告、终端输出、安全导出和插件升级失效均已实现 | 功能主链对齐 | 导出拒绝验收；条件分支和并行继续延后 |
| 本地优先与安全边界 | 默认脱敏、终端值显式批准、报告不保存值、导出二次审查、可信市场事务恢复已完成 | 高度对齐 | 外部发布凭据和 unsigned 包人工门禁；清理质量产物入库策略 |
| 可测试与可发布 | 430 项自动化、Release 零警告、响应式及长输出桌面场景通过 | 基线健康 | 拆分过大的桌面脚本，并完成外部门禁 |

## 三、本轮开发内容审计

### 3.1 组合动作统一入口

新增 `ManagedWorkflowSearchProvider`，只发布本地托管、定义哈希稳定且针对当前插件目录预检有效的工作流。命令中心和超级面板共用 `OpenWorkflowReview` 动作与 `WorkflowReviewNavigation`，打开的是现有权限审查页，而不是绕过批准直接执行。固定和最近使用排序仍然有效。

审计判断：实现符合统一入口目标，也保持了“搜索结果不是伪插件命令”和“执行前再次加载、预检”的安全边界。对应纯逻辑测试覆盖有效工作流、无效定义、哈希变化和动作类型。

### 3.2 展示协调与响应式布局

执行审查、准备失败、执行结果、输出摘要、终端输出和报告详情已提取到 `WorkflowExecutionPresentation`；窗口层主要负责应用投影、焦点、剪贴板和对话框。Tool Center 与工作流编辑器修复了内容宽度不受视口约束、宿主未拉伸和窄窗只裁剪不重排的问题，默认宽屏与 `720 × 560` 紧凑布局已经通过完整桌面套件。

审计判断：职责边界比原来清晰，响应式实现对齐“优雅、扁平且不拥挤”的目标。下一步不应继续把质量状态堆入 `MainWindow`，而应把审查状态和可访问性状态收束为独立模型或 AutomationPeer。

### 3.3 长终端输出与敏感值处理

质量工作流可生成 100 个 UUID，目标输出约 3.7K 字符；终端输出视图使用限高滚动。标题栏增加明确的批准、确认、取消和清除动作，并提供清除快捷键。质量模式报告改写入工作流隔离根下的 `.reports`，不会污染用户 `%LocalAppData%`。

审计判断：产品功能、静态门禁和当前多显示器 250% 缩放环境的真实验收均已闭环。根因不是终端值处理，而是统一入口进入审查时同时物化重型编辑表单，以及长内容 UIA 树与坐标输入不稳定。编辑配置区现已和审查/运行/结果区分离；质量模式改用带 5 秒超时的窗口级消息驱动，仅在显式质量参数下开放。通过证据位于 `artifacts/quality/workflow-long-output-20260723/desktop-ui-smoke.json`。

### 3.4 插件安装事务稳定性

`LpakInstaller` 对 Windows 临时句柄占用造成的目录移动失败增加了最多 6 次、25–800ms 的有限指数退避，安装、卸载、回滚和启动恢复共用同一策略；目标已存在或源消失时不会盲目重试。

审计判断：修复范围受限，没有改变同卷原子移动和事务日志语义，解决了完整测试并行时的真实竞争问题。后续应保留一次安装/卸载并发回归，不扩大为无限重试。

## 四、风险与问题清单

### P0：发布或验收阻断项

1. **v1.9 外部门禁未完成**：SmartScreen、杀毒软件、首次启动、真实辅助功能、干净 Windows 及 Registry/CDN 受控凭据演练仍需人工或外部环境完成。
2. **发布证据边界**：当前 `v1.9.0-rc.1` 仍固定在 `d3cd956`，本轮组合动作改动不属于该 RC；若进入新候选，必须重新生成包、Manifest、哈希和完整证据，不能沿用旧 RC 结论。

### P1：下一阶段必须完成

1. **文件导出拒绝路径**：覆盖目标已存在、目录不存在、重解析点、审查后目标变化、值哈希变化和权限失败；必须验证无覆盖、无残留临时文件、报告不泄露终端值。
2. **桌面验收脚本治理**：`run-desktop-ui-smoke.ps1` 已承担过多场景与诊断输入；插件升级场景已拆为独立脚本，后续继续提取公共窗口驱动和报告汇总层。
3. **质量产物治理**：继续区分“应提交的最终证据”“本地诊断”“可再生成矩阵”，形成稳定的命名、保留和 `.gitignore` 策略；本次只提交选定的最终通过证据。

### P2：体验增强

1. 统一内置插件的加载、空态、错误、权限和结果组件，减少插件内部自定义样式偏差。
2. 在组合动作稳定后评估复制工作流、模板和参数预设。
3. 条件分支、并行执行、云同步和后台无人值守继续延后，直到权限、取消和恢复语义有正式协议。

## 五、后续开发顺序与完成定义

### 阶段 A：提交当前稳定基线（已完成）

目标：把统一入口、响应式修复、长输出实现、安装器有限重试、测试和审计文档纳入稳定主线。

完成定义：

- Release 0 警告、0 错误；430 / 430 测试通过。
- 只提交源码、测试、正式脚本、文档和明确选定的最终通过证据，不提交历史诊断 artifacts。
- 按项目当前协作约定直接提交并推送 `master`，保持本地与远端 ahead/behind 为 `0 / 0`。

### 阶段 B：标题栏自动化与长输出复验（已完成）

目标：消除对深层 `FindFirst`、固定坐标和合成修饰键的依赖。

实施顺序：

1. [x] 为审查批准、确认、取消、清除建立窗口级可测试状态与命令。
2. [x] 使用仅限显式质量模式的注册窗口消息驱动，避免深层 UIA 遍历、固定坐标和合成修饰键。
3. [x] 使用 `SendMessageTimeout` 增加调用超时和宿主存活边界。
4. [x] 在当前多显示器 250% 环境完成长输出场景；单屏 100% 保留为外部环境补充，不再阻塞 Stage C。

完成证据：批准、执行、`terminal-length = 3699`、限高滚动、清除和隔离报告均通过；失败路径使用 60 秒场景上限和 5 秒窗口消息上限。

### 阶段 C：插件升级失效验收（已完成）

目标：证明工作流不会使用过期插件定义或过期批准执行。

已完成：

1. [x] 搜索动作携带当前工作流定义和插件权限计划生成的状态指纹。
2. [x] 打开审查前重新加载托管定义并按当前插件目录预检，由运行会话拒绝搜索后发生的定义变化或 v1→v2 升级。
3. [x] 审查后升级由执行器实时预检拒绝，保留脱敏审计报告且不调用任何工作流命令。
4. [x] 纯逻辑测试覆盖定义变化、插件版本、注册修订、授权指纹和零步骤执行。
5. [x] 隔离桌面事务使用 v1 插件打开审查，经正式 `LpakInstaller` 升级 v2 后确认旧审查；结果为拒绝、零步骤、脱敏报告、事务目录清理和隔离根删除。
6. [x] `PluginScanner` 统一负责受跟踪目录插件的运行时释放与定点重载；审查或执行期间收到插件变化时保持审查快照，不同步重建重型编辑器。

完成证据：`artifacts/quality/workflow-plugin-upgrade-20260723/workflow-plugin-upgrade.json`。纯逻辑测试、隔离安装事务和真实 UI 场景均通过；拒绝原因可访问且不泄露敏感值。

### 阶段 D：文件导出拒绝验收

目标：证明二次审查后的所有变化都会安全拒绝。

已完成：

1. [x] 导出结果增加稳定失败分类，权限与 I/O 失败不再回显系统异常细节。
2. [x] 文件访问收敛到导出器专用内部边界，生产路径仍使用同目录 `CreateNew`、写穿透、落盘刷新和无覆盖移动。
3. [x] 确定性测试覆盖已有目标、缺失目录、重解析点、审查后目标变化、值变化、移动竞态、权限失败与取消；逐项核对无覆盖、无临时文件残留和结果不含终端明文。
4. [x] 质量模式窗口消息在真实宿主中复验拒绝矩阵；真实目录联接和拒绝写入 ACL 均生效，结束后恢复 ACL 并移除联接。
5. [x] 真实宿主成功路径校验 3699 字节内容与 SHA-256 后删除明文文件；隔离执行报告不含终端值，日志无错误。

完成证据：`artifacts/quality/workflow-terminal-export-full-20260723/desktop-ui-smoke.json`。所有拒绝场景均无覆盖、无残留、无报告泄露；成功场景保持同目录临时文件、写穿透和无覆盖原子移动。

### 阶段 E：v1.9 外部发布门禁

目标：在真实外部环境完成 unsigned 通道发布决策。

当前进度：

1. [x] 下载、干净 Windows、物理 DPI 和真实辅助功能均具有独立采集、人工批准与汇总验证器。
2. [x] HTTPS Registry 演练摘要明确区分预检与完整演练；只有部署、公开验证、回滚和再次验证全部通过才写入 `passed=true`。
3. [x] 新增统一外部发布决策器，绑定 Release Manifest、四类批准摘要、候选提交、分发通道、包文件、SHA-256 和完整 Registry 演练，并哈希锁定全部输入。
4. [ ] 在外部设备和受控 Registry/CDN 上生成真实批准摘要；不得使用测试夹具、同人审批或 `PreflightOnly` 结果代替。

完成定义：不同人员完成采集与批准；SmartScreen、杀毒软件、首次启动、物理 DPI、读屏器和完整插件生命周期均绑定同一候选提交与包哈希；Registry 最后提交并能回滚。

### 阶段 F：体验增强

外部门禁继续阻塞 v1.9 正式发布，但不冻结已经位于 `v1.9.0-rc.1` 之后的 `master` 产品开发。体验增强只能复用现有安全协议，不新增条件分支、并行或后台执行等协议级复杂能力。

当前进度：

1. [x] 支持把当前组合动作深拷贝为新 ID 的未保存草稿；不覆盖源文件、不继承定义哈希或报告，也不自动继承敏感输入保存批准。真实 Release 宿主已验证命令中心审查关闭、复制完成、源文件保留和副本未自动落盘，证据为 `artifacts/quality/workflow-duplicate-v13-20260723/desktop-ui-smoke.json`。
2. [x] 建立只读官方模板目录及导入信任边界：目录不进入托管仓储写接口，模板键禁止越界，采用前重新核对定义哈希；目录身份与 `official.templates` 来源声明都不自动提升信任，采用仍进入现有外部导入审查并生成未保存草稿。
3. [ ] 评估参数预设的作用域、敏感值和持久化语义。
4. [ ] 继续统一插件内部空态、加载态和错误态。

## 六、本次 GitHub 发布范围

本次提交包含：

- 统一工作流搜索与审查导航。
- 执行展示投影与对应测试。
- 宽屏/紧凑响应式修复和质量启动参数。
- 长终端输出、显式批准、清除、隔离报告和质量脚本。
- 复制为新草稿、隔离质量驱动和真实宿主证据。
- 只读模板目录、首个 UUID 模板、哈希锁定选择与导入审查复用。
- 插件事务目录移动有限重试。
- 当前状态、UI/UX 进度、编排协议与本审计文档。

本次提交明确不包含：

- `artifacts/` 下未跟踪的历史矩阵、截图和失败诊断目录。
- 任何真实 Registry/CDN 凭据、私钥或人工批准文件。
- 对 `v1.9.0-rc.1` 标签和既有候选包的修改。
