# Long助手 v1.11.0 当前开发审计与收口计划

> 审计日期：2026-08-14
>
> 产品代码基线：`194368bed16263b9c800a5ffe87b5ba251012255`；本次审计提交后以 `master` / `origin/master` 的共同 HEAD 作为 RC15 资产来源
>
> 源码身份：`v1.11.0-rc.15` 收尾演练身份；剪贴板重试修复已提交、推送并通过 CI，待生成新资产和完成桌面探索验收
>
> 目标版本：公开、稳定、透明披露风险的 unsigned `v1.11.0`

## 1. 审计结论

当前实现仍对齐最初产品方向，没有出现功能或设计路线偏移：Long助手保持 Long 自有的优雅、华丽、扁平化和动效化视觉体系，以 uTools 为统一唤起、搜索、上下文匹配、超级面板、工作区和插件化交互的参考，不实施 uTools 插件兼容层；Windows 原生、本地优先、能力授权和可审计插件平台仍是工程边界。

工程主体已经进入稳定版收尾，而不是继续扩功能的阶段。25 个内置插件、42 条命令、三类插件运行时、统一工作区、可信本地市场、安装/更新链路、LPWP Long助手侧合同和主要 UI 已经落盘。退出可观测性、关闭总预算、WebView 生命周期、工作区切换、后台长驻留、混合生命周期和真实托盘恢复均已形成自动化或实机闭环。

但项目**还不能宣称完全收口，也不能创建稳定版 Release**。S1 物理事件工程范围已经关闭，当前主要缺口转为发布政策代码、最终候选身份和人工/隔离环境证据。

## 2. 可复现基线

| 项目 | 2026-08-14 状态 | 判定 |
|---|---|---|
| 本地与远端 | 审计时 `master == origin/master == 194368b`，领先/落后 `0 / 0`；本次仅追加审计文档 | 通过 |
| GitHub PR | 审计开始时无开放 PR | 通过 |
| 主分支 CI | 产品代码 CI `31787011302`，两个 Job 全部成功 | 通过 |
| Release 构建 | 最近完整复核 0 警告、0 错误 | 通过 |
| 全量自动化 | 1461 / 1461 | 通过 |
| 插件/命令 | 25 / 42 | 对齐冻结目标 |
| 自动功能证据 | 87 条插件正向证据 | 通过 |
| 空闲内存 | 5×9 秒中位数 198.1 MB、最大 198.7 MB | 通过但接近 200 MB 门槛 |
| 当前候选 | RC14 作为失败探索证据保留；RC15 修复基线已就绪，尚未生成资产 | 演练进行中 |

`artifacts/**` 是本机原始证据和截图，未纳入本次提交；审计只把可复核结论、精确提交和必要哈希写入版本库。

## 3. 与最初需求的对齐情况

### 3.1 产品与交互

- 统一唤起、命令搜索、上下文匹配、超级面板、工作区和插件目录已构成统一效率入口；uTools 只作为交互逻辑参考，没有引入其插件兼容负担。
- 软件名称、应用图标、内部品牌和 README 已统一为“Long助手”。
- 亮暗主题、国际化、键盘路径、可访问性语义、DPI 适配和动效降级均有实现及自动证据；最终物理 DPI 和读屏人工签核仍未关闭。
- 当前不应继续扩大视觉改版或插件数量。除非人工验收发现阻断缺陷，稳定版前只做收口修复。

### 3.2 插件与平台

- 正式资产保持 25 个插件、42 条命令，其中 18 个 WebView2、7 个原生插件；ClipboardHistory 为 Web UI 加原生后台；C# Script 运行时与模板可用，但没有正式内置 Script 插件。
- DLL、C# Script、WebView2 三类运行时，Manifest Schema、验证器、打包器、SDK、UI Kit、市场事务和能力授权边界均已落盘。
- 真实 Long Grid、正式插件发布者身份和生产 Registry/CDN 不具备时，应延期到 v1.12，不能伪造为 v1.11.0 已通过。

### 3.3 安装、更新与发布

- 已有一键启动和一键打包入口，能够生成 self-contained ZIP、framework-dependent ZIP 和 Inno Setup 安装 EXE。
- 自动更新采用独立 RSA 清单签名与包哈希校验；更新私钥只允许保存在被忽略的 `local-secrets/`，不得进入 Git。
- 当前没有 Windows Authenticode 证书，稳定版必须明确记录 `distribution_channel=unsigned`、`signed=false`、`publisher_identity=unverified`，并提示 SmartScreen 与 SHA-256 核对。

## 4. 阶段审计

| 阶段 | 当前状态 | 剩余工作 |
|---|---|---|
| S0 计划与政策 | 完成 | 保持本文与总计划同步 |
| S1A 退出可观测性 | 完成 | 无 |
| S1B 关闭整体预算 | 完成 | 无 |
| S1C-A WebView 稳定性 | 完成 | 16 / 16 已通过 |
| S1C-B 工作区切换 | 完成 | 12 轮、84 次转换已通过 |
| S1C-C 后台/混合生命周期 | 完成 | 隐藏期零轮询、趋势和清理已通过 |
| S1C-D 托盘恢复 | 完成 | 8 / 8 实机通过 |
| S1C-E 锁屏/解锁 | 完成 | 真实锁定、解锁、恢复、身份保持和清理全部通过 |
| S1C-F 睡眠/唤醒 | 完成 | 真实挂起、自动恢复、解锁后恢复、身份保持和清理全部通过 |
| S1C-G 显示器/DPI | 工程关闭 | `16ada60` 真实 2→1→2 与 200%/250% 双屏矩阵通过；最终用户签字留在 S5 |
| S2A unsigned 合同 | 完成 | `53f5a4b` 统一策略、稳定版合同与回归测试通过 |
| S2B 单维护者模式 | 完成 | `f8235a4` 三层风险合同完成，`998237e` 稳定 CI 时序断言；默认严格模式不变 |
| S2C 外部生态延期 | 完成 | `1880714` 结构化凭证、两层哈希锁定、默认关闭和反伪造测试完成 |
| S3 RC15 演练 | 进行中 | 剪贴板写入有限重试已通过测试和 CI；待全套新资产与探索验收 |
| S4 v1.11.0 冻结 | 未开始 | RC15 无代码问题后再冻结正式身份 |
| S5 产品人工验收 | 未完成 | 25 / 25 插件及核心桌面体验真实签核 |
| S6 隔离环境验收 | 未完成 | 单机 VM/Sandbox、新用户、DPI、读屏、下载、安装/升级/卸载 |
| S7 发布 | 未开始 | 最终聚合门禁通过后创建不可变 GitHub stable Release |
| S8 发布后复核 | 未开始 | 更新发现、下载、安装和回滚复核 |

## 5. S1C-E 本次物理运行审计

修复 PowerShell 解析问题后的干净提交 `eb8dc3a` 已生成 ready 文件，并通过 `LockWorkStation()` 进入真实锁屏；维护者随后从本机控制台真实解锁，探针自行完成并退出。ready 证据记录：

- `plugin_id=com.long.hardwaremonitor`；
- `session_id=4493e03f8d6843fd8779c1db3b1e1555`；
- `content_identity=18137316`；
- 锁屏前 `interaction_available=true`；
- `session-recovery.ready.json` SHA-256 为 `a6d7ca9b05d1baf1742e05f5c23e35c943255ed98bb8c89a936049a195575e7a`。

最终 `session-recovery.json` 记录 `passed=true`：UTC `16:30:23` 真实锁定、`16:37:55` 真实解锁，锁定后交互不可用，解锁后交互、宿主状态、WebView 内容身份和嵌入表面全部恢复/保持，最终清理通过。资源从锁定前 1298 句柄、42 线程、约 336.6 MiB 私有内存降至解锁后 1199 句柄、28 线程、约 253.5 MiB 私有内存；监控器和宿主随后均退出。最终 JSON SHA-256 为 `a607ab7edbcea8ca07273b149e4e0e38c319e7a79b5c00096385b234e5881185`，标准错误为空文件。S1C-E 正式关闭；原始证据继续只保留本机 `artifacts/quality/s1c-session-eb8dc3a/`。

## 6. 当前阻断与风险

1. **S1 工程链已闭环**：锁屏、睡眠、显示器拓扑和双屏物理 DPI 均已通过；最终用户签字留在 S5/S6。
2. **外部生态依赖仍不可用**：政策与门禁已代码化；Long Grid、发布者身份和生产 Registry/CDN 仍按 v1.12 延期。
3. **RC15 尚未完成演练**：RC14 已冻结为失败探索证据；需从本次文档推送后的共同 HEAD 生成 RC15 全套新资产并完成探索验收。
4. **内存余量较小**：198.7 MB 虽低于 200 MB 硬门槛，但已超过 190 MB 预警线；最终候选必须原样复跑，不得用历史数字代替。
5. **人工与隔离证据不足**：25 插件正式人工批准仍未达到 25 / 25；没有第二台电脑时只能采用 Sandbox、VM 或新本地用户并披露隔离强度。
6. **签名边界**：没有 Authenticode 证书不是 unsigned 发布的代码阻断，但必须透明披露；RSA 更新签名仍是强制安全边界。

## 7. 后续严格顺序

### 第一步：关闭 S1 物理事件

1. S1C-E 锁屏/解锁已完成；把本次正式结论随审计文档推送。
2. S1C-F 真实睡眠/唤醒已完成；保留结构化结论与本机原始证据。
3. 审计当前显示设备；有可用多屏时执行拓扑变化，无第二显示器时如实记录受限范围，并完成本机可执行的物理 DPI 复跑。

### 第二步：实现 S2 发布政策

1. S2A 已完成：稳定版 unsigned Manifest、安装权限、README/Release Notes 和更新 RSA 合同已统一并通过 CI。
2. S2B 已完成：批准、摘要和最终聚合器支持显式 `single_maintainer`，默认严格模式、身份一致性和版本绑定均有回归测试。
3. S2C 已完成：缺失的 Long Grid、正式发布者身份和生产 Registry/CDN 以结构化 `deferred` 保留原因、目标版本和默认关闭状态。

### 第三步：RC15 与正式冻结

1. 拉取并确认 `master == origin/master`、领先/落后 `0 / 0`、无已跟踪改动，再从该精确提交生成 RC15 全套资产和哈希。
2. 完成 RC15 安装、首次启动、核心工作区、25 插件、失败路径、托盘、退出和资源复跑；发现任何代码问题即创建新的候选身份。
3. RC 演练不再发现代码问题后，独立提交冻结最终 `1.11.0`，重新生成所有正式证据，不复用历史 RC 产物或人工回执。

### 第四步：人工验收与发布

1. 对最终提交完成 25 / 25 插件和核心桌面人工回执。
2. 在本机可获得的最高隔离级别完成下载、SmartScreen、安装、升级、卸载、DPI、辅助技术和性能验收。
3. 运行最终聚合门禁，人工确认单维护者和延期风险，创建 GitHub stable Release。
4. 发布后验证自动更新发现、包下载/哈希、安装和回滚。

每一步继续采用文档、自动化/实机证据、独立提交、推送和主分支 CI 的闭环；当前按用户确认直接在 `master` 开发，不额外创建功能分支。不得提交 `artifacts/**` 或 `local-secrets/**`。

## 8. 收口判定

当前可判定为：**产品与架构方向对齐，主要代码功能接近完成，稳定版发布尚未收口。**

只有 S1–S4 全部完成、最终版本绑定干净提交，且 S5–S7 的人工/隔离/聚合门禁通过或按已批准政策明确延期后，才能称为“已收口并可发布”。目前下一开发批次固定为 S3 RC15 演练；不应插入新功能或大规模 UI 改版。
