# Long助手 v1.11.0 S1C-A WebView 会话稳定性审计

> 执行日期：2026-08-13
>
> 验收提交：`626f01a4a3a504eb996c19b5faa8fb2408358f82`
>
> 验收类型：单机真实 Release 宿主自动化复跑
>
> 结论：通过

## 1. 需求对齐

本批对应稳定版总计划 S1C 的“16 轮 WebView 会话创建/释放”。它只复核资源生命周期，
不新增 UI、功能或 uTools 插件兼容性，保持“Long 自有视觉、参考 uTools 交互逻辑”的既定
产品边界。

验收要求：

- 精确完成 16 轮 Base64 Web 插件会话创建与释放；
- 每轮页面成功打开、结束请求成功、会话与工作区内容完成释放；
- 32 个打开/释放资源样本完整；
- 相对首轮暖机基线，句柄增长不超过 64、线程增长不超过 8、私有内存增长不超过 64 MiB；
- 探针完成后宿主正常退出。

## 2. 执行条件与命令

执行前确认跟踪工作区干净，且没有正在运行的 `LongBetterWindows.Host`。随后从验收提交构建
Release 宿主并运行：

```powershell
dotnet build .\src\LongBetterWindows.Host\LongBetterWindows.Host.csproj -c Release --no-restore
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\verify-webview-lifecycle.ps1 `
  -OutputPath artifacts\quality\s1c-webview-626f01a\webview-lifecycle.json `
  -TimeoutSeconds 180
```

Release 构建结果为 0 警告、0 错误。原始 JSON 按项目策略仅保存在本机 `artifacts/**`，不进入
Git 历史。

## 3. 验收结果

| 项目 | 实测 | 门槛 | 结果 |
|---|---:|---:|---|
| 会话循环 | 16 / 16 | 16 / 16 | 通过 |
| 资源样本 | 32 | 32 | 通过 |
| 打开/结束/释放 | 每轮全部为 true | 不允许失败 | 通过 |
| 句柄增长 | +35 | <= +64 | 通过 |
| 线程增长 | -1 | <= +8 | 通过 |
| 私有内存变化 | -118,272,000 bytes（约 -112.8 MiB） | <= +64 MiB | 通过 |
| 探针总判定 | `passed=true` | true | 通过 |

本地原始证据 SHA-256：

```text
A896B39220F3114C0ACD0D0768367A61B1214F7456175BF7E7FDD3DE5CDF2BB1
```

## 4. 审计结论与下一步

S1C-A 已关闭。当前没有发现 WebView 会话、线程或私有内存持续累积，也没有需要修改代码的
阻断项。下一步严格按总计划进入 S1C-B：复跑 12 轮工作区多模块切换，检查 3 个插件模块与
4 个管理模块的身份复用、模块数量稳定、资源趋势和最终清理。
