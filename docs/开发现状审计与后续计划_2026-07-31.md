# Long助手开发现状审计与后续计划

审计日期：2026-07-31

审计分支：`agent/lpwp-widget-bridge`

审计提交：`6e68ad29e0fc2652a0d1393c9c00acd4bcec0954`

对应 Draft PR：[PR #14](https://github.com/Longyuyeee/Long_BetterWindows/pull/14)
最近完整 CI：[Repository CI #30568879734](https://github.com/Longyuyeee/Long_BetterWindows/actions/runs/30568879734)

本文是 2026-07-31 起的开发与交接事实源。更早审计文档保留阶段过程，
但提交、测试数量和后续顺序冲突时以本文及 GitHub 当前状态为准。

## 1. 审计结论

Long助手的主体产品方向已经基本对齐最初需求：

- 产品定位已经稳定为 Windows 统一效率入口；
- 参考 uTools 的搜索、命令、插件和快速唤起交互逻辑，但不实现 uTools
  插件兼容；
- 主界面、统一工作区、管理中心、插件市场、设置、主题、动效、响应式布局、
  DPI 和辅助功能已经形成统一工程体系；
- 25 个正式内置插件、42 条命令、四种插件运行形态、生产验证、确定性打包、
  市场信任和回滚工具已经落盘；
- Contracts、Testing、WPF、Web 与 IPC SDK 均已进入 `1.1.0` 版本边界；
- LPWP Widget 已经从协议推进到可见工作台、真实 WebView Surface、参考插件包
  和 IPC 客户端基础。

但项目目前仍不能判定为“完全收口并可正式发布”：

1. 当前 LPWP 成果仍位于串联 Draft PR，没有进入 `master`；
2. PR-C 只完成共享 IPC 客户端基础，Host Broker 服务、目录端点、命令调用、
   取消和 `plugin.open` 尚未完成；
3. PR-D 的 Broker 开关/诊断、兼容门禁和签名参考包尚未完成；
4. 25 项真实桌面插件人工验收仍为 `0 passed / 25 pending / 0 failed`；
5. 物理 DPI、任务栏、读屏、管理员性能、真实下载、干净 Windows 和
   Registry/CDN 回滚仍需要外部环境签核；
6. 当前最新稳定 Release 仍为 `v1.8.0`，最新预发布为 `v1.11.0-rc.2`；
   它们均早于本轮 LPWP 开发，不能代表当前源码。

当前阶段应定义为：

> 产品主体和自动化工程目标基本对齐；LPWP 正在从 PR-B 进入 PR-C；
> 代码可继续开发和评审，但主线整合、跨进程 Broker、真实桌面与发布门禁
> 未关闭，因此 `release_eligible=false`。

## 2. 分支、PR 与主线状态

### 2.1 当前提交链

| 比较 | Git 结果 | 说明 |
| --- | ---: | --- |
| `origin/master...origin/agent/lpwp-widget-contracts` | `0 / 1` | PR-A 比主线领先 1 个提交 |
| `origin/agent/lpwp-widget-contracts...origin/agent/lpwp-widget-bridge` | `0 / 14` | PR-B/PR-C-A 比 PR-A 领先 14 个提交 |
| `origin/master...origin/agent/lpwp-widget-bridge` | `0 / 15` | 当前开发分支比主线领先 15 个提交，无反向分叉 |

当前开放 PR：

| PR | Base ← Head | 状态 | 最近机器判断 |
| --- | --- | --- | --- |
| [#13](https://github.com/Longyuyeee/Long_BetterWindows/pull/13) | `master` ← `agent/lpwp-widget-contracts` | Draft、mergeable | PR Checks 全绿 |
| [#14](https://github.com/Longyuyeee/Long_BetterWindows/pull/14) | `agent/lpwp-widget-contracts` ← `agent/lpwp-widget-bridge` | Draft、mergeable | 手动 Repository CI 全绿 |

风险说明：

- PR #14 的 Base 不是 `master`，因此 `.github/workflows/build.yml` 中仅监听
  `pull_request.branches: [master]` 的自动 PR Checks 不会挂到 #14；
- 当前 #14 的 `statusCheckRollup` 为空，不能误写成“PR Checks 已自动通过”；
- 本轮通过的是针对 #14 Head 手动触发的完整
  [Repository CI #30568879734](https://github.com/Longyuyeee/Long_BetterWindows/actions/runs/30568879734)；
- `mergeable` 只表示 GitHub 当前未检测到文本冲突，不代表人工审查、验收或发布
  门禁已经通过。

推荐整合顺序：

1. 先审查并合并 PR #13；
2. 将 PR #14 的 Base 改为 `master`，或在更新后的 `master` 上重建等价分支；
3. 让 #14 获得面向 `master` 的自动 PR Checks；
4. 复核差异仍只包含 PR-B/PR-C 范围，再决定合并；
5. PR-C 后续较大服务端切片应独立分支/PR，避免继续扩大 #14。

未经用户明确确认，本审计不执行合并、改 Base、关闭 Draft 或创建 Release。

## 3. 产品与最初需求对齐度

| 最初目标 | 当前判断 | 证据与剩余边界 |
| --- | --- | --- |
| Windows 统一效率入口 | 基本对齐 | 命令面板、统一工作区、Launcher、管理中心和模块路由已落盘 |
| uTools 式交互逻辑 | 基本对齐 | 已参考搜索、命令、插件与快速唤起；按已确认范围不做 uTools 插件兼容 |
| 优雅、华丽、扁平化、动效化 UI | 工程侧对齐 | Long Design System、主题、动效、Reduced Motion、响应式和视觉矩阵已建立；最终美感仍需实机签核 |
| 内置插件 UI 一致性 | 基本对齐 | Web 状态组件与 WPF UI Kit 已形成；真实桌面抽查仍未全部完成 |
| 插件开发体系 | 高度对齐 | Schema、Validator、四运行时、SDK、Mock/Test Host、CI 模板和确定性 `.lpak` 已具备 |
| 插件与 Host 解耦 | 已完成既定目标 | 8 个原生生产组件不再直接引用 Host；公共合同由 SDK 承担 |
| Widget 与 Long Grid 互操作 | 进行中 | PR-A/PR-B 和 PR-C-A 已完成；Host Broker 与最小互操作尚未完成 |
| 安全市场和可回滚发布 | 工程侧基本完成 | 签名、Registry、事务安装与回滚工具存在；真实生产凭据演练未完成 |
| 一键启动、一键打包、自动更新 | 工具已具备 | 脚本、安装器、更新 Manifest 和签名链已落盘；当前 LPWP 提交尚未生成新 RC |

## 4. 当前代码与交付规模

### 4.1 正式插件

源码目录排除 `bin`、`obj` 和模板后：

| 指标 | 当前值 |
| --- | ---: |
| 正式插件 Manifest | 25 |
| 正式命令 | 42 |
| WebView 插件 | 18 |
| Native 插件 | 7 |
| 人工验收 | 0 passed / 25 pending / 0 failed |

`SamplePlugin` 仍计入正式矩阵；参考 Widget 位于 `samples/`，不重复计入
25 个正式内置插件。

### 4.2 SDK 与协议

| 交付物 | 版本/状态 |
| --- | --- |
| `LongBetterWindows.PluginSdk` | `1.1.0` |
| `LongBetterWindows.PluginSdk.Testing` | `1.1.0` |
| `LongBetterWindows.PluginSdk.Wpf` | `1.1.0` |
| `@long-assistant/plugin-sdk` | `1.1.0` |
| `LongBetterWindows.PluginIpc` | `1.1.0`，PR-C-A 已完成 |
| Plugin API | `1.1.0`，继续兼容 `min_api_version: 1.0.0` |
| LPWP | 1.0，PR-A 完成、PR-B 代码完成、PR-C 进行中 |

### 4.3 LPWP 里程碑

| 阶段 | 状态 | 已完成 | 未完成 |
| --- | --- | --- | --- |
| PR-A 共享契约 | 代码完成 | Widget DTO、Schema、语义和包资源校验、SDK 版本 | 合并进 `master` |
| PR-B Widget Bridge/沙箱 | 代码完成，桌面待验收 | Bridge、身份绑定、事件、虚拟来源、CSP、生命周期、状态、Surface、目录和 24 列工作台 | 真实安装、交互、重启、暂停和辅助功能验收 |
| PR-B 参考包 | 代码完成 | 双 Widget、确定性 unsigned `.lpak`、CI 制品、跨环境 SHA-256 基线 | 发布者签名与正式分发 |
| PR-C-A IPC 客户端基础 | 完成 | 信封、错误码、帧、SID 哈希 Pipe 名、`host.hello` 客户端、NuGet | Host 服务端未接入 |
| PR-C-B 只读 Host Broker | 未开始 | — | ACL、身份/完整性级别、握手状态、ping、catalog |
| PR-C-C 命令链 | 未开始 | — | invoke、cancel、deadline、并发、断线回收 |
| PR-C-D 互操作 | 未开始 | — | `plugin.open`、Golden Fixtures、Long Grid 最小客户端 |
| PR-D 发行门禁 | 未开始 | — | Broker 设置/诊断、签名参考包、兼容与迁移门禁 |

## 5. 自动化和远端证据

当前 Head `6e68ad2` 的本地结果：

- Release 解决方案构建：0 警告、0 错误；
- IPC/SDK 专项：32 / 32；
- .NET 全量测试：1061 / 1061；
- Web SDK 类型与 Mock Host：10 / 10；
- 四运行时生产验证和确定性打包：4 / 4；
- 参考 Widget SHA-256：
  `0FAD064CEEDD7AF04B3F5EAC5EC9CC3ED713D763D700AA43F87F3D775FB35CB3`。

远端 [Repository CI #30568879734](https://github.com/Longyuyeee/Long_BetterWindows/actions/runs/30568879734)：

- `Build and test`：success；
- `Plugin platform`：success；
- `repository-test-results`：已上传；
- `plugin-sdk-packages`：已上传；
- `lpwp-reference-widget`：已上传。

远端 SDK 制品已下载复核，包含四个 NuGet：

- `LongBetterWindows.PluginSdk.1.1.0.nupkg`；
- `LongBetterWindows.PluginSdk.Testing.1.1.0.nupkg`；
- `LongBetterWindows.PluginSdk.Wpf.1.1.0.nupkg`；
- `LongBetterWindows.PluginIpc.1.1.0.nupkg`。

IPC 包内只有自身 DLL/XML、README、Nuspec 和 NuGet 元数据，没有携带 Host 或
WPF 程序集。

## 6. 未关闭风险

### P0：正式发布阻断

1. 25 个插件真实桌面人工验收未完成；
2. 任务栏分组、固定/取消固定和多窗口身份未签核；
3. 100%、125%、150%、200% DPI、多显示器和跨屏行为未签核；
4. 高对比度、Reduced Motion、完整键盘路径和 Narrator/NVDA 未签核；
5. 管理员 WPR/WPA 原生性能归因未完成；
6. 真实 GitHub 下载、SmartScreen/杀毒、干净 Windows 安装/升级/卸载未签核；
7. 生产 Registry/CDN 发布、失败恢复和回滚演练未完成；
8. 当前 LPWP 提交尚未合并主线，不能从它生成正式候选。

### P1：LPWP 代码收口

1. PR-A/PR-B 的串联 PR 整合；
2. PR-C Host Broker 的身份、只读目录、命令与取消链；
3. Long Grid 最小互操作测试；
4. PR-D Broker 开关、脱敏诊断、兼容门禁和签名参考包；
5. PR-B 真实 WebView Widget 桌面验收。

### P2：发布后增强

- 扩展 Web/WPF 插件 UI Kit 组件覆盖；
- 独立进程运行时、CPU/内存配额和崩溃恢复；
- 统一插件目录、市场 Registry 和文档元数据；
- 公共 NuGet/npm 发布与长期版本策略。

P2 不应插队到 PR-C、PR-D 和 P0 人工门禁之前。

## 7. 后续开发计划

### 阶段 0：整理串联 PR

目标：让已经验证的 PR-A/PR-B/C-A 具备可审查、可自动检查的主线整合路径。

退出条件：

- PR #13 合并或获得明确处置；
- PR #14 面向更新后的 `master`；
- 自动 PR Checks 挂载并全绿；
- PR 差异不包含历史无关提交；
- 不重写或丢失当前已复核的参考包与 IPC 制品。

### 阶段 1：PR-C-B 只读 Host Broker

实施顺序：

1. Host 引用 `LongBetterWindows.PluginIpc`；
2. 创建 `LongPluginBrokerService` 与连接级状态机；
3. Named Pipe 服务端使用当前用户限制；
4. 复核客户端 SID、会话和进程完整性级别，不自动提权；
5. 首帧只接受 `host.hello`，失败后断开；
6. 实现 `health.ping`；
7. 实现脱敏的 `plugin.catalog.list/get`，不返回秘密设置或可执行对象；
8. 覆盖跨用户、错误协议、超帧、未握手、断线和多连接测试。

退出条件：

- Long Grid 测试客户端可连接、握手、ping 和读取只读目录；
- Long Grid 仍不能直接读取/执行 DLL、Script 或 WPF；
- Broker 尚未开放命令副作用；
- Release、全量测试、IPC 专项和 CI 全绿。

### 阶段 2：PR-C-C 命令调用与取消

实施顺序：

1. `command.invoke` 只调用现有命令编排与能力检查；
2. 参数使用稳定 DTO，不允许 CLR 类型名或任意反序列化；
3. deadline 和取消令牌传递到插件调用链；
4. 同插件默认最多 4 个并发命令；
5. Pipe 断开取消该连接全部未完成请求；
6. 无法取消时丢弃迟到结果；
7. 日志只记录审计 ID、方法、耗时、结果分类，不记录令牌、剪贴板或完整敏感参数。

退出条件：

- 最小客户端可调用一个低风险现有命令；
- 超时、取消、能力拒绝、插件崩溃和断线均映射为标准错误；
- 不引入第二套插件生命周期或权限系统。

### 阶段 3：PR-C-D UI 打开与互操作

实施：

- `plugin.open` 通过 Long助手现有统一工作区/窗口编排执行；
- 增加请求/响应 Golden Fixtures；
- 提供 Long Grid 最小互操作示例和 CI 说明；
- 验证 Long助手关闭、升级、回滚与断线降级。

退出条件：Long Grid 最小客户端能枚举并调用现有命令插件，PR-C 协议验收完成。

### 阶段 4：PR-D 发行与兼容门禁

实施：

- 设置中提供 Broker 开关，默认策略和升级策略明确；
- 提供可导出的脱敏 Broker 诊断；
- 市场最低 API/宿主版本和新增权限提示；
- 使用安全的发布者密钥流程生成签名参考 `.lpak`，私钥不进入 Git；
- 补齐安装、升级、回滚、卸载和双宿主版本兼容报告。

需要用户决定：

- SDK 是否公开发布到 NuGet/npm；
- 使用正式发布者密钥还是仅开发信任链；
- Broker 首版默认开启还是灰度开启。

### 阶段 5：真实桌面与受控环境验收

按固定顺序：

1. 11 个低风险插件和基础 UI/任务栏；
2. 5 个中风险插件；
3. 高风险插件与关键桌面副作用；
4. Widget 安装、多实例、移动缩放、重启恢复、暂停和辅助功能；
5. 物理 DPI、多显示器、任务栏、读屏和 WPR/WPA；
6. 真实下载、干净 Windows 和 Registry/CDN 回滚。

只有真实操作回执可以关闭这些门禁，自动化不得替代。

### 阶段 6：冻结与新 RC

前置条件：

- PR-A 至 PR-D 全部进入 `master`；
- 所有必须人工回执有效；
- 聚合门禁为 `release_eligible=true`；
- 源码提交冻结且工作区干净。

随后从冻结提交重新生成安装包、便携包、更新 Manifest、SHA-256 总账和
GitHub Prerelease。不得复用 `v1.11.0-rc.2` 的旧二进制。

## 8. 每批开发纪律

后续每个代码切片都必须：

1. 开始前核对 Base、Head、远端领先分支和工作区；
2. 明确本批协议范围与明确不做的内容；
3. 运行专项测试、Release 构建、全量测试和相关 SDK/运行时矩阵；
4. 更新本审计或对应实施交接；
5. 只暂存本批文件，不提交 `artifacts/`、密钥或生成二进制；
6. 独立提交并推送；
7. 等待 GitHub CI 终态；
8. 下载并复核新增远端制品；
9. CI 或制品未验证前不宣布本批完成。

## 9. 下一动作

当前最合理的下一动作不是继续扩 UI 或插件种类，而是：

1. 由用户/维护者确认 PR #13、#14 的串联整合方式；
2. 整合路径确认后进入 PR-C-B 只读 Host Broker；
3. 同时由用户开始低风险插件和参考 Widget 的真实桌面验收；
4. 在 PR-C/PR-D 与人工门禁关闭前，不生成新的正式 RC。

## 10. 2026-08-01 连续推进审计

### 10.1 主线整合

- PR #13 已以 `126b9c8f1f84c9611b7170e8ad2a92e2610187ff` 合入 `master`；
- PR #14 已改基线、重跑自动检查，并以
  `b198ece992112bff4b902300b6ba16a9aba164eb` 合入 `master`；
- PR-A、PR-B、PR-C-A 因而均已进入主线，不再属于串联 Draft PR 风险。

### 10.2 PR-C-B：只读 Host Broker

状态：**本地代码与自动化完成，等待独立分支 GitHub CI 终态**。

已完成：

1. Host 引用无 WPF 依赖的 `LongBetterWindows.PluginIpc` 契约包，并在插件运行时
   就绪后启动 Broker、宿主退出前停止 Broker；
2. Named Pipe 使用 `CurrentUserOnly`，连接建立后再次核对客户端 SID、登录会话和
   Token 完整性级别，任一不一致即失败关闭；Broker 不执行提权或跨完整性级别代理；
3. 连接状态机强制首帧为 `host.hello`，限制协议、帧大小和 deadline；
4. 当前仅暴露 `health.ping`、`plugin.catalog.list`、`plugin.catalog.get`；目录投影复用
   `HostProvider.PluginStore`，支持单调修订与 `if_revision`；
5. DTO 不返回插件目录、入口路径、能力列表、默认设置或进程内对象；命令执行、打开
   UI 和写操作在本切片中明确返回 `surface_not_supported`；
6. 新增 8 项 Broker 回归，覆盖安全边界不一致、握手前请求、错误协议、超大帧、
   脱敏目录、目录修订、缺失插件和并发连接。

本地证据：Release 解决方案构建 0 警告、0 错误；.NET 全量测试
`1069 / 1069`。本批未提交工作区既有 `artifacts/`。

下一步固定为 PR-C-C：复用现有 `CommandExecutor` 接入
`plugin.command.invoke/cancel`，落实调用级并发、deadline、断线取消、能力二次检查与
结果大小边界；不得新建第二套命令注册表或绕过 `PluginAccessContext`。

### 10.3 PR-C-C：命令调用与取消

状态：**本地代码与自动化完成，等待独立分支 GitHub CI 终态**。

已完成：

1. IPC 契约新增 `command.invoke`、`command.cancel`、输入上下文、结构化输出和取消回执；
2. Host 端复用现有 `CommandRegistry` 与 `CommandExecutor`，因此继续执行 Manifest 参数
   校验、插件按需启动和 `PluginAccessContext` 能力边界，没有第二套命令实现；
3. 同一连接支持多路并发请求和按请求 ID 分发响应，命令运行期间可以接收取消；
4. 每插件最多并发 4 个命令，deadline、显式取消和 Pipe 断开均传播到插件
   `CancellationToken`，迟到结果不再写回；
5. 命令输出按 UTF-8 字节计数限制为 512 KiB，完整响应继续受 1 MiB IPC 帧上限保护；
6. 客户端新增显式请求 ID 与 `CancelCommandAsync`；本地等待被取消时会安全消费迟到响应，
   不会污染同连接后续请求；
7. 错误映射覆盖 `plugin_not_found`、`command_not_found`、`invalid_request`、`timeout`、
   `cancelled` 和失败关闭的 `internal_error`。

验证证据：Broker 专项 `15 / 15`；Release 解决方案构建 0 警告、0 错误；.NET 全量
`1076 / 1076`。本批仍未提交工作区既有 `artifacts/`。

下一步固定为 PR-C-D：实现 `plugin.open` 的宿主调度边界、提交 IPC Golden Fixtures，
并形成不依赖 Long Grid 仓库的最小互操作探针；真实 Long Grid 端到端仍需对方工程接入。

### 10.4 PR-C-D：打开 UI、Golden Fixtures 与互操作探针

状态：**本地代码与自动化完成，等待独立分支 GitHub CI 终态**。

已完成：

1. Broker 新增 `plugin.open`，复用 `PluginMainUiLauncher` 的按需启动、运行状态和
   `IHasMainUI` 边界；来自 Pipe 后台线程的调用统一调度到 WPF Dispatcher；
2. 缺失插件、无主界面和启动失败分别映射为 `plugin_not_found`、
   `surface_not_supported`、`plugin_crashed`；
3. 新增 6 个 IPC Golden Fixtures，覆盖 hello、command.invoke、plugin.open 和标准错误；
   测试执行 JSON 语义无漂移往返以及公开 DTO 形状校验；
4. Fixtures 随 `LongBetterWindows.PluginIpc` NuGet 放入 `fixtures/ipc/`，已本地解包复核；
5. 新增仅依赖 IPC 包的 `long-ipc-probe`，支持 hello、ping、catalog/get、invoke 和 open，
   并以 JSON/稳定退出码表达结果；离线失败路径已实际运行验证；
6. 新增 Long Grid 最小接入说明，明确 feature 协商、取消、重试、离线和重连验收；
7. Broker 与 Golden Fixture 测试已纳入 GitHub `Build and test` 全量门禁；当前推送令牌
   无 `workflow` scope，因此不在本批改写 `Plugin platform` 的专项筛选表达式。

验证证据：Broker + Golden 专项 `20 / 20`；Release 解决方案（含 Probe）构建 0 警告、
0 错误；.NET 全量 `1081 / 1081`；IPC NuGet 已确认包含 DLL、README 和 6 个 Fixtures。

代码侧 PR-C 已收束。下一阶段进入 PR-D：Broker 开关与脱敏诊断、安全威胁模型、
兼容/迁移门禁和签名参考包。真实 Long Grid 跨仓库端到端、真实 Widget 桌面交互和
25 插件人工回执仍属于外部验收，不得因本批自动化通过而标记完成。

### 10.5 PR-D-A：Broker 开关与脱敏诊断

状态：**本地代码、UI 与自动化完成，等待独立分支 GitHub CI 终态**。

已完成：

1. `%LocalAppData%/LongBetterWindows/broker.json` 保存 Broker 启用状态，首次运行和配置
   损坏时默认启用；写入采用同目录临时文件与原子覆盖；
2. 设置页新增中英文“Long Grid 互操作”卡片，可即时启用/停用；停用会取消接受循环、
   断开现有连接并取消连接内命令，重新启用无需重启宿主；
3. 新增脱敏诊断导出，只包含启动/捕获时间、运行状态、连接计数、方法白名单计数和
   标准错误码计数；未知方法统一为 `unsupported`，未知错误统一为 `internal_error`；
4. 诊断明确不保存请求 ID、命令参数、响应内容、SID、进程 ID、插件目录或入口路径；
5. 设置页响应式布局、Automation Name、礼貌 Live Region 与中英文资源已接通；
6. 设置持久化与诊断专项覆盖默认值、损坏回退、原子写入和敏感字段拒绝。

验证证据：Release 解决方案构建 0 警告、0 错误；设置/Broker/本地化专项
`103 / 103`；.NET 全量 `1084 / 1084`；暗色中文设置页工程截图成功生成并人工检查
既有布局无回归。本批仍不提交任何质量截图或用户 `artifacts/`。

下一步进入 PR-D-B：提交 Broker/Widget 威胁模型和自动兼容门禁，随后审计签名参考包
所需密钥与现有发布签名工具；真实私钥继续只存在本地，不进入 Git。

### 10.6 PR-D-B：安全威胁模型与机器兼容门禁

状态：**本地实现与验证完成，待独立分支 GitHub CI 终态**。

主线复核：PR #15、#16、#17、#18 已依次通过 GitHub `Build and test` 与
`Plugin platform`，并分别合入 `master`；当前 PR-C 已完整进入主线，PR-D-A 也已收束。

本批完成：

1. 新增 `docs/protocol/LPWP_SECURITY_THREAT_MODEL.md`，明确资产、信任边界、攻击面、失败行为、
   隐私与日志规则、密钥规则、残余风险及发布条件；明确 Long Grid 不直接加载 Long助手插件原生代码；
2. 新增机器可读基线 `docs/protocol/lpwp-compatibility-baseline.json`，锁定 LPWP 1.0 协议、API 版本、
   帧与 deadline 边界、每插件并发上限、7 个 Broker 方法、安全不变量及 6 个 Golden Fixtures；
3. 新增兼容门禁测试，把机器基线与运行时常量、公开方法、Fixtures 和关键安全源码不变量双向校验，
   协议或安全边界发生漂移时直接阻断；
4. 新增 `verify-lpwp-compatibility.ps1` 与可双击的 `验证LPWP兼容性.cmd`，一次执行 Release 构建、
   Broker/IPC 契约、Web SDK、四运行时矩阵、参考 Widget 打包和 IPC NuGet Fixtures 解包校验，
   并在仓库外输出带 SHA-256 的 JSON 报告；报告保守保持 `release_eligible=false`，不伪造外部验收；
5. 兼容门禁的首次组合运行捕获到 deadline 与连接析构并发时重复取消已释放 CTS 的竞态；Broker 清理已
   改为幂等安全取消，并把 deadline 回归提升为同连接连续 12 次超时；
6. 修复解决方案并行构建时 Host 清理插件输出目录与插件项目复制本地化文件竞争的问题；重复 Release
   构建恢复为 0 警告、0 错误。

本地验证证据：兼容门禁完整通过，聚焦 .NET `31 / 31`、Web SDK `10 / 10`、运行时矩阵 `4 / 4`、
参考 Widget 打包及 IPC NuGet 6 个 Fixtures 解包校验通过；Release 构建 0 警告、0 错误；.NET 全量
`1087 / 1087`。本地报告生成于系统临时目录，不提交构建产物或用户 `artifacts/`。

下一步固定为 PR-D-C：复核现有 Marketplace 发布签名链和本机仓库外密钥状态，补齐“参考 Widget →
签名元数据 → 本地可信根验证 → 可复核报告”的一键路径。若不存在用户认可的正式/开发发布者私钥，
该门禁保持阻塞并明确交由用户决定，不生成临时私钥冒充正式发布者。随后进入 Long Grid 跨仓库真实 E2E、
25 插件人工回执和真实桌面/安装升级外部门禁。

### 10.7 PR-D-C：签名参考 Widget 与独立可信根复核

状态：**代码链与自动验证完成；真实正式签名产物因缺少 Marketplace 发布者私钥而按设计阻塞**。

已完成：

1. 新增本地 Bundle 验证管线与 `verify-bundle` CLI，先复用部署计划校验 Registry/发布报告/包哈希一致性，
   再通过宿主 `PluginPackageValidator` 的 Remote Registry 路径逐包验证 RSA-SHA256 与 PublisherSigned 信任级别；
2. 验证器不直接信任 Bundle 自带的信任片段，调用方必须另外提供预期 Publisher Key ID 与 64 位公钥
   SHA-256 指纹；二者任一不符即阻断，验证报告不包含签名正文或公钥 PEM；
3. 新增 `verify-marketplace-bundle.ps1`，可对离线发布 Bundle 输出原子化、脱敏、机器可读报告；
4. 新增 `publish-signed-reference-widget.ps1`，串联确定性参考 Widget 打包、现有 Marketplace 发布签名、
   独立身份复核和收口摘要；仓库内私钥必须命中 `.gitignore`，脚本本身不生成私钥，最终摘要继续明确
   `external_long_grid_e2e=false`、`manual_widget_validation=false`、`release_eligible=false`；
5. 新增 `docs/protocol/LPWP_SIGNED_REFERENCE_RELEASE.md`，明确自动更新私钥与 Marketplace 发布者私钥
   禁止混用，并给出真实签名和独立复核操作；
6. 本机仅发现已忽略的应用自动更新私钥，没有 Marketplace 发布者私钥。未读取或输出私钥内容，未生成
   临时正式身份，也未把任何密钥或签名构建产物加入 Git。

自动验证证据：Marketplace 发布/签名/可信根聚焦测试 `6 / 6`（新增正常复核和不可信根拒绝路径）；
PowerShell 两个新入口静态解析通过；Release 构建 0 警告、0 错误，.NET 全量 `1090 / 1090`。
真实签名参考包需要用户确认 Marketplace 发布者私钥、Key ID、
Publisher 名称和正式 HTTPS 包地址后才能生成。

下一步进入 PR-D-D：把 LPWP 兼容报告、签名参考包状态和外部回执统一并入最终发布门禁；代码层继续尽量
消除可自动完成的缺口，但 Long Grid 跨仓库 E2E、25 插件真实桌面回执、正式发布者身份及安装/升级环境
仍只能由对应真实环境关闭。

### 10.8 PR-D-D：LPWP 最终收口聚合门禁

状态：**本地实现与完整聚合实跑完成，待独立分支 GitHub CI 终态**。

已完成：

1. `verify-final-closure.ps1` 在非跳过模式下调用完整 LPWP 一键兼容门禁，并将协议版本、提交、
   .NET 契约、Web SDK、四运行时矩阵、6 个 Fixtures、IPC 包与参考 Widget SHA-256 纳入最终报告；
2. LPWP 临时包和报告仅生成在系统临时目录，读取后清理，不污染仓库或最终证据目录；
3. LPWP 子门禁失败、报告缺失、提交不一致、协议漂移或任一自动矩阵未通过都会写入 `machine_blockers`；
   使用 `-SkipBuildAndTests` 时明确增加“LPWP 被跳过”阻断，不能形成机器就绪假象；
4. 最终人工/外部清单由 7 类扩展为 10 类，新增 `lpwp-long-grid-e2e`、`lpwp-widget-desktop`、
   `lpwp-signed-reference`，分别保持跨仓库、真实桌面和正式发布者身份的诚实阻断状态；
5. 用户最终验收手册与收口交接同步要求 `lpwp_compatibility.valid=true`，并说明 10 类当前基线。

完整聚合实跑结果：Release 构建通过、全量测试通过、`long.plugin.ipc/1.0` 兼容门禁有效、6 个 Fixtures
有效、`machine_blockers=[]`、`ready_for_human_validation=true`；当前 10 类人工/外部状态均未关闭，故
`release_eligible=false`，符合实际情况。

下一步不再扩张产品主干功能。继续补齐 LPWP 三类外部证据的不可变采集/批准/聚合合同，并复核现有
最终外部发布决策是否完整消费插件人工回执、任务栏、WPR 与 LPWP 证据；随后只能等待用户和 Long Grid
工程在真实环境执行验收。

### 10.9 最终决策完整性 A：强制产品验收合同

状态：**本地实现与专项测试完成，待独立分支 GitHub CI 终态**。

审计发现：旧 `verify-external-release-gate.ps1` 已严格消费真实下载、干净 Windows、物理 DPI、辅助功能
和 HTTPS Marketplace 部署回滚证据，但没有强制消费 25 插件人工回执、任务栏视觉、WPR 与 LPWP 三类
证据，因此理论上存在“外围发布环境全通过、产品人工验收仍缺失却生成最终决策”的假阳性路径。

本批修复：

1. 最终外部门禁新增强制 `ProductAcceptanceGatePath`，要求分类为
   `approved_final_product_acceptance` 且通过；
2. 合同锁定 25 个插件、42 条命令、25 份插件人工回执和五个精确验证 ID；多项、少项、重复或改名均拒绝；
3. 五份批准与插件矩阵必须作为 `<摘要名>.sources/` 可移植来源随附；门禁复算 SHA-256 后再次解析内容，
   核对提交、分类、状态、审核人、插件数量和 `release_eligible`，不能通过同步篡改摘要哈希绕过；
4. 最终决策新增 `product_acceptance` 指标、schema 版本和输入哈希；安装升级文档已同步新的必填参数；
5. 外部门禁专项 `31 / 31` 通过，覆盖一致候选通过及不完整产品验收拒绝路径。

下一步固定为完整性 B：实现五类证据的不可覆盖批准回执与 `verify-final-product-acceptance.ps1` 汇总器，
让新增强制合同从“可靠阻断”变为用户完成真实验证后可正常关闭的路径。

### 10.10 最终决策完整性 B：批准回执与可移植汇总器

状态：**代码实现完成，真实回执按设计等待用户/Long Grid/发布者环境**。

已完成：

1. 新增 `approve-final-validation-evidence.ps1`，以精确 ID 生成任务栏视觉、WPR、Long Grid E2E、
   Widget 桌面和签名参考包五类不可覆盖批准回执；要求干净跟踪提交、Release 宿主哈希、审核人、说明、
   `artifacts/quality/` 下至少一份原始证据及显式 `-ConfirmPassed`；
2. WPR 回执先调用现有原始证据完整性验证；Long Grid 回执要求双方提交、LPWP 1.0 和六个核心方法；
   签名参考回执要求独立输入 Publisher Key ID/指纹并与已验证签名报告匹配，不接受自动更新密钥替代；
3. 新增 `verify-final-product-acceptance.ps1`，要求插件矩阵 25/25 回执、25 插件/42 命令且
   `release_eligible=true`，并要求五份精确命名回执已进入 Git、绑定当前提交与当前 Release 宿主哈希；
4. 汇总器将五份回执和脱敏插件矩阵复制为同名 `.sources/`，生成外部门禁要求的
   `approved_final_product_acceptance` 摘要；失败时清理临时/半提交来源目录，拒绝覆盖既有摘要；
5. 签名参考报告新增明确分类与通过状态；插件矩阵新增稳定分类；
6. 实跑发现 Windows PowerShell 5.1 没有 `Path.GetRelativePath`。新批准脚本和既有
   `approve-plugin-manual-evidence.ps1` 均改为受边界检查的兼容实现，避免用户完成真实验证后卡在回执写入。

当前不会生成任何通过回执：25 项插件人工检查、五类最终验证、正式 Marketplace 身份及 Long Grid
跨仓库报告仍需真实执行。代码侧关闭路径已经完整，下一步只继续自动化能验证的失败路径和文档一致性，
不以测试夹具冒充真实验收。

验证结果（2026-08-01）：

- 五个相关脚本均通过 Windows PowerShell 语法解析，且实测兼容有序字典属性写入和 Git 已跟踪绝对路径检查；
- 未传 `-ConfirmPassed` 的批准命令按设计失败；跟踪工作区不干净时汇总器按设计失败且不留下摘要；
- 最终产品验收、插件人工批准、插件正向矩阵和外部门禁专项测试 `45 / 45` 通过；
- Release 全量构建 `0` 警告、`0` 错误，全量测试 `1094 / 1094` 通过。

### 10.11 最终决策完整性 C：修复候选提交与回执提交的循环依赖

二次闭环审计发现：回执最初绑定生成时的 `HEAD`，但汇总器又要求该值等于提交回执后的 `HEAD`；正常提交
回执必然改变提交号，因此旧流程无法到达成功状态。

修复策略：批准器与汇总器都强制接收同一个完整 `ExpectedSourceCommit`。它必须是当前 `HEAD` 的祖先；从冻结
候选到当前提交之间，只允许 `docs/plugin-manual-approvals/` 与 `docs/final-validation-approvals/` 下的 JSON
回执变化。产品、脚本或其他文档一旦变化，批准和汇总都会失败，必须冻结新候选并重新验收。插件矩阵仍在当前
干净 `HEAD` 执行，以消费已经提交的回执，但最终可移植摘要统一绑定冻结候选提交。这样既允许逐份提交真实回执，
又不允许借“证据提交”掩盖候选源码漂移。

验证结果：两个脚本通过 Windows PowerShell 解析，专项测试 `3 / 3`；Release 全量构建 `0` 警告、
`0` 错误，全量测试 `1094 / 1094` 通过。

### 10.12 LPWP 外部证据可执行性：Long Grid E2E 摘要生产器

新增 `new-lpwp-long-grid-e2e-evidence.ps1`，由真实操作者把 Long助手冻结候选提交、Long Grid 受测提交、
六个核心方法的逐项确认以及原始日志/录像哈希收束为不可覆盖的 `lpwp_long_grid_e2e` 摘要。输出和输入均限制
在 `artifacts/quality/`，并复用冻结候选后的“只允许批准 JSON 变化”规则。

最终批准器进一步要求摘要 schema、操作者、六个精确方法和原始证据集合完整；复算调用者同时提供的原始证据
路径、大小和 SHA-256，并拒绝操作者与批准人相同。该入口降低跨仓库交接的手工 JSON 风险，但不会运行或伪造
Long Grid：握手、目录、调用、取消与打开仍必须由两端真实进程完成。

验证结果：新增与批准脚本均通过 Windows PowerShell 解析，专项测试 `4 / 4`；Release 全量构建 `0`
警告、`0` 错误，全量测试 `1095 / 1095` 通过。

### 10.13 主线最终收口复核与执行交接

在主线 `c1462b49a43070c06b7344e92db475d1a3b6d763` 实跑完整 `verify-final-closure.ps1`：跟踪工作区
干净，Release 构建、全量测试和 LPWP 兼容套件通过，`machine_blockers=[]`、
`ready_for_human_validation=true`。插件合同为 25 插件/42 命令/47 项自动证据，真实人工回执仍为
`0 / 25`；剩余人工/外部类别精确为 10，`release_eligible=false`。

新增 `收口执行交接_2026-08-01.md`，把换电脑后的拉取、重新聚合、冻结候选、25 插件回执、五类最终批准、
最终产品摘要和统一外部门禁整理为单一路径，并明确候选 EXE 保存、逐回执提交与禁止伪造规则。至此不存在已知且
仍可由当前代码环境独立关闭的产品阻塞；下一阶段是用户、Long Grid、管理员会话和发布基础设施的真实执行。

### 10.14 RC3 版本唯一性修复与权威状态收束

2026-08-01 再审计发现：GitHub `v1.11.0-rc.2` 已绑定历史提交
`26b5732e7a78cb1c605897afa5a4159eabeb9f94` 并公开安装器、双 ZIP、发布清单与更新清单；当前修复后
主线比该标签前进 180 个提交、涉及 699 个文件，却仍声明相同版本号。继续打包会形成同版本不同源码/哈希，
违反不可变 Release 和自动更新身份边界。

本阶段将项目、安装器、发布脚本、IPC Fixture、质量测试和操作文档统一提升为 `v1.11.0-rc.3`，保留四段式
Windows 文件版本 `1.11.0.0`。`CHANGELOG.md` 新增 RC3 的统一工作区、插件平台、LPWP 与发布门禁摘要；
README 和 `当前开发状态.md` 明确 RC2 是不可覆盖的失败候选、RC3 尚未发布且仍需 10 类真实验收。

RC3 版本提交进入 `master` 后必须重新执行 `verify-final-closure.ps1`；只有新的完整主线提交可作为
`ExpectedSourceCommit`，版本提升前的报告不能直接批准 RC3。

验证结果：打包预检解析 `version=1.11.0-rc.3`、输出目录不存在、.NET/Inno Setup/发布脚本均就绪；
版本、打包入口和 IPC Fixture 专项 `69 / 69` 通过。新增跨项目/脚本/安装器/Fixture 版本一致性回归后，
Release 全量构建 `0` 警告、`0` 错误，全量测试 `1096 / 1096` 通过。

### 10.15 RC3 插件人工验收导航与候选防错

进入真实验收前的可执行性审计发现，既有矩阵、回执和失效规则完整，但操作人仍需手工抄写插件 ID、检查 ID
和受测 EXE，且没有一个入口能证明解压目录里的 EXE 就来自冻结候选 ZIP。该缺口容易导致“操作正确但验错构建”。

本阶段新增 `plan-next-plugin-validation.ps1`：先调用权威矩阵校验，再验证当前版本、干净 Release Manifest、
候选提交祖先关系、自包含 ZIP 的 Manifest 哈希，以及解压 EXE 与 ZIP 内 EXE 的 SHA-256 一致性。冻结候选之后
只允许新增插件人工批准 JSON；产品、脚本、测试或普通文档变化均使候选过期。脚本按
`low → medium → high → critical` 和矩阵原序输出下一项、证据目录与显式冻结 EXE 的批准命令模板，也可按
插件 ID 定位；它不会启动操作、采集证据或生成批准。

由于本阶段本身修改了发布脚本，提交 `565f26305c7f6d2c8446fc742f32fb5e188a3e57` 生成的首批 RC3 本地包
按规则作废。合入后将在新 `master` 重跑完整聚合并用 `-ReplaceExisting` 重建同一未发布 RC3 本地候选；新的
`release-manifest.json` 提交号、包哈希和 EXE 哈希才是后续人工验收权威对象。真实状态仍为 0 / 25 回执，
不得用规划器输出冒充人工通过。

### 10.16 RC3 一键人工验收会话

规划器解决了“下一项是什么、受测对象是否正确”，但实操仍需手工创建目录并启动 EXE。继续审计发现，如果
开发构建宿主仍在运行，Windows 单实例/进程复用可能让操作人误以为已启动冻结候选；多次尝试共用同一证据目录
也会使截图和结论边界不清。

新增 `start-plugin-validation.ps1`：复用规划器全部校验，启动前拒绝任何运行中的 Long助手宿主，再创建仅属于
本项检查的 `artifacts/quality/` 证据目录和不可覆盖 `validation-session.json`，最后用显式文件路径与工作目录
启动冻结 EXE。会话绑定候选提交、插件/检查、命令、EXE 哈希、启动时间和进程 ID；`-PrepareOnly` 可只准备，
`-PluginId` 可定点选择。已有目录、不同 EXE、旧候选或并行宿主均失败关闭。

该脚本只把人工验收送到正确起点，状态固定为 `pending_human_observation`，不会采集截图、点击 UI、判断美观或
生成批准回执。本阶段修改了脚本和文档，因此前一 RC3 本地候选再次失效；合入后仍须在新主线重跑完整收口、
重建安装器/双 ZIP 和更新签名，再以新会话文件开始 Base64 真实验收。

### 10.17 Prepared 会话恢复竞态修复

首次真实执行 `start-plugin-validation.ps1 -PrepareOnly` 后发现：会话已按设计创建目录，但默认启动仍把所有既有
目录视为冲突，导致交接文档中的“先准备、后启动”无法到达。修复后，仅允许候选提交、插件 ID、检查 ID、
EXE 哈希、人工 pending 状态完全一致且 `launch_status=prepared_only` 的会话恢复。脚本在启动进程前先用旧文件
SHA-256 原子切换为 `launching`，并在启动成功或失败后再次锁定中间哈希原子写入结果；并发启动、文件变化、
旧候选和其他既有目录仍失败关闭。该修复不生成通过结论，已有本地 prepared 会话也不会被提交。

### 10.18 人工会话到批准回执的防转录完成入口

会话启动链闭环后，最后仍需从 JSON 手工复制插件 ID、检查 ID 和受测 EXE 到批准命令，存在选错项或误用开发
EXE 的风险。新增 `complete-plugin-validation.ps1`：重新执行候选规划，要求会话与当前 pending 检查在版本、提交、
插件、检查、受测路径及 SHA-256 上完全一致，并要求 `launch_status=started`、人工状态仍为 pending。

调用者只提供审核人、真实观察说明、会话目录内至少一份非空 UI 证据及 `-ConfirmPassed`。脚本拒绝目录外文件，
拒绝用 `validation-session.json` 代替截图/录像，并把会话文件作为附加哈希证据交给既有不可覆盖批准器。输出状态
为 `receipt_created_pending_commit`，提醒操作者继续审查并单独提交回执；失败或未观察时不运行。本入口不读取
截图内容、不自动判断通过，也不改变当前 0 / 25 的真实人工状态。

### 10.19 人工验收启动直达与正常产品路径隔离

继续按执行顺序审计时发现，一键会话虽然已经锁定正确候选、插件与检查，但启动后仍停留在宿主默认页面，操作人
还需从 Launcher 手工检索待验插件。这一步既增加误选风险，也使“一键进入当前检查”的交接目标没有完全落盘。
现有 `--quality-open-plugin-runtime` 属于自动质量探针参数，不适合作为真实人工验收的产品路径。

本阶段新增正常启动参数 `--open-plugin <插件ID>`。宿主完成插件目录加载后，直接调用与工作区插件运行、Broker
打开请求相同的 `PluginMainUiLauncher`；缺失插件、启动失败或没有主界面均按现有状态失败并记录，不引入第二套
插件生命周期。`start-plugin-validation.ps1` 把待验插件 ID 作为该参数传给精确冻结 EXE，同时将参数写入
`validation-session.json`。`-PrepareOnly` 和严格 prepared 会话恢复语义保持不变，脚本仍不截图、不判断 UI、
不生成批准回执。

本改动会再次使旧本地 RC3 候选按规则过期；合入后必须在新 `master` 重跑完整收口、重新打包并更新本地更新清单
签名，再准备 Base64 会话。真实人工状态仍是 0 / 25，GitHub Release 继续禁止创建。

验证结果：PowerShell 5.1 语法解析通过；启动参数、会话传参与既有插件打开服务专项 `10 / 10` 通过；Release
全量构建 `0` 警告、`0` 错误，全量自动化测试 `1104 / 1104` 通过。
