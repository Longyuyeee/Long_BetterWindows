# .NET 原生插件总收口与仓库 CI 审计（PX3D）

> 审计日期：2026-07-30
> 本地状态：已完成
> 远端状态：已完成

## 本批结果

- Contracts SDK、WPF UI Kit、Testing SDK、Web SDK、lockfile、API 版本和机器审计统一为 `1.0.0`。
- 原生 Loader 只共享 Contracts SDK 与 WPF UI Kit；Testing SDK 明确不进入生产加载上下文。
- 八个原生生产组件均无 Host 项目引用，`host_coupled_projects` 为空。
- 仓库 CI 恢复为两个独立检查：`Build and test` 与 `Plugin platform`。
- CI 覆盖 Release 构建、全量测试、SDK/Loader 边界、Web SDK、四运行时矩阵和三个 .NET SDK 包。
- workflow 同时响应 `master` push、面向 `master` 的 PR 和手动触发；暂不启用强制 PR 分支保护，保持当前直接主分支协作方式。

## 本地验证

- Release 构建：0 警告、0 错误。
- 全量自动化：979 / 979。
- SDK/Loader/CI 定向门禁：27 / 27。
- Web SDK：5 / 5。
- 四运行时矩阵：4 / 4。
- Contracts、WPF、Testing 三个 `1.0.0` NuGet 包均成功生成。

## 远端验证

- GitHub Actions 运行 [`30509425367`](https://github.com/Longyuyeee/Long_BetterWindows/actions/runs/30509425367) 成功。
- `Build and test`、`Plugin platform` 两个 Job 均通过并上传测试/SDK 包产物。
- 首轮暴露的 PowerShell 7 到 Windows PowerShell `PSModulePath` 继承问题已修复，全量脚本测试在 Windows PowerShell 下运行。
- checkout/setup-node/upload-artifact 使用 v7，setup-dotnet 使用 v6；最终运行无 Node 20 弃用注解。

## 后续检查

1. 转入物理桌面、DPI、任务栏和辅助功能验收。
2. 完成管理员性能、干净 Windows 和真实下载验证。
3. 完成受控 Registry/CDN 发布与回滚演练。

本批不改变 25 项真实桌面插件验收的 pending 状态。
