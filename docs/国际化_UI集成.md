# Long窗口国际化 UI 集成

> 状态：G1、G2 前三批、工作流四个子批次及 G16-A/B1 已完成，整体国际化进行中
> 语言：`zh-CN`、`en-US`
> 最后更新：2026-07-24

## 目标与边界

国际化服务于 Long 自身的优雅、扁平、动效化 Windows 体验。语言切换不得改变插件权限、命令参数、工作流审查或市场信任语义，也不承担 uTools 插件兼容。

宿主负责自身界面和统一状态文案；Long 原生插件可在后续声明自己的语言资源。插件未声明翻译时必须如实显示原内容，不使用机器翻译伪造完整覆盖。

## G1 已完成

1. `I18nService` 从静态占位实现升级为实例服务，支持中文完整回退、英文覆盖、未知语言拒绝和资源键查询。
2. 当前语言保存到 `%LocalAppData%\LongBetterWindows\language.json`，使用同目录临时文件与原子移动，启动时恢复。
3. `--language zh-CN|en-US` 可为质量采集覆盖当前语言，不修改持久化选择。
4. 语言资源映射到 WPF `ResourceDictionary` 的 `i18n.*` 动态键；设置页切换后即时刷新已迁移控件、页标题与运行状态。
5. 已迁移主窗口标题栏、命令中心/超级面板入口、Tool Center 导航、页面标题、系统集成卡片、设置语言/主题/动效/隐私/鼠标手势。
6. 英文紧凑布局采用可换行侧栏文案与纵向手势卡操作，避免长英文挤压或裁切。

## 验证结果

- Release 构建：0 警告、0 错误。
- 自动化：522 / 522 通过。
- 资源测试：中文回退、语言持久化、单次变更事件、动态资源写入、非法语言拒绝、两种语言键集合一致。
- 视觉证据：
  - `artifacts/quality/i18n-g1-20260723-final/en-system-wide.png`
  - `artifacts/quality/i18n-g1-20260723-final/en-settings-compact.png`
  - `artifacts/quality/i18n-g1-20260723-final/zh-settings-compact.png`

## G2 第一批：概览页（已完成）

1. 欢迎横幅、统计卡、快捷操作、平台状态及页面内容可访问名称全部改用动态语言资源。
2. 平台状态移除已经过时的“阶段 5”文字，改为不绑定测试数量的本地质量门禁与外部发布签核状态。
3. 新增 `--quality-show-welcome`，可在不修改首次运行状态的情况下重复采集欢迎区。
4. 紧凑布局下欢迎按钮和命令中心按钮进入下一行，避免长英文压缩正文；统计卡标签允许换行。
5. 英文宽屏、英文紧凑和中文浅色紧凑渲染通过，证据位于 `artifacts/quality/i18n-g2-overview-20260724/`。
6. Release 构建 0 警告、0 错误，全量自动化 509 / 509 通过。

## G2 第二批：插件管理（已完成）

1. 已安装数量、搜索占位、刷新、空态、无匹配结果、插件列表、运行状态和启停动作全部接入动态资源。
2. 设置、权限与调用记录入口的 ToolTip、可访问名称及子窗口标题已本地化。
3. 插件名称、能力标识和运行时标识继续按 Manifest 原文显示，避免宿主伪造插件自身翻译。
4. 顶部工具栏在窄屏将标题与搜索操作拆为两行；插件卡操作区统一放到信息区下方，防止长英文及能力标签被右侧按钮压缩。
5. 英文宽屏、英文紧凑和中文浅色紧凑真实 25 插件渲染通过，证据位于 `artifacts/quality/i18n-g2-plugin-management-20260724/`。
6. Release 构建 0 警告、0 错误，全量自动化 510 / 510 通过。

## G2 第三批：插件市场（已完成）

1. 市场来源、品牌区、搜索/分类、结果数量与状态、版本、可信状态、兼容性、能力权限、下载反馈及安装/卸载动作均接入动态资源。
2. 安装前审查、发布者验证、SHA-256、权限变化、原生 DLL 高信任警告、事务回滚保证和失败/取消反馈完成迁移；质量验证不自动确认任何安装或卸载。
3. `MarketplacePresentation` 不再生成中文状态、兼容说明或权限差异文本，只投影语言无关的安装状态和版本要求。
4. 宽屏保持结果—详情双栏；紧凑窗口采用主从交互，首次显示结果列表，鼠标或 Enter/Space 明确打开详情，返回按钮回到列表。
5. 修复异步选择把外层页面滚到中段、可信状态长英文裁切和版本下拉对象文本空白三个视觉问题。
6. 新增 `--quality-market-list` 只读质量入口以及中英文格式参数一致性测试。
7. 英文宽屏、英文紧凑列表/详情和中文浅色紧凑列表渲染通过，证据位于 `artifacts/quality/i18n-g2-marketplace-20260724/`。
8. Release 构建 0 警告、0 错误，全量自动化 512 / 512 通过。

## G2 第四批 A：工作流编辑器基础（已完成）

1. 本机工作流列表、宽屏/紧凑导航、工具栏、空态、草稿身份字段、失败策略、步骤卡、补偿命令、敏感输入确认和预检状态已接入动态资源。
2. 失败策略、步骤影响、输入类型、列表数量/摘要和草稿保存状态改为运行时投影；语言切换会刷新当前列表与草稿，同时保留工作流 ID、当前失败策略和编辑内容。
3. 工作流控件加载/卸载对称订阅语言事件，避免页面反复进入后残留回调。
4. `720 × 640` 紧凑布局将编辑器标题操作区移到独立一行，英文长标题不再被准备、导出、复制、删除和保存动作截断。
5. 英文深色宽屏、英文深色紧凑和中文浅色紧凑渲染通过，证据位于 `artifacts/quality/i18n-g2-workflow-foundation-20260724/`。
6. Release 构建 0 警告、0 错误，全量自动化 513 / 513 通过。

本子批次没有宣称整个工作流完成。参数 Schema/绑定编辑器、导入与模板的运行时审查文案、执行批准、结果、报告时间线、终端输出和导出反馈仍按后续子批次迁移。

## G2 第四批 B：工作流参数与绑定编辑器（已完成）

1. 输入类型、文本/路径/PNG 操作、参数预设、Schema 字段控件、高级参数、未声明参数、步骤输出绑定及其可访问名称已接入动态资源。
2. 图片摘要、参数数量/需修正状态、类型与范围/长度/默认值/敏感值约束、绑定目标和模型自有校验错误已迁移到运行时语言投影。
3. 文件夹、文件与 PNG 选择器标题、PNG 过滤器、大小/格式错误和警告框标题已本地化。
4. 国际化服务支持在完整宿主初始化前以 `zh-CN` 回退资源惰性初始化，保持参数/绑定模型可独立测试；正式启动仍按用户选择重新加载语言。
5. 本批涉及的 XAML、输入模型和绑定模型已通过中文硬编码归零门禁。插件 Manifest 声明的字段名、说明和预设名继续按插件原文显示。
6. 英文深色宽屏、英文深色紧凑和中文浅色紧凑渲染通过，证据位于 `artifacts/quality/i18n-g2-workflow-invocation-20260724/`。
7. Release 构建 0 警告、0 错误，全量自动化 514 / 514 通过。

## G2 第四批 C：工作流导入与模板审查（代码已完成）

1. 外部文件选择器、模板目录与菜单、模板校验数量、覆盖草稿确认和采用失败反馈已接入动态语言资源。
2. 审查标题、来源、SHA-256、信任级别、敏感输入状态和预检摘要由当前语言投影；插件数与问题数使用中英文一致的格式参数。
3. 语言切换会重新渲染当前导入或模板审查，同时保留工作流定义、来源哈希和审查模式，不自动采用、覆盖或保存。
4. 底层自然语言错误保留在技术日志，界面只显示本地化稳定提示；Manifest、安装器、市场和工作流验证器的统一错误码留到 G16。
5. 导入审查新增稳定 UI Automation 标识；资源键集合、格式参数和本批固定中文归零门禁通过。
6. Release 构建 0 警告、0 错误，全量自动化 515 / 515 通过。
7. 英文宽屏/紧凑和中文紧凑真实视觉证据尚未采集，保留为候选验收门禁，不将代码完成误报为视觉验收完成。

## G2 第四批 D：工作流执行、报告与终端输出（代码已完成）

1. 权限审查、风险与失败策略、运行中/取消、完成/失败/回滚状态、输出摘要、报告列表和脱敏时间线全部接入动态语言资源。
2. `WorkflowExecutionPresentation` 显式接收翻译函数，不读取全局 WPF 服务；状态、事件、权限和输出摘要均可在无窗口环境下验证中英文。
3. 终端输出的显示授权、复制、内存清除和两阶段文件导出完成迁移；导出失败按 `WorkflowTerminalOutputExportFailure` 映射稳定提示。
4. 语言切换保持当前执行审查、运行状态、执行结果、终端输出和报告选择；清除终端输出会同步擦除缓存，刷新语言不会恢复敏感值。
5. 主窗口顶部质量入口的取消审查、终端授权、确认执行和清除敏感输出同步使用动态资源和可访问名称。
6. 展示层不再向用户回显底层自然语言错误；统一结构化错误契约继续由 G16 覆盖 Manifest、安装器、市场和工作流验证器。
7. Release 构建 0 警告、0 错误，全量自动化 517 / 517 通过。
8. 真实双语执行、长终端输出及宽屏/紧凑视觉证据尚未采集，保留为候选验收门禁。

## G16-A：诊断、开发者入口与 Toast 外壳（已完成）

1. 平台诊断的 CPU、工作集、能力调用、历史图表、插件排行、空态和刷新可访问名称全部接入动态资源。
2. 诊断面板缓存最近性能快照；语言变化只重新投影平均值和活跃插件数量，不重启协调器或丢失图表数据。
3. 诊断指标卡在小于 720px 的内容宽度切换为单列；开发者入口在宿主窄布局下同步切换单列。
4. 开发工作台、设计系统、开发文档和关于卡片，以及文档目录缺失/空态、版本与能力/插件/运行时统计完成迁移。
5. 插件开发工具原生窗口与预览标题、Toast 窗口可访问名称接入语言资源；调用方消息及文档文件名按原内容显示。
6. Release 构建 0 警告、0 错误，全量自动化 518 / 518 通过。
7. 开发工作台 HTML、脚本创建/文档查看等对话框留给 G16-B；稳定错误码和 UI 错误映射留给 G16-C。

## G16-B1：原生开发界面（已完成）

1. `ScriptCreationDialog` 的标题、文件名、语言、模板、扩展名提示、操作和校验反馈全部由 `zh-CN` / `en-US` 资源提供。
2. 模板显示文案与内部契约分离，选择结果稳定为 `hotkey`、`note` 或 `blank`；切换语言不会改变模板类型。
3. `DesignSystemPreview` 的说明、组件样例、状态反馈和 UI Automation 名称使用动态资源。
4. `DocViewer` 的标题和 Markdown 是调用方内容，不由查看器翻译；生成 HTML 使用当前宿主语言设置 `lang`，文档原文保持不变。
5. 中英文资源各 572 个业务键且集合一致（另含 `_lang` 元数据）；本批 XAML 固定中文归零门禁通过。
6. Release 构建 0 警告、0 错误，全量自动化 519 / 519 通过。
7. G16-B2 迁移 `PluginDevTools.html`、WebView 消息反馈及统一确认/错误对话框；运行时语言切换必须保留标签、编辑内容、脏状态、插件选择和日志。

### G16-B2 实施顺序

1. **B2.1 工作台语言桥与静态外壳**：导航完成后由宿主发送 `{ language, strings }`，页面通过 `applyLocalization` 原位刷新 `lang`、标题和静态控件；禁止用重载实现语言切换。
2. **B2.2 动态状态与宿主错误**：API/能力说明、文件树、只读标签、保存/预览/日志/创建反馈全部使用资源键；宿主错误使用稳定键或错误码。
3. **B2.3 统一对话框与生成模板**：迁移未保存确认和剩余确认/错误界面，生成模板按创建时语言投影；补齐双语宽屏/紧凑、主题及减少动画证据。

状态保留门禁：任一步运行时语言切换都不得重新导航 WebView，不得丢失当前插件、标签顺序、编辑内容、脏状态、搜索条件或已载入日志。

### G16-B2.1 完成记录

1. `PluginDevTools` 在页面导航成功后发送受控 `{ language, strings }`，语言变化时复用同一消息，不调用导航或页面重载。
2. 页面 `applyLocalization` 原位更新文档语言、标题、静态文本、占位符和工具提示；CodeMirror、`tabs`、`fileCache` 和插件选择保持原对象。
3. 真实插件名与日志一旦写入便移除静态占位标记，语言刷新不会用空态覆盖运行时内容。
4. 侧栏、工具栏、编辑空态、搜索、日志外壳和新建插件表单完成双语迁移。
5. 中英文各 602 个业务键且集合一致；Release 构建 0 警告、0 错误，全量自动化 520 / 520 通过。
6. B2.2 继续迁移动态日志状态、API/能力说明、文件树/只读标记、保存/预览/创建反馈和宿主稳定错误键；视觉证据统一在 B2.3 采集。

### G16-B2.2-A 完成记录

1. 插件空态、日志状态、文件树、只读标记、未保存确认及保存/创建/预览反馈使用运行时资源键。
2. 页面缓存插件与文件树投影；语言变化幂等重绘，真实日志、搜索条件、标签顺序、编辑内容及脏状态保持不变。
3. 保存异常只写调试日志，页面接收稳定错误键；DLL 模板不受支持和目录冲突同样不传自然语言。
4. 中英文各 616 个业务键且集合一致；Release 构建 0 警告、0 错误，全量自动化 520 / 520 通过。
5. B2.2-B 继续迁移 Long API 目录和能力名称/说明；生成模板及视觉证据仍由 B2.3 完成。

### G16-B2.2-B1 完成记录

1. 42 条 Long API 由宿主按当前语言投影为 `{ api, description, capability }`，HTML 不再保存自然语言 API 说明。
2. API 目录与字符串字典通过同一 `localization` 消息原子发送，搜索和 CodeMirror 补全共享该目录。
3. 语言变化按当前筛选条件重绘 API 列表，不重新导航或修改编辑内容。
4. 中英文各 658 个业务键且集合一致；Release 构建 0 警告、0 错误，全量自动化 520 / 520 通过。
5. B2.2-B2 继续迁移 32 项能力名称、说明和无可用能力空态。

### G16-B2.2-B2 完成记录

1. `KnownCapabilities` 的真实集合为 32 项；本文先前的 31 项口径已修正。
2. 能力名称和说明使用稳定资源键，图标、ID 与安全等级保持语言无关。
3. 语言变化重新投影能力元数据时快照并恢复已勾选 ID，不改变权限草稿。
4. 自动化遍历两种语言与全部已知能力，拒绝名称或说明缺失。
5. 中英文各 723 个业务键且集合一致；Release 构建 0 警告、0 错误，全量自动化 521 / 521 通过。
6. B2.2 完成；下一步 B2.3 处理统一对话框、生成模板和真实双语视觉证据。

### G16-B2.3-A 完成记录

1. Web 插件模板按创建时宿主语言生成文档语言、说明、操作和反馈；C# Script 模板注释同步投影。
2. 模板生成显式接收语言与翻译函数，HTML 文本编码和 JavaScript JSON 序列化边界保持不变。
3. 自动化覆盖显式英文模板、插件名与翻译文本编码、Unicode 安全序列化和中文残留拒绝。
4. 中英文各 731 个业务键且集合一致；Release 构建 0 警告、0 错误，全量自动化 522 / 522 通过。
5. B2.3-B 继续处理工作台自定义确认对话框和真实双语宽屏/紧凑视觉证据。

### G16-B2.3-B 工程完成记录

1. 工作台未保存确认使用 `role="alertdialog"` 自定义对话框，默认聚焦取消，支持 Esc 和焦点循环。
2. 确认目标绑定标签对象而非旧数组下标，执行时重新定位，避免关闭错误标签。
3. 对话框使用动态资源；语言切换保留对话框、脏标签和编辑内容。
4. 系统 Edge 无头加载本地 HTML，在英文 `900 × 650`、英文 `600 × 400`、中文 `600 × 400` 下确认无文本溢出，取消焦点和遮罩正确。
5. 证据位于 `artifacts/quality/i18n-g16-b23-workbench-headless-20260724/`；真实 WPF/WebView2 视觉验收仍保留为候选门禁。
6. 中英文各 733 个业务键且集合一致；Release 构建 0 警告、0 错误，全量自动化 522 / 522 通过。

## 后续开发顺序

### G2：宿主完整迁移

按使用频率依次迁移：

1. [x] 概览页和欢迎区。
2. [x] 插件管理。
3. [x] 插件市场。
4. [x] 组合动作编辑、导入/模板审查、执行审查、执行结果、报告、终端输出和导出对话框工程迁移。
5. [x] 平台诊断、开发者入口、脚本创建器和设计系统预览。
6. [ ] Toast 外壳已完成；统一确认、失败、空态、加载态和剩余可访问名称继续由 G16-B2/C 收口。

每批必须保持中英文资源键严格一致，并在宽屏与 `720 × 640` 紧凑窗口下各完成至少一次真实渲染复核。

### G3：Long 原生插件语言协作

在现有 Manifest 和 Web 消息桥上增加可选语言声明及宿主语言变更通知。未声明的旧插件保持兼容；语言切换不得重启插件、清空本地状态或绕过生命周期管理。

2026-07-24 G3-A / G17-A 协议基础已完成：

1. Manifest 支持可选 `localization.default_language` 与语言到 JSON 相对路径的 `resources` 映射，并校验语言标识、数量、默认回退和目录边界。
2. 宿主以受限大小和条目数加载资源，生成防御性复制的 `PluginLanguageContext`；缺少当前语言时回退插件默认语言。
3. DLL 使用可选 `IPluginLanguageLifecycle`，C# 脚本使用 `LanguageChanged` 委托，Web 使用 `long.language-changed` 消息；未声明语言的插件不进入通知链。
4. `PluginScanner` 在初始化后发送当前语言，`PluginRuntimeCoordinator` 订阅后续变化；通知不调用插件启停方法，失败仅记录日志。
5. WebView 只保留最新语言快照，导航完成后投递并在后续页面导航时重放，不积压旧消息、不重建页面。

下一批选择一个 Web 内置插件和一个 DLL 内置插件作为双语样板，同时验证 Manifest 显示元数据与插件内部 UI 使用同一资源目录。

2026-07-24 G17-B 双运行时样板已完成：

1. Base64Tool 使用 24 组中英文插件资源，覆盖插件/命令元数据、页面、ARIA、占位符、操作、成功状态和错误反馈；语言切换原位重绘并保留输入、输出和当前状态。
2. FolderNotePlugin 使用 11 组中英文插件资源，实现 `IPluginLanguageLifecycle`，动态更新插件名称、命令元数据、Toast、设置入口和当前活动备注 HUD；备注内容、热键注册和 ADS 状态不变。
3. `PluginEntry` 与 `CommandDescriptor` 增加只读显示投影，插件管理、平台诊断、命令搜索和工作流命令选择统一消费本地化名称；原 Manifest、`PluginCommand` 和目录修订保持不变。
4. Release 输出已验证包含两个样板的 `zh-CN.json` / `en-US.json`。Base64 无头 Edge 英文 `760×680`、中文 `620×560` 验证无水平溢出，双向切换后输入、结果和状态保持。
5. 下一批 G17-C 按使用频率迁移 ClipboardTool、QuickNotePlugin 等 Web 插件；其后再分组处理剩余 DLL 插件，避免一次提交混入全部 25 个插件。

### G4：完成门禁

只有满足以下条件才把 README 国际化标记为完成：

1. 宿主用户可见硬编码文本审计归零，允许的品牌名、协议常量和开发日志必须列入白名单。
2. `zh-CN` / `en-US` 键集合、格式占位符和可访问名称自动校验通过。
3. 主窗口、命令中心、超级面板、市场、工作流、插件管理和关键对话框均有双语视觉证据。
4. 语言切换、重启恢复、紧凑布局、浅色/深色主题和减少动画模式完成回归。
