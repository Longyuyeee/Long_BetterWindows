# v1.11.0 S1B 插件关闭整体预算开发审计

> 日期：2026-08-13
> 阶段：S1B 插件关闭整体预算
> 状态：代码与自动化验收完成；干净提交真实宿主验收待合并后执行

## 1. 本批目标

本批只处理稳定版总计划的 S1B，不修改 UI、版本号、插件协议或 unsigned 发布政策。目标是在保留单插件 3 秒预算和串行启动清理的前提下，为全部插件增加 20 秒硬预算，避免 25 个失联插件把退出时间累积到约 75 秒。

## 2. 实现结果

1. `PluginRegistry.ShutdownAllAsync` 同时接受单插件预算和整体预算。
2. 每个插件实际等待预算取“单插件预算”和“整体剩余预算”的较小值。
3. 整体预算耗尽后不再启动剩余插件的清理，并以 `skipped_total_budget` 结构化记录每个未启动插件。
4. 返回 `PluginShutdownReport`，包含总耗时、整体预算、每插件状态/耗时/等待预算和全部未完成插件 ID。
5. 保持顺序遍历，没有引入 `Task.WhenAll` 或主动并行关闭。
6. 已经开始但超时的插件继续由迟到观察器接管，不重新修改 Registry 状态、健康状态、自动启动偏好或 UI。
7. 插件关闭不完整时，`ShutdownAllForHostAsync` 抛出不包含插件私密错误正文的稳定异常，使 S1A 顶层 `plugins` 步骤如实成为 `failed`；`BestEffortShutdownSequence` 仍继续执行 Plugin Runtime 和 Host Services。
8. 正式宿主固定使用单插件 3 秒、整体 20 秒预算。

## 3. 需求对齐审计

| S1B 要求 | 落地状态 | 审计结论 |
|---|---:|---|
| 保留单插件预算 | 已完成 | 正式宿主仍为 3 秒 |
| 增加 20 秒整体预算 | 已完成 | App 退出接线显式传入 20 秒 |
| 等待较小预算 | 已完成 | 每轮根据整体剩余时间重新计算 |
| 记录所有未完成插件 ID | 已完成 | 超时、失败、整体预算跳过均进入报告 |
| 不静默成功 | 已完成 | 宿主包装器将不完整报告提升为顶层稳定失败 |
| 后续宿主清理继续 | 已完成 | 顶层最佳努力序列捕获失败并继续 |
| 迟到任务不回写状态/UI | 已覆盖 | 既有迟到完成回归继续通过 |
| 不新增主动并行关闭 | 已完成 | 仍按注册表快照顺序启动清理 |

## 4. 自动化验收

- 退出链相关定向测试：15 / 15 通过。
- 全量自动化测试：1419 / 1419 通过，0 失败，0 跳过。
- Release 构建：0 警告，0 错误。
- 合成多失联场景：单插件预算 100 ms、整体预算 140 ms；前两项超时，第三项明确标记 `skipped_total_budget`，总耗时低于 500 ms。
- 合成正常目录：25 / 25 项全部 `passed`，在 2 秒整体测试预算内完成。
- 顶层传播：不完整插件退出抛出 `IncompletePluginShutdownException`，未完成 ID 可审计且异常正文不包含插件内部错误。

## 5. 合并后实机验收

代码合并到干净 `master` 后重新构建 Release，并运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\verify-host-process-cleanup.ps1 `
  -OutputPath artifacts\quality\s1b-shutdown-<commit>\host-process-cleanup.json `
  -CleanupTimeoutSeconds 30
```

通过条件：

- shutdown JSON 的四个顶层步骤全部 `passed`；
- 正常 25 插件没有超时或整体预算跳过；
- 宿主目标退出时间不超过 15 秒，硬门槛不超过 30 秒；
- 本次宿主派生 WebView2 / Worker 最终残留为 0；
- 报告绑定干净源码提交、宿主 EXE SHA-256、PID 和退出码；
- 原始证据只保留本机，文档提交其路径、摘要和 SHA-256。

## 6. 阶段判定

S1B 代码和自动化门禁已达到可合并状态。真实宿主验收通过并单独推送审计结果后，S1B 才正式关闭；随后按顺序进入 S1C 稳定性复跑。
