# Long助手 v1.11.0 S1C-B 工作区切换稳定性审计

> 执行日期：2026-08-13
>
> 验收提交：`156faf49f38a9c0934236c204bce601f4424797b`
>
> 验收类型：单机真实 Release 宿主自动化复跑
>
> 结论：通过

## 1. 需求对齐

本批对应稳定版总计划 S1C 的“12 轮工作区多模块切换”。它验证统一工作区的模块身份复用、
页面切换和最终清理，不修改产品交互或视觉范围，也不增加 uTools 插件兼容性。

验收范围包括：

- Base64、开发者工具箱、剪贴板历史 3 个真实插件模块；
- 概览、插件管理、市场、设置 4 个管理模块；
- 12 轮共 84 次真实页面转换；
- 每轮 7 个模块、其中 3 个插件运行模块，不重复创建；
- 插件会话和 WebView 内容身份保持复用；
- 资源增长低于既定门槛，结束后插件会话和模块全部清理。

## 2. 执行条件与命令

执行前确认跟踪工作区干净，且没有正在运行的 `LongBetterWindows.Host`。随后从验收提交构建
Release 宿主并运行：

```powershell
dotnet build .\src\LongBetterWindows.Host\LongBetterWindows.Host.csproj -c Release --no-restore
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\verify-workspace-switch-stability.ps1 `
  -OutputPath artifacts\quality\s1c-workspace-156faf4\workspace-switch.json `
  -TimeoutSeconds 180
```

Release 构建结果为 0 警告、0 错误。原始 JSON 按项目策略仅保存在本机 `artifacts/**`，不进入
Git 历史。

## 3. 验收结果

| 项目 | 实测 | 门槛 | 结果 |
|---|---:|---:|---|
| 稳定循环 | 12 / 12 | 12 / 12 | 通过 |
| 页面转换 | 84 | 84 | 通过 |
| 插件 / 管理模块 | 3 / 4 | 3 / 4 | 通过 |
| 总模块 / 插件运行模块 | 7 / 3，每轮稳定 | 7 / 3 | 通过 |
| 资源样本 | 13 | 13 | 通过 |
| 句柄增长 | +34 | <= +64 | 通过 |
| 线程增长 | 0 | <= +8 | 通过 |
| 私有内存变化 | -116,453,376 bytes（约 -111.1 MiB） | <= +64 MiB | 通过 |
| 切换 / 增长 / 清理判定 | 全部为 true | 全部为 true | 通过 |

探针完成后 `LongBetterWindows.Host` 进程数为 0。本地原始证据 SHA-256：

```text
63D54A818CA6924AF1709AE4E6CC8C3E46F31E0355B3FC854863101B8D6B752A
```

## 4. 审计结论与下一步

S1C-B 已关闭。统一工作区在连续跨插件与管理页面切换中没有出现模块复制、会话身份漂移或
资源超限。下一步严格按总计划进入 S1C-C：正式复跑三插件组合长驻留与资源趋势门禁，并
同时覆盖嵌入插件、独立窗口混合生命周期以及最终会话、窗口、模块清理。
