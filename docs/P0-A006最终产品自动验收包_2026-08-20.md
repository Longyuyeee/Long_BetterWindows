# P0-A006 最终产品自动验收包

> 日期：2026-08-20
> 范围：最终产品聚合、可移植闭包与自动资格判定
> 状态：本地完成，待提交与远端 CI

## 开发结果

- `verify-final-product-acceptance.ps1` 不再读取 reviewer、插件/最终批准回执或外部延期签字，改为严格消费 A005 final closure schema v2。
- 新增 final product acceptance schema v3；输出固定为 `automated_final_product_acceptance`，并复制、哈希锁定唯一 final closure 来源。
- 聚合器复核提交、dirty 状态、94 个 gate、25 插件/42 命令、宿主 EXE 哈希和发布资格；失败与未运行 gate 均拒绝生成通过包。
- A007 继续负责把外部发布门禁从旧 `approved_final_product_acceptance` 合同迁移到新自动摘要，本批不混改下游合同。

## 真实测试差异

| 检查 | 预期 | 首次实际 | 修正后实际 |
|---|---|---|---|
| 旧脚本基线 | 因缺少人工回执而拒绝 | 更早暴露 PowerShell 多行调用缺少续行符，脚本无法执行矩阵 | 旧调用链已整体移除 |
| schema 与定向测试 | 6 / 6 | 3 / 6；正则转义、错误顺序和 nullable 警告不符合预期 | 6 / 6，0 警告 |
| 全量自动化 | 1491 / 1491 | 一次嵌套闭环运行受旧构建状态干扰 | 独立全量 1491 / 1491 |
| Release 真实闭环 | 94 gate，93 passed、1 environment blocked | 首次传入 `win-x64` 的另一份 EXE，哈希保护按设计拒绝 | 使用闭包绑定 EXE 后完全一致 |
| 闭包可移植性 | 来源存在且 SHA-256 匹配 | 完全一致 | 完全一致 |

当前唯一阻断为非管理员环境缺少 WPR 采集权限：`native-performance-preflight` 为 `blocked_environment`；其余 93 个 gate 通过，失败 0、未运行 0。这不是人工签字缺失或产品代码失败。

## 下一步

提交并等待远端 CI 通过后关闭 A006。随后进入 A007，只迁移 `verify-external-release-gate.ps1` 的 unsigned、离线和测试服务通道策略，不恢复人工批准合同。
