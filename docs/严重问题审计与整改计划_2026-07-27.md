# Long助手严重问题审计与整改计划

> 审计日期：2026-07-27
> 审计起点：`master` / `origin/master` @ `26b5732`（`v1.11.0-rc.2`）
> 范围：主题与颜色、交互性能、25 个内置插件、分离窗口任务栏身份
> 发布判断：`v1.11.0-rc.2` 被真实用户测试阻断，不再作为可发布候选

## 1. 结论

用户报告的四类问题均能在代码或真实运行中找到依据，不是单纯的环境差异：

1. 运行时主题切换只更新颜色资源，没有替换既有画刷实例，造成主体切换而标题栏、控件仍保持旧主题。基础按钮悬停模板还会把主按钮背景改成普通表面色，但保留白色前景。
2. QuickLaunch 在每次输入时同步递归扫描文件和文件内容；本地文件索引还会频繁复制大列表并对完整索引排序。这是交互卡顿的高可信根因。
3. Web 插件初始化在导航和 JS 命令处理器就绪前返回，真实命令存在随机失败或 30 秒超时。DevToolkit 没有命令处理器，PortManager 调用了不存在的桥接方法，WindowManager 指南依赖跨程序集窗口样式并在运行时解析失败。
4. 分离插件窗口沿用宿主进程身份和图标，并设置主窗口为原生 Owner，Windows 任务栏会将其继续视为同一应用。

本轮先修复共性根因和已复现插件故障。候选恢复前仍需完成 5 个高副作用/外部依赖插件的人工功能验收，以及真实任务栏分组观察。

## 2. 已完成整改

### 2.1 UI 与主题

- 主题切换现在重新生成语义画刷和兼容别名，不再只替换 `Color`。
- 主按钮拥有独立 hover/pressed 颜色；基础按钮不再覆盖主按钮背景。
- `Styles.xaml` 中卡片、分段控件和滚动条迁移到动态语义资源。
- DevToolkit 固定深色 CSS 全部迁移到 Long Web UI Kit 变量。
- ScriptCreationDialog 固定深色背景、文本、输入框和按钮迁移到 Long Design System。
- 真实 UI Automation 验证暗色切换亮色后，主体和标题栏同步更新。

### 2.2 性能

- QuickLaunch 搜索增加取消、180 ms 防抖和查询版本校验。
- 文件名及内容递归扫描移到后台线程；窗口关闭时取消未完成搜索。
- 本地文件索引上限从 75,000 调整为 30,000，快照批次从 256 调整为 2,048。
- 查询改为有界最佳匹配，不再为每次输入投影、排序完整索引。
- 空闲测量没有发现持续 CPU 异常；当前高优先级问题集中在交互式磁盘扫描。

### 2.3 插件可靠性

- WebView2 初始化会等待桥接注入和首次导航完成，避免首条命令早于页面处理器。
- DevToolkit 增加 `dev-toolkit.open` 命令处理器。
- PortManager 将错误的 `findOwner` 调用修正为公开 API `findPortOwner`。
- WindowManagerGuide 移除跨程序集 `LongDialog` 查找，改用自身窗口属性和 WindowChrome。
- 修复后 DevToolkit、PortManager、WindowManager 三条真实命令均以退出码 0 完成。

### 2.4 分离窗口任务栏身份

- 分离窗口不再设置主窗口为原生 Owner，只保留应用内返回目标。
- 每个插件使用稳定的 `LongAssistant.Plugin.<plugin-id>` AppUserModelID。
- 每个插件生成确定性独立图标，并在窗口句柄创建后写入任务栏身份。
- 代码与命令冒烟已确认属性写入不再抛异常；最终分组效果仍需在真实任务栏人工确认。

## 3. 25 插件审计矩阵

等级说明：A 为带输入或关键结果的真实功能冒烟；B 为真实命令、页面初始化和退出清理通过；C 为静态契约通过但因副作用或外部依赖待人工执行。

| 插件 | 运行时 | 等级 | 当前判断 |
|---|---|---:|---|
| Base64 编解码 | Web | A | 编码、解码真实命令通过 |
| 剪贴板历史 | Web | B | 打开、初始化、退出通过 |
| 剪贴板工具箱 | Web | B | 打开、初始化、退出通过 |
| 颜色拾取器 | Native | C | 采样失败会自动退出；真实桌面取色待人工验收 |
| 开发者工具箱 | Web | A | 缺失命令处理器已修复，带文本命令通过 |
| 文件整理器 | Web | B | 打开通过；真实文件事务待隔离目录验收 |
| 批量重命名 | Web | B | 打开通过；真实重命名待隔离目录验收 |
| 文件夹备注助手 | Native | C | 无效目标会返回失败、保存会等待 ADS 结果；真实写入待人工验收 |
| 硬件监控 | Web | B | 打开和宿主能力初始化通过 |
| JSON 格式化 | Web | A | 格式化、压缩真实命令通过 |
| 宏录制器 | Native | C | 空宏正确失败，坐标和取消已修复；真实录制回放待人工验收 |
| Markdown 预览 | Web | B | 打开、初始化、退出通过 |
| 密码生成器 | Web | A | 真实生成命令通过 |
| 端口管理器 | Web | A | API 名称错配已修复，真实监听端口查询通过 |
| 快捷启动器 | Native | B | 打开通过；递归搜索已改为可取消后台任务 |
| 快速便签 | Web | B | 新建命令和退出通过 |
| 正则测试器 | Web | B | 命令入口和初始化通过 |
| 示例插件 | Native | B | 示例命令通过 |
| 截图工具 | Native | C | 全屏命令会等待截图与剪贴板结果；真实截图待人工验收 |
| 文本对比 | Web | A | 带文本真实命令通过 |
| 时间戳转换 | Web | A | 真实转换命令通过 |
| 翻译工具 | Web | A | 在线真实翻译通过；离线失败提示待人工验收 |
| URL 工具箱 | Web | A | 编码、解码真实命令通过 |
| UUID 生成器 | Web | A | 真实生成命令通过 |
| 窗口管理 | Native | B | 指南解析故障已修复，真实打开命令通过；布局副作用待人工验收 |

当前口径：21 / 25 个插件完成真实命令或生命周期冒烟，其中 10 个完成带输入/结果功能冒烟；4 个高副作用插件保留正向人工验收，Translate 保留离线路径验收。

## 4. 发布阻断与后续顺序

### P0：完成后才允许生成新 RC

1. 在真实桌面逐项验收 ColorPicker、FolderNote、Macro、Screenshot、Translate。
2. 分别在亮/暗主题打开宿主核心页面、ScriptCreationDialog、DevToolkit 和至少 4 个代表性插件，检查文字对比度、hover、focus 和禁用态。
3. 打开两个不同插件的分离窗口，确认任务栏图标和分组可区分，关闭/返回行为正确。
4. 对 QuickLaunch 大目录搜索执行输入延迟、取消和宿主响应性采样。
5. 运行完整 Release 构建、全量测试、25 插件扫描和 WebView2 残留检查。

### P1：新 RC 之后

1. 将真实插件命令矩阵固化为可重复的分级测试脚本。
2. 增加 Web 插件公开 API 名称的静态契约检查，防止页面与桥接方法漂移。
3. 将插件启用状态、启动耗时和后台资源占用纳入诊断页。
4. 继续清理旧主题资源和未使用渐变，避免新页面绕过语义色。

## 5. 候选策略

- 不修改历史标签和历史 Release。
- `v1.11.0-rc.2` 保持可追溯，但状态明确为被用户测试取代。
- 本轮修复合入 `master` 后，完成 P0 人工验收再生成新的 `v1.11.0-rc.3` 或后续候选。
- 新候选必须从干净提交重新构建，不能复用 RC2 的包、Manifest、哈希或通过结论。

## 6. 本轮自动验证

- Release 解决方案构建：0 警告、0 错误。
- 全量自动化：662 / 662 通过。
- 主题运行时切换：UI Automation 验证暗色到亮色后主体与标题栏同步。
- 主题证据：`artifacts/quality/audit-theme-dark-20260727/`、`audit-theme-light-20260727/` 为启动主题基线；`audit-theme-runtime-20260727/` 与 `audit-theme-runtime-fixed-20260727/` 分别记录运行时修复前后。
- 真实插件命令：DevToolkit、PortManager、WindowManager 修复后退出码均为 0。
- DevToolkit 与 ScriptCreationDialog 固定十六进制主题色扫描归零。

## 7. 第二轮高副作用插件整改

2026-07-27 对 ColorPicker、FolderNote、Macro、Screenshot、Translate 完成逐文件审计：

1. Translate 原命令处理器没有等待网络请求，网络失败仍可能向宿主返回成功。现已等待翻译完成并返回真实结果；在线真实命令退出码为 0，耗时约 2.9 秒。
2. FolderNote 原命令在没有有效文件夹时仍返回成功，异步 ADS 保存由 `Action<string>` 变成了不可观察的 `async void`。现已返回真实目标解析结果，等待保存完成，失败时保留窗口并提示；停用插件会关闭活动 HUD。
3. Screenshot 全屏命令原先在截图和剪贴板写入前返回成功。现已等待捕获及写入，并将失败反馈给命令调用方。区域截图仍是明确的交互式“已开始”语义。
4. Macro 的绝对鼠标输入直接使用像素坐标，未转换到 Windows 要求的 0–65535 虚拟桌面坐标，可能造成回放点击偏位；循环状态还会被单轮播放改回 Idle，无法可靠停止。现已使用虚拟桌面归一化、统一取消令牌，并在钩子安装失败、空宏或忙碌状态下返回失败。
5. ColorPicker 在桌面 DC 或光标采样失败时不再留下无响应取色窗口。

本轮验证：

- Release 构建 0 警告、0 错误。
- 相关 UI、插件和本地化测试 121 / 121 通过。
- 全量自动化 662 / 662 通过。
- 25 / 25 插件真实加载通过。
- Translate 在线真实命令成功；Macro 空动作命令按设计返回失败，未进行任何键鼠回放。

## 8. 第三轮性能与启动状态审计

2026-07-27 继续针对“软件运行后电脑卡顿”审计启动链路和 QuickLaunch：

1. 确认统一命令执行器此前会把按需启动等同于用户启用，并在每次首次调用插件时持久写入 `auto_start=true`。插件一旦被命令中心或工作流使用，后续启动便可能永久增加自动启动项。现已为 `PluginRegistry.StartPluginAsync` 增加明确的持久化参数；插件管理页主动启用仍会保存设置，命令执行仅在本次会话启动插件，不再改变下次启动策略。
2. QuickLaunch 首次打开此前仍在 UI 线程同步递归枚举开始菜单。现已改为窗口显示后在后台发现应用，应用列表完成后自动刷新当前查询。
3. QuickLaunch 文件名搜索此前只有命中结果后才检查取消，因此无匹配的大目录仍可能长时间占用后台线程。现改为逐候选检查取消，并将每个搜索根的候选上限设为 5,000。
4. QuickLaunch 内容搜索现限制为 500 个候选文件、单文件最大 1 MiB，避免误读超大日志或数据文件造成瞬时内存和磁盘压力；窗口关闭时会释放搜索取消令牌。

真实 Release 空闲探针结果：

- 宿主启动日志：`10:46:22.518`；25 个插件运行时就绪：`10:46:28.971`，约 6.45 秒。
- 5 秒空闲采样：工作集 206.4 MB，私有内存 172.8 MB，25 个插件、42 条命令。
- 探针进程正常以退出码 0 自行结束；包含 5 秒空闲等待的进程总时长约 13.88 秒。
- 当前开发输出目录中 25 个插件均被判定为自动启动，其中一部分是旧命令执行语义遗留的 `config.json`。本轮已经阻止新增污染，但尚未证明现有用户配置能够无损迁移。

性能发布判断仍为 **P0 未关闭**。下一批按以下顺序推进：

1. 为自动启动设置增加来源/版本信息，区分用户显式启用与旧版本命令调用污染，并提供一次性兼容迁移。
2. 将非后台、非热键插件从“启动时实例化”推进到“命令首次调用时实例化”，降低 25 个程序集全量加载的启动成本。
3. 建立冷启动、插件运行时就绪、5 秒空闲工作集和私有内存的重复采样门禁；不能再只依赖单次低于 200 MB 的历史结果。
4. 在大目录样本上记录 QuickLaunch 首次显示、连续输入、取消查询和无匹配查询的 P50/P95 延迟。

本轮自动验证：

- Release 解决方案构建：0 警告、0 错误。
- 定向命令契约测试：38 / 38 通过。
- 全量自动化：662 / 662 通过。
- `git diff --check` 通过。

## 9. 第四轮自动启动与发布污染整改

本轮继续追踪 25 个插件全部自动启动的来源，并完成以下整改：

1. 自动启动偏好现在区分 `LifecycleDefault`、`ManifestDefault`、`LegacyUnknown` 和 `User` 四种来源。用户从插件管理显式启停后会同时写入 `auto_start_source=user`；旧版没有来源字段的配置保持原值并标记为 `LegacyUnknown`，不在无法辨别用户意图时强制覆盖。
2. Base64、剪贴板工具、批量重命名、JSON 格式化、Markdown 预览、密码生成、快速便签、示例插件、文本对比和翻译共 10 个纯工具插件改为默认按需启动。干净输出的自动启动插件由 25 个降为 7 个，仅保留剪贴板历史、QuickLaunch 和 5 个全局热键原生插件。
3. 发现 Web 插件构建采用增量复制，运行时生成的 `config.json` 会残留在 `bin`，发布目标可能继续将其复制进候选包。构建现会先清空 18 个 Web 插件目标目录，并排除 `bin`、`obj` 和 `config.json`；发布复制再次排除 `config.json`。
4. 构建后检查确认仍包含 25 个插件目录，运行时配置文件为 0。Base64 在默认按需状态下真实命令成功，退出码 0，执行后配置文件仍为 0。

真实性能对比：

- 插件运行时就绪由约 6.45 秒降为约 4.46 秒，缩短约 31%。
- 包含 5 秒空闲等待的进程总时长由约 13.88 秒降为约 11.31 秒。
- 5 秒空闲工作集为 206.1 MB，私有内存 171.1 MB；内存没有实质下降，证明仅减少 `StartAsync` 不足以解决全量运行时实例创建成本。
- 下一批 P0 明确为 Manifest/命令目录预注册与插件运行时延迟实例化；需要同时覆盖插件管理、语言切换、热重载、卸载和命令首次调用，不能只修改扫描循环。

本轮验证：

- Release 构建：0 警告、0 错误。
- 全量自动化：675 / 675 通过。
- 构建输出：25 个插件目录、0 个 `config.json`。
- `git diff --check` 通过。

## 10. 第五轮插件运行时延迟实例化

本轮完成上一轮确定的 Manifest/命令目录预注册与运行时按需激活：

1. `PluginEntry` 现在分别维护目录状态与可空运行时实例。扫描阶段读取 Manifest、默认设置和本地化资源，并注册全部 42 条命令，但不再创建默认关闭插件的 WebView 或原生程序集实例。
2. `PluginRegistry.StartPluginAsync` 统一负责首次激活。激活和生命周期各有独立互斥，8 个并发启动请求只创建一次实例、只执行一次 `StartAsync`，失败会进入 `Error` 状态。
3. 插件管理页打开窗口前会先按会话启动插件，但不会把这次打开持久化为自动启动；Manifest 声明窗口的未激活插件仍可显示打开入口。
4. 语言切换可在运行时未创建时更新目录投影；首次激活后再通知插件语言生命周期。热重载和卸载会等待正在进行的启动完成，再停止插件并释放对应加载上下文，避免孤立实例。
5. 真实 Base64 回归确认扫描日志先出现“运行时等待按需激活”，完整命令键 `com.long.base64:base64.encode` 执行时才出现“运行时已按需激活”。命令退出码为 0，执行后仍为 0 个 `config.json`。启动命令必须使用 `插件 ID:命令 ID` 完整键，单独使用 `base64.encode` 会按契约返回不存在。

真实性能复核：

- 3 次 3 秒空闲探针的插件就绪中位数为 4.105 秒，较上一轮约 4.46 秒小幅改善；样本波动范围为 2.625–5.495 秒。
- 3 次空闲工作集中位数为 209.8 MB，私有内存中位数为 181.0 MB；另一次工作集样本为 196.2 MB。单次低于 200 MB 不能替代重复采样门禁。
- 内存未随 18 个默认关闭实例的延迟创建稳定下降，说明当前主要占用来自 WPF 宿主、7 个必要常驻插件和基础服务。性能 P0 仍未关闭，不生成新 RC。
- 下一批按顺序审计宿主服务常驻成本、7 个自动启动插件的必要性与资源释放，再建立同一干净环境的冷/热启动和内存重复采样。

本轮验证：

- Release 构建：0 警告、0 错误。
- 全量自动化：678 / 678 通过。
- 真实空闲探针和默认关闭 Base64 首次命令退出码均为 0。
- 构建输出：25 个插件目录、42 条命令、0 个 `config.json`。
- `git diff --check` 通过。

## 11. 第六轮宿主首屏与隐藏页面成本整改

本轮先用空插件目录与完整插件目录各执行 3 次 3 秒空闲探针，分离宿主和插件成本：

- 空插件宿主平均工作集 202.5 MB、私有内存 190.1 MB。
- 完整 25 插件平均工作集 205.8 MB、私有内存 191.5 MB。
- 插件目录、42 条命令和 7 个自动启动实例合计只增加约 3.3 MB 工作集，证明当前主要成本在主窗口和宿主页面，不在延迟插件目录。

据此完成以下整改：

1. 插件管理控件从 Tool Center XAML 固定实例改为首次进入插件页时创建。概览页不再构造插件搜索、虚拟列表和卡片模板。
2. Column Injection、旧右键菜单、启动项和 Sparse Package 状态只在首次进入系统页时查询。
3. 鼠标手势投影和 GitHub 自动更新检查只在首次进入设置页时执行，不再与主窗口首屏和插件扫描竞争。
4. 插件扫描连续发布的 25 次目录变更通过 Dispatcher 空闲优先级合并刷新，避免插件页在启动阶段重复重建完整卡片集合。
5. 中英文切换只刷新已经初始化的系统/设置状态，不会因为语言变化提前触发隐藏页面的注册表、包管理或网络检查。

整改后完整插件概览页 3 次样本为 202.4、195.9、195.4 MB，平均 197.9 MB、中位数 195.9 MB；私有内存平均 174.1 MB。相对整改前完整插件平均工作集 205.8 MB 下降约 7.9 MB，私有内存下降约 17.4 MB。最大值仍为 202.4 MB，因此严格重复采样门槛继续保持未通过。

插件页 3 次平均工作集 214.5 MB、私有内存 246.3 MB。批处理减少了扫描期瞬时重建，但 25 张复杂 WPF 卡片本身仍有明显页面成本，后续需继续审计卡片模板和页面离开后的回收策略。`1120×760` 浅色工程截图位于 `artifacts/quality/lazy-toolcenter-20260727/`，控件布局、颜色和滚动区域正常。

插件功能审计同时确认：ClipboardHistory 虽声明 `start_with_host=true` 和后台运行，但 Web 适配器的 `StartAsync` 不初始化页面，剪贴板监听与 `Ctrl+Shift+V` 实际要等首次打开页面才注册。该状态不能视为后台功能完成。下一批优先建立不依赖隐藏 WebView2 的原生后台执行层，并让 Web 页面只承担展示和交互。

本轮验证：

- Release 构建：0 警告、0 错误。
- 全量自动化：678 / 678 通过。
- 概览、插件、系统和设置页真实命名导航均正常退出，退出码为 0。
- 插件页工程截图生成成功，未使用坐标点击自动化。
- `git diff --check` 通过。

## 12. 第七轮 Web 界面与原生后台组件分层

本轮关闭上一轮确认的 ClipboardHistory “声明后台运行、实际依赖首次打开页面”问题，并将修复抽象为可复用的混合插件架构：

1. Web 插件 Manifest 可选声明 `background.entry_point`，由宿主在同一插件生命周期内加载原生 DLL 后台组件。WebView 仍按需创建，后台组件不依赖隐藏页面。
2. 新增 `WebPluginWithBackgroundAdapter`，统一组合 Web UI、原生后台生命周期、命令、语言切换和资源释放。原生组件可通过 `IPluginOpenRequestSource` 请求显示 Web UI，ClipboardHistory 的 `Ctrl+Shift+V` 因此能够在未打开页面时工作。
3. ClipboardHistory 的剪贴板监听、去重、500 条上限、持久化和全局热键已迁移到 `ClipboardHistory.Background.dll`；页面只负责展示、查询和用户交互，不再重复注册热键。
4. Manifest 校验限制后台入口只能用于 `webview`，且必须是插件目录内的相对 DLL 路径，阻止绝对路径和目录穿越。
5. 真实运行暴露出 `StorageService` 在写锁内再次获取读锁的问题，导致监听成功但历史持久化失败。现改为在写锁内直接生成快照，并在写盘失败时回滚内存状态；服务同时正式实现 `IDisposable`，确保插件卸载时释放锁资源。

真实隔离冒烟结果：

- 宿主启动后未打开 ClipboardHistory 页面，写入唯一剪贴板标记后，`clipboard_history` 成功持久化 1 条匹配记录。
- 空闲阶段宿主 WebView 子进程数为 0，证明后台监听不依赖隐藏 WebView2。
- 空闲样本工作集 197.4 MB、私有内存 171.6 MB。
- 执行 `com.long.clipboardhistory:clipboard-history.open` 后才初始化 WebView；命令退出码 0，后台热键冲突数为 0，退出时热键和 WebView 均正常释放。

后续开发顺序：

1. 优化插件页 25 张复杂 WPF 卡片的模板成本、可视区创建和离开页面后的回收，降低插件页 214.5 MB 工作集与 246.3 MB 私有内存。
2. 为 ClipboardHistory 增加并发写入/页面打开期间的存储一致性压力测试，评估是否需要存储版本号或跨实例更新通知。
3. 在同一干净环境执行至少 5 轮冷启动、热启动、概览页和插件页采样，按中位数与最大值判断性能 P0；门槛通过前仍不生成新 RC。
4. 完成主题对比度、任务栏独立身份及高副作用插件的人工验收清单，关闭剩余发布阻断项。

本轮验证：

- Release 构建：0 警告、0 错误。
- 自动化测试扩展至 685 项，覆盖后台入口合法性、目录越界和扩展名约束。
- 真实后台监听、持久化、按热键/命令打开 UI 和退出资源释放均通过。
- `git diff --check` 通过。

## 13. 第八轮插件管理页资源回收与高 DPI 合成优化

本轮沿插件页对象持有链和真实渲染成本继续审计，确认已有回收虚拟化只能限制可视容器数量，不能处理页面离开后的长期持有：

1. `PluginManagementControl` 此前被 `PluginManagementHost` 永久缓存；导航离开只把外层面板设为 `Collapsed`，控件仍订阅全局 `PluginsChanged`，并继续持有卡片投影、数据源和已实现的视觉容器。
2. 控件现实现 `IDisposable`。离开插件页或 ToolCenter 卸载时，宿主会先断开全局事件、取消待执行的 Dispatcher 刷新、清空 `ItemsSource`，再从 `ContentControl` 移除页面；再次进入时重新按需创建。
3. 单张卡片的能力区域原先使用嵌套 `ItemsControl` 为最多 3 项能力建立容器和模板。现改为单个预计算摘要徽标，保留前三项与剩余数量信息，减少可视树节点和绑定。
4. 真实 200% DPI 截图确认列表卡片的 Level 1 阴影会为每个可视卡片增加离屏合成表面。插件管理属于高频滚动的操作列表，现改为无阴影描边卡片，保留 hover、边界和状态层级。

同一 Release 输出、`1120×760`、每次启动后等待 9 秒的 5 次独立插件页采样：

- 整改前：工作集平均 209.8 MB、最大 222.0 MB；私有内存平均 258.7 MB、最高样本 283.6 MB。
- 整改后：工作集样本为 207.8、208.6、208.6、206.3、200.8 MB，平均 206.4 MB、中位数 207.8 MB、最大 208.6 MB。
- 整改后私有内存样本为 208.5、212.3、209.5、213.7、205.2 MB，平均 209.8 MB、中位数 209.5 MB、最大 213.7 MB。
- 工作集平均下降 3.4 MB、最大值下降 13.4 MB；私有内存平均下降 48.9 MB。工作集仍未稳定低于 200 MB，性能 P0 保持未关闭。

验证与证据：

- Release 构建：0 警告、0 错误。
- 全量自动化：685 / 685 通过。
- 25 个插件目录、25 份 Manifest、0 个 `config.json`，ClipboardHistory 原生后台 DLL 存在。
- `1120×760` 浅色、200% 物理 DPI 截图及元数据位于 `artifacts/quality/plugin-page-reclamation-20260727/`；布局、颜色、按钮、能力摘要和滚动区域正常。
- 性能采样不使用坐标点击；无法触发 WPF `Click` 路由的 UI Automation 选择样本已排除，没有计入结论。

下一开发顺序：

1. 建立可重复的插件页进入/离开语义质量探针，直接验证页面弱引用回收和离页后的私有内存，而不是依赖工作集立即归还。
2. 执行 ClipboardHistory 后台组件与可见页面并发修改历史的存储一致性压力测试，必要时增加版本检查或原子更新 API。
3. 对主窗口剩余共享阴影、欢迎区和导航视觉树做逐项 A/B 采样；在 5 次概览页和插件页最大值都满足门槛前不生成新 RC。
4. 继续完成人工主题对比度、任务栏独立身份和高副作用插件验收。

## 14. 第九轮页面回收证明与剪贴板并发一致性

本轮关闭第八轮留下的两个可自动验证问题：

1. 新增 `--quality-plugin-page-release-report` 质量入口。宿主会真实进入插件页、返回概览、释放页面，再执行三轮完整压缩 GC；报告以原页面弱引用是否仍存活作为通过条件，并用独立退出码阻断失败。
2. 真实 Release 探针退出码为 0，`plugin_page_collected=true`，证明 `PluginManagementControl` 不再被 ToolCenter、全局事件或待处理 Dispatcher 操作持有。报告位于 `artifacts/quality/plugin-page-release-probe-20260727/release-probe.json`。
3. 探针单次工作集为 207.9 → 208.1 MB、私有内存为 184.9 → 224.8 MB。完整 GC 会触发压缩与内存提交，因此这些瞬时数值不能解释为离页内存下降；本轮只采用弱引用回收结论，继续以独立启动的 5 次采样判断性能门槛。
4. 审计确认 ClipboardHistory 原生后台与可见 Web 页面此前各自执行 `get` + 整份 JSON `set`。两条本地队列只能保证各自串行，跨组件交错时会发生最后写入覆盖。
5. `IStorageService` 与 `long.storage` 新增原子 `compareExchange`：在同一写锁内比较预期旧值、提交新值并持久化；不匹配返回 `data=false`，磁盘失败会恢复旧内存状态。
6. ClipboardHistory 后台与页面均改为“读取快照 → 变换 → CAS”，冲突时重新读取并重放，最多重试 8 次。新增并发测试确认 16 个相同旧快照的竞争写入只有 1 个成功，陈旧快照不会覆盖当前值。

真实功能验证：

- 隔离 Release 宿主等待原生后台就绪后写入唯一剪贴板标记，退出码 0，历史中恰好存在 1 条匹配记录；测试后恢复原剪贴板文本。
- `com.long.clipboardhistory:clipboard-history.open` 真实页面命令退出码 0，新 Bridge API 注入与页面初始化成功。
- 从空 `config` 首次打开页面成功创建存储并写入 1 条当前记录，持久化冲突耗尽警告为 0，覆盖 Web Bridge `null` 预期值和首次竞争写入。
- 退出日志确认 `Ctrl+Shift+V` 已注销，ClipboardHistory WebView2 资源已释放。

本轮验证：

- Release 构建：0 警告、0 错误。
- 全量自动化：689 / 689 通过，包含 Web Bridge `null` 预期值解析边界。
- 页面弱引用回收探针、原子存储竞争、原生后台记录和 Web 页面命令均通过。
- 性能 P0 仍保持未关闭；下一轮继续对概览页共享视觉层和基础服务做可重复 A/B 审计。

## 15. 第十轮概览页合成成本 A/B 与基础服务审计

本轮先对默认概览页执行 5 次独立 Release 启动采样，再审计 `ServicesInitializer` 及其启动链。性能计数器、网络枚举和计划任务计时器均为调用时创建或执行；鼠标手势默认关闭时不会安装全局钩子。当前没有证据支持直接延迟某个宿主能力服务，盲目改造会扩大插件能力解析和启动时序风险。

概览页首屏会同时创建 5 张 `LongCard`，全局样式为每张卡应用 `DropShadowEffect`。结合第八轮插件管理页的同类证据，本轮新增局部 `OverviewCard` 样式，仅在概览页取消阴影，保留背景、描边、圆角、内边距和 hover 语义；全局设计令牌及其他页面不变。新增质量门禁固定 5 张概览卡必须使用该轻量样式。

同一 Release 输出、`1120×760`、每次启动后等待 9 秒的 5 次独立采样：

- 整改前工作集平均 200.6 MB、中位数 206.2 MB、最大 206.6 MB；私有内存平均 173.7 MB、中位数 178.6 MB。
- 整改后工作集平均 203.1 MB、中位数 209.6 MB、最大 211.1 MB；私有内存平均 149.6 MB、中位数 154.6 MB。
- 私有内存均值下降 24.1 MB，约 13.9%；进程 CPU 时间均值由 5.75 秒降至 5.54 秒，约 3.7%。
- 工作集均值上升 2.5 MB，仍存在明显驻留集合波动，严格 `<200 MB` 最大值门槛未通过。此次只认定“实际提交内存下降”，不认定性能 P0 已关闭。

验证与证据：

- Release 构建：0 警告、0 错误。
- 全量自动化：690 / 690 通过。
- `1120×760` 概览截图确认布局、描边、文字和按钮对比度正常。
- 原始 A/B 样本与截图位于 `artifacts/quality/overview-render-cost-20260727/`。

下一步开发顺序：

1. 为宿主启动链增加分段、低开销的质量测量，分别记录服务初始化、主窗口构造、插件目录装载和运行时就绪，先定位 CPU 与工作集峰值阶段。
2. 仅对测量确认的高成本服务实施按需创建或后台预热，并保持 `HostProvider` 能力注册、插件首次调用和退出释放契约。
3. 以相同 5 轮参数复测概览页和插件页；两者最大工作集稳定低于门槛前，性能 P0 与新 RC 均保持阻断。
4. 继续人工验收主题对比度、任务栏独立身份和高副作用插件正向流程。

## 16. 第十一轮启动分段测量与管理卡合成收敛

本轮新增 `--quality-startup-report <path>` 质量入口。该入口只在显式启用时创建 `StartupPerformanceTrace`，在应用启动、服务初始化、主题应用、主窗口构造、句柄就绪、窗口 Loaded、事务恢复、包安装、插件扫描和运行时就绪处采集单次资源快照；不创建轮询器或常驻计时器，写完 JSON 后以退出码 0 自动结束。

每个节点记录累计耗时、累计 CPU、工作集、私有内存、托管堆和线程数。报告由应用自身生成，不再依赖固定等待后强制终止进程。参数解析和有序 JSON 样本均有自动化契约覆盖。

首轮 5 次 Release 启动审计：

- 服务初始化平均 89.6 ms，私有内存只增加约 1.2 MB；性能计数器、网络枚举和计划任务均为调用时执行，当前不实施服务懒加载。
- 主窗口构造到 Loaded 平均 1206.0 ms，是启动链最大阶段；插件扫描平均 656.4 ms，但运行时就绪前只增加少量私有内存。
- 结论与第十轮视觉层证据一致：当前首启主要成本在 WPF 主窗口视觉树和渲染，而不是基础服务对象。

基于该结论，ToolCenter 新增统一 `ManagementCard`。5 张概览卡与 16 张系统、开发者、设置及宿主状态卡复用同一无阴影描边样式，保留语义背景、边框、圆角、内边距和 hover；全局 `LongCard` 及 ToolCenter 外部页面不变。

整改后 5 次同参数启动：

- 运行时就绪平均耗时 2427.5 → 2147.3 ms，下降约 11.5%；中位数 2084.9 → 1970.6 ms，下降约 5.5%。
- 累计 CPU 平均 2734.4 → 2503.1 ms，下降约 8.5%；CPU 中位数仅下降约 1.2%。
- 工作集平均 187.4 → 187.2 MB，最大值均为 198.6 MB；私有内存平均 141.5 → 142.1 MB，没有改善。
- 两组按先后顺序执行，插件扫描和服务阶段也受到文件缓存影响，因此只能认定“观察到启动耗时改善且无内存收益”，不能把全部降幅归因于阴影调整，也不能据此关闭 9 秒空闲内存门槛。

验证与证据：

- Release 构建：0 警告、0 错误。
- 新增后全量自动化：691 / 691 通过。
- 首次全量运行中 `LocalFileSearchProviderTests.SearchAsync_UsesCurrentLocalizedProjection` 曾出现一次空结果；定向连续 5 / 5 及随后完整 691 / 691 均通过，暂记为异步索引稳定性观察，不在本批无证据修改搜索实现。
- 系统页和设置页 `1120×760` 截图确认层级、边界、按钮对比度和滚动区域正常。
- 10 份原始启动报告、汇总和视觉证据位于 `artifacts/quality/startup-segments-20260727/`。

下一步开发顺序：

1. 将启动 A/B 改为同一二进制交错运行或加入显式对照开关，降低文件缓存和运行顺序对因果判断的影响。
2. 继续拆分主窗口“XAML 构造”和“首次布局/渲染”成本，优先评估系统、开发者和设置面板的视觉树按需创建，不改变现有字段与导航契约。
3. 对插件扫描只做目录发现、Manifest 解析、运行时实例化三个子阶段测量；仅当某子阶段在交错样本中稳定占主导时再优化。
4. 保持 9 秒概览页和插件页最大工作集门槛、主题/任务栏/高副作用插件人工验收及新 RC 阻断状态。

## 17. 第十二轮同二进制交错 A/B 与扫描子阶段审计

本轮修正第十一轮“先基线、后优化”顺序运行无法排除文件缓存的证据缺口。新增 `--quality-management-card-shadows`，只在质量模式把 21 张 `ManagementCard` / `OverviewCard` 恢复为旧 `Long.Shadow.Level1`；正常启动仍使用无阴影样式。启动报告新增 `management_card_shadow_count`，对照探针实测命中 21 / 21 张卡，避免仅记录请求参数却没有改变视觉树。

首次实现使用构造阶段 VisualTree 遍历，报告虽然记录开关为真，但优化与对照截图 SHA-256 完全相同。该批 12 轮调试数据被判定无效并移出工作区。最终实现改用 LogicalTree 和样式继承链匹配，为每张卡恢复独立效果实例；优化与对照截图哈希不同，质量基线才被接受。

启动时间轴同时增加 `main_window_first_layout`、`main_window_first_render`、目录发现、单文件插件、Manifest 读取和插件注册阶段。插件引擎只接收通用 `Action<string>` 阶段回调，不依赖质量服务类型。

有效矩阵使用同一 Release 二进制，按 `A/B/B/A/B/A/A/B/A/B/B/A` 交错运行，无阴影与阴影各 6 轮：

- 无阴影运行时就绪平均 3965.3 ms、中位数 3630.6 ms；阴影平均 4366.6 ms、中位数 4275.6 ms。平均下降约 9.2%，中位数下降约 15.1%。
- 无阴影累计 CPU 平均 3763.0 ms、中位数 3726.6 ms；阴影平均 4375.0 ms、中位数 4461.0 ms，分别下降约 14.0% 和 16.5%。
- 私有内存均值 171.4 → 148.5 MB，下降 22.9 MB、约 13.4%；中位数下降 18.2 MB，最大值下降 25.6 MB。
- 工作集均值和中位数方向不一致，最大值仅 202.2 → 200.3 MB，继续判定为高波动且未稳定改善，不能关闭严格空闲门槛。
- 构造到首次布局平均下降约 20.8%，首次布局到 Loaded 平均下降约 30.4%，确认差异主要位于 WPF 视觉树合成。

截图进一步确认旧 `DropShadowEffect` 作用于整个 Border 子树，卡片正文和按钮边缘也出现重影；无阴影描边版文本更清晰，卡片边界和 hover 语义仍完整。

插件扫描子阶段：

- 目录发现两组均约 15 ms，不是优化目标。
- Manifest 读取、注册及 7 个自动启动实例在不同运行中受磁盘缓存和系统负载影响明显，交错组间方向与视觉开关无关。
- 当前不改变扫描、延迟注册或自动启动语义；后续如继续优化，必须为注册与自动启动实例再分段，而不是压缩目录枚举。

验证与证据：

- Release 构建：0 警告、0 错误。
- 全量自动化：691 / 691 通过。
- 12 份有效交错报告、1 份 21 卡命中探针、两张系统页截图、元数据和汇总位于 `artifacts/quality/startup-interleaved-final-20260727/`。
- 本轮更正第十一轮口径：顺序 A/B 的 5–11% 观察值不能作为因果证据；本轮同二进制交错矩阵才是管理卡阴影结论的有效依据。

下一步开发顺序：

1. 将系统、开发者和设置面板迁移为首次导航时创建的独立视图，减少默认概览首屏仍然构造的隐藏视觉树。
2. 保留现有 ToolCenter 导航、状态字段、动态语言、响应式布局和质量命名入口；按页面拆分，避免把业务逻辑继续堆回主控件。
3. 使用本轮启动探针复测“构造到首次布局”和私有内存，并分别截图首次进入三个新页面。
4. 性能 P0、新 RC、主题/任务栏和高副作用插件人工门禁继续保持原状态。
