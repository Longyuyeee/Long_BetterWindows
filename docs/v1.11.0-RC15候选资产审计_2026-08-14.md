# Long助手 v1.11.0-rc.15 候选资产审计

> 审计日期：2026-08-14
>
> 候选源码：`b6076ae6034e66fc5b109fa45c1520bf6d2fed3f`
>
> 版本身份：`v1.11.0-rc.15`
>
> 分发政策：公开 prerelease、unsigned、当前用户安装、不请求提升权限

## 结论

RC15 已从精确干净提交 `b6076ae` 完成全套生产打包。Release 构建、1461 项全量测试、两种发布模式、真实命令冒烟、WebView2 清理、安装器编译、插件/命令/开发文档数量以及三份实物哈希全部通过。Manifest 记录 `source_dirty=false`、`release_eligible=true`，候选资产工程结构合格，可以进入完整桌面探索验收。

本结论只关闭“RC15 资产生成与结构审计”，不代表 RC15 已完成探索验收，也不允许冻结或发布正式 `v1.11.0`。RC15 尚未创建 Git 标签或 GitHub Release；本机原始资产继续位于被排除的 `artifacts/**`，不进入源码提交。

## 构建与测试

- `release.ps1` 在不存在的目标目录上执行，没有使用 `-Force`，没有覆盖 RC2–RC14 历史候选。
- Release 解决方案构建：0 警告、0 错误。
- Release 全量自动化：1461 / 1461，通过 1461，失败 0，跳过 0。
- 两种候选均通过 `com.long.base64:base64.encode` 真实命令冒烟，退出码为 0。
- self-contained 启动约 3605 ms、命令约 2714 ms、WebView2 清理约 6566 ms。
- framework-dependent 启动约 3564 ms、命令约 2298 ms、WebView2 清理约 6487 ms。
- 两种候选均没有新增残留 WebView2 进程。

## 资产总账

| 文件 | 字节数 | SHA-256 |
|---|---:|---|
| `LongBetterWindows-v1.11.0-rc.15-win-x64-self-contained.zip` | 83,799,306 | `37a00e3b9ad43de75a69b2359952d4c77c1725de0fbc2fd0e4afa8120bee9378` |
| `LongBetterWindows-v1.11.0-rc.15-win-x64-framework-dependent.zip` | 13,943,933 | `3fae795550a729b337d6c7ff0446898c39ea2a53c61b2a4af70c6430618b990f` |
| `LongAssistant-Setup-v1.11.0-rc.15.exe` | 59,701,293 | `d568c759f90eeb6f1a194bfd038361c5942abfc97a291b0e6c7805b150899c5b` |

`SHA256SUMS.txt` 的三项逐件重算全部一致。`release-manifest.json` SHA-256 为 `6d31ab4304809cb23bac90aad5412fec865c0bc4aa7dcea670712949e060912d`。

## 结构与发布政策

两种 ZIP 均包含：

- 25 个插件 Manifest；
- 25 个唯一插件 ID；
- 42 条命令；
- 12 份发布态开发文档；
- 规范 `/` ZIP 路径，反斜杠条目为 0。

安装器和 Manifest 如实记录：

- `install_scope=current-user`；
- `installer_privileges=lowest`；
- `requires_elevation=false`；
- `distribution_channel=unsigned`；
- `publisher_identity=unverified`；
- `authenticode_status=not_signed`；
- `signed=false`；
- 需要 SmartScreen 披露、SHA-256 核对和 RSA-SHA256 更新清单签名。

## 机器就绪聚合

完整 `verify-final-closure.ps1` 复跑确认：

- 源码干净，构建、全量测试和 LPWP 兼容性全部通过；
- 插件正向矩阵合同有效，25 个插件、42 条命令、87 条自动证据；
- LPWP 1.0 的 .NET 合同、Web SDK、四运行时矩阵和 6 个 Fixture 有效；
- 机器阻断为 0，`ready_for_human_validation=true`；
- 人工/外部类别仍有 10 项，插件正式回执为 0 / 25，因此最终聚合仍正确记录 `release_eligible=false`。

完整机器报告 SHA-256 为 `42f71b15c1a2bd827a857da8d6bcab11b15c89ee2efc4a9a95fa8d5d5ef0c9d3`。这与候选 Manifest 的 `release_eligible=true` 不矛盾：前者表示“整个稳定版发布尚未通过人工门禁”，后者表示“本次生成的候选资产来自干净源码并通过机器打包门禁”。

## 下一步

1. 使用 RC15 self-contained 候选执行完整桌面探索验收，不使用开发输出目录替代候选。
2. 覆盖安装/首次启动、命令中心、Launcher、Super Panel、Workspace、Widget、25 插件关键路径、托盘、热键、亮暗主题、键盘、任务栏与正常退出。
3. 如果发现任何需要修改代码、脚本、资源或普通文档的问题，保留 RC15 资产并提升为新的 RC 身份。
4. 只有连续一轮完整探索验收不再发现需修改问题，才允许进入最终 `1.11.0` 身份冻结。

本机原始路径：`artifacts/releases/v1.11.0-rc.15/` 与 `artifacts/quality/final-closure-b6076ae-rc15/`。这些路径不得提交 Git。

## 桌面自动化验收补充

2026-08-14 已使用本页锁定的 self-contained 候选完成全量桌面 UI 自动化探索，最终 `passed=true`、`error=null`，报告 SHA-256 为 `5baa7f39d0f39fe08039afda8499b3f8efef1bf224dcec1703a2a822b577ed33`，退出后无宿主残留。期间修复的是仓库外部验收脚本的 WPF UIA 阻塞与跨进程全桌面搜索问题，候选程序和本页记录的 RC15 资产未改变。

RC15 现在可以继续进入安装、首次启动和人工验收，但尚未获得正式版发布资格。详细审计见 [`v1.11.0-RC15桌面自动化验收审计_2026-08-14.md`](v1.11.0-RC15桌面自动化验收审计_2026-08-14.md)。
