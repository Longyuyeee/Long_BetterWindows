# Long窗口当前开发状态

> 最后更新：2026-07-24
> 唯一开发主线：`master`
> 当前开发目标：`v1.10.0`（尚未冻结）
> 历史候选：`v1.9.0-rc.1`

## 当前基线

- .NET 8 WPF 宿主，支持 DLL、C# Script 和 WebView2 三种插件运行时。
- 27 个宿主能力接口，v1.8 的壁纸、亮度、网络监控等能力全部保留。
- 25 个内置插件，发布项目必须复制 25 个插件目录和 25 个 Manifest。
- Long Design System、统一命令中心、上下文搜索、超级面板和鼠标手势已进入主线。
- 可信插件市场支持签名校验、安装、升级、降级、卸载、中断恢复、离线缓存和并发控制。
- 市场发布工具支持目录生成、部署、验证和回滚。
- 当前 Release 构建为 0 警告、0 错误；轻量生成/转换工具 G17-D1 完成后自动化测试 574 / 574 通过。
- `win-x64` 自包含与框架依赖发布包均已通过宿主及真实命令冒烟，包含 25 个插件目录、25 个 Manifest、25 个唯一插件 ID 和 42 条命令，并生成双包 SHA-256。
- 25 插件连续 5 次空闲内存采样通过：中位数 165.6 MB，最大值 181.4 MB，低于 200 MB 门槛。

## 发布状态

`v1.9.0-rc.1` 作为历史 unsigned 候选保持不可变；其后的命令参数 Schema、Sparse Package 和国际化功能已进入 `master`，下一次候选从当前主线按 `v1.10.0` 重新冻结、构建和生成证据。正式发布仍需完成：

1. 真实 Registry/CDN、公钥和发布凭据的受控环境演练。
2. unsigned 包的真实下载/SmartScreen 观察、干净用户环境以及物理 DPI/可访问性人工签核。

2026-07-22 发布决策调整为公开 `unsigned` 通道：代码签名作为未来可选增强，不再阻塞 v1.9。Manifest 必须如实保留 `signed=false`、`publisher_identity=unverified` 和安全提示；只有干净源码及全部自动门禁通过时才能写入 `release_eligible=true`。`v1.9.0-rc.1` Prerelease 已发布，标签精确指向候选提交 `d3cd956`，四个远端资产的 GitHub SHA-256 digest 与本地一致。自包含包已通过真实浏览器下载，80,569,737 字节和 SHA-256 与 Manifest 一致，并保留 Windows `Zone.Identifier` 的互联网区域 `ZoneId=3`；脱敏证据位于 `artifacts/quality/release-download-v1.9.0-rc.1/download-evidence.json`。下载门禁已形成不可变的采集、独立批准、汇总验证三段式，批准文件必须锁定原证据哈希并要求操作人与审核人不同。当前未生成正式批准文件，因此 SmartScreen、杀毒软件或首次启动仍未人工签核。默认生产令牌变量仍未配置，候选内 Marketplace Registry 仍是本地包配置且发布者信任库为空；RC 完成人工门禁后再决定正式 `v1.9.0`。

## 设计对齐审计

2026-07-23 再次对照最初产品愿景、插件通信设计、UI/UX 规范和交互平台路线完成主线审计。当前实现继续遵循 Windows 原生、本地优先、插件能力化、统一入口、上下文匹配、契约优先和可信分发等核心边界，产品方向保持一致。组合动作、市场事务和 Web 生命周期均复用宿主能力与审查边界，没有形成绕过插件命令契约的第二套执行系统。

当前偏差集中在工程收口，而不是产品方向：

1. `ClipboardHistory`、`DevToolkit`、`FileOrganizer`、`HardwareMonitor`、`PortManager` 随 v1.8 主线保留时没有声明统一命令、窗口和生命周期契约；该缺口已在首批收口中补齐，源码与新候选包基线均为 25 插件、42 条命令。旧候选包记录的 37 条命令仅作为合并时历史证据保留。
2. 新 `MarketplaceControl` 已成为唯一正式市场入口；旧 `MarketPanel` 及旧下载、安装、更新服务已在确认无运行时引用和迁移责任后移除，并由架构测试防止双实现回流。
3. `PerformancePanel` 已迁移到 Long Design System，并通过独立刷新协调器接入 ToolCenter“平台诊断”；`CapabilityDetailPanel` 已迁移为数据模板视图，可从每个插件卡片查看权限和本地调用记录。
4. 宿主大型类正在增量拆分：性能刷新、插件管理、启动参数、质量运行时和插件启动协调均已形成独立模块，`App.xaml.cs` 从 672 行降至约 430 行；市场运行时、展示投影与会话协调已拆分，`MarketplaceControl` 不再保存安装/卸载待确认状态，并通过协调器处理加载取消、操作防重入和结果生命周期；超级面板搜索、动作、分组、拖拽、键盘和窗口生命周期已形成独立模块。组编辑会话、二级动作菜单以及上下文摘要/动作结果视图投影均已迁出窗口；`SuperPanelWindow.xaml.cs` 当前 501 行，只保留 Popup/ContextMenu 控件应用和事件转接。插件来源发现、目录运行时装载、单文件脚本生命周期与热重载监控均已拆分，`PluginScanner.cs` 从 731 行降至 286 行。Web 插件运行与呈现链路已完成协议、参数、宿主分发、WebView 生命周期和呈现协调分层，`WebPluginRuntime.cs` 与 `WebPluginAdapter.cs` 分别降至 111 行和 114 行。

## 2026-07-20 审计结论与开发决策

本次审计覆盖 `master` 与 `origin/master` 同步状态、累计源码差异、宿主主要类规模、自动化测试、视觉证据以及最初 UI/UX 设计目标。结论如下：

1. **方向对齐**：统一命令、上下文匹配、超级面板、插件生命周期和可信市场已经构成 Windows 效率中枢，不需要在 v1.9 发布前新增更多独立工具。
2. **架构进度健康**：启动、插件装载、Web 运行时、性能刷新和超级面板的状态/行为已形成独立模块；旧市场双实现已移除。当前拆分应以事务边界和可测试行为为依据，不以文件行数归零为目标。
3. **市场交互风险已收口**：新增 `MarketplaceSessionCoordinator`，旧目录加载会被新请求取消，重复下载/预览/确认被拒绝；失败保留有效确认态，成功后清理状态并刷新列表。`MarketplaceControl` 只消费协调结果并维护 WPF 展示与文件选择器。
4. **最高发布风险位于外部环境**：真实 Registry/CDN、正式公钥与发布凭据、unsigned 包真实下载提示、干净用户环境、物理 DPI 和可访问性无法仅靠源码测试替代，必须作为独立发布门禁留在 v1.9 范围内。Windows 代码签名已降为可选增强。
5. **当前证据可信**：审计期间发现上下文 Provider 与整套测试共享线程池时，慢同步实现可能令快速 Provider 在超时前无法获得调度；现已改为独立长任务调度，超时仍从提交时开始，并完成 5 轮目标压力复测。Release 构建为 0 警告、0 错误，298 / 298 测试通过。基于干净提交 `d3cd956` 的新候选包记录 `distribution_channel=unsigned`、`publisher_identity=unverified`、`signed=false`、`release_eligible=true`，并包含双包 25 / 25 插件、42 条命令、真实命令退出码 0 和 WebView2 零新增残留；双包实际 SHA-256 与 Manifest、SHA256SUMS 完全一致，ZIP 内各有 25 个标准 `/` 路径 Manifest 且无反斜杠条目。真实浏览器下载证据进一步锁定自包含包哈希、字节数、HTTPS 来源主机和 `ZoneId=3`，且不记录 GitHub 临时 URL 查询参数。市场协调器真实隔离事务再次覆盖签名安装、拒绝、升级、回退、卸载和重启恢复；本地市场发布演练进一步覆盖一次性 RSA 签名、基线部署、候选预演、覆盖与回滚，并确认回滚后旧 Registry 所引用包仍存在、私钥已删除；平台诊断与插件管理截图均有渲染元数据。

## 远端分支审计

2026-07-24 已重新抓取、比较并整合全部远端功能分支：

1. `agent/development-audit-20260724` 唯一领先提交 `df841c8` 已快进并入 `master`，带入命令参数 Schema、Win11 Sparse Package 和宿主国际化里程碑。
2. `agent/v1.9-release-hardening` 与 `agent/workflow-audit-hardening` 无独有提交，均已被主线完整包含。
3. `agent/release-candidate-handoff` 的 `6aef0cb` 从历史主线分叉；功能整合基线 `df841c8` 相对它为 110 个独有提交对 1 个旧交接提交。该提交的大包内容已由后续主线按模块演进覆盖，不再合并，避免旧宿主结构和历史发布口径回流。
4. 唯一开发主线仍为 `master`。四个 `agent/*` 分支只保留为远端历史引用，不作为后续开发起点；确认不再需要历史对照后可在 GitHub 删除。

开发顺序已经确定：

1. [x] **P0 市场交互收口**：市场会话/操作协调器已覆盖加载取消、重复点击、确认态清理、成功刷新和失败保留，并通过纯逻辑单元测试、架构门禁与真实隔离事务。
2. [x] **P0 v1.9 本地发布门禁**：已从干净提交 `d3cd956` 重新生成 `unsigned` 双候选包；两种包均通过 25 个唯一插件、42 条命令、真实 Base64 命令、宿主退出、标准 ZIP 路径和 WebView2 差集验证，SHA-256 与机器可读 Manifest 已固定在本地候选目录。
3. **P0 v1.9 外部发布门禁**：本地签名、部署、覆盖与回滚全链演练已完成；生产演练器已改为在写入线上 Registry 前强制执行候选 Dry Run、Registry 最后提交检查和现网公开基线验收，并核对实际部署与预演 Release ID。干净 Windows 环境和真实下载现均已建立采集、独立人工批准和哈希汇总三段式门禁，并要求证据显式绑定 `unsigned` 或 `signed` 通道。统一决策器进一步绑定 Release Manifest、下载、干净环境、物理 DPI、可访问性和完整 HTTPS Registry 演练，拒绝包哈希错配、预检冒充完整演练及覆盖既有决策。Windows Authenticode 工具链保留为可选增强。`d3cd956` unsigned 候选已经重建并通过独立复核；真实浏览器下载、哈希和互联网来源标记已自动验证。下一步由人工执行 SmartScreen、杀毒软件、首次启动和完整生命周期观察，再由不同审核人批准，并使用真实 Registry/CDN 完成演练。
4. **P1 体验签核**：当前主线桌面 UI 已重新完成键盘执行、菜单、窗口交接、Web 插件分离返回、市场确认取消和三种可访问性模式冒烟；核心 Window/Edit/List/Button 的可访问名称与类型均已由 UI Automation 读取验证，动态状态已补充 Live Region。真实辅助功能和物理 DPI 均已建立采集、人工批准、汇总三段式门禁，采集要求干净受跟踪源码并重建指定提交，汇总拒绝混用不同提交的证据；当前机器处于普通主题、动画开启且未运行读屏器，因此不伪造人工签核。物理 100%/125%/150%/200% DPI、真实 Windows 设置和实际屏幕阅读器朗读仍需人工设备签核。
5. **P1 v1.9 冻结发布**：固定版本、哈希、命令与插件清单，冻结源码并发布；不把外部凭据写入仓库。
6. **v1.9 之后**：组合动作编排首轮闭环已经完成，包括安全持久化、Tool Center 编辑、结构化输入与参数、外部导入审查、权限批准、顺序执行、取消、逆序补偿、脱敏报告和工作流导出。工作流现采用 Schema v3，v2 以同来源、空绑定语义迁移；规范化定义哈希与授权指纹均包含绑定结构。`PluginCommandResult` 已增加防御性复制的 `Text` / `Path` 类型化输出；执行器按成功步骤保存隔离快照，解析文本、路径和参数绑定，允许补偿引用自身主命令输出，并统一限制 64 个输出、64 字符键和 65,536 字符值。Manifest 命令可声明输出键、类型和说明，读取、预检与执行均校验声明；文件夹备注助手返回 `selected-folder` Path。Tool Center 基于执行位置和 Manifest 声明提供来源下拉与类型兼容目标，失效声明会保留并阻止保存。Web 命令桥使用请求 ID 等待页面异步结果，支持取消、30 秒超时、64 个待处理请求上限以及结果大小/类型校验。Base64、URL、JSON、时间戳和 UUID 共八条 Web 命令现返回真实 `result` Text。执行结果默认只显示不含值的输出元数据；本次权限确认显式批准后，成功工作流可显示、复制或清除未消费的终端输出。文件导出再执行一次独立批准，审查目标绝对路径、UTF-8 字节数与值 SHA-256；写入采用同目录临时文件、写穿透和无覆盖原子移动，并拒绝不存在的目录、重解析点路径、审查后值/目标变化及已有目标。执行报告 Schema v1 保持不变，不保存摘要、终端值或导出记录。详细契约见 `docs/组合动作编排协议.md`。

## 下一开发阶段

当前主线已完成全部功能分支整合。最近完成的代码基线为 574 / 574 自动化测试通过、Release 构建 0 警告 / 0 错误；按以下顺序继续推进：

1. **P0 外部发布门禁**：完成人工 SmartScreen/杀毒软件、干净 Windows、物理 DPI、真实辅助功能及 Registry/CDN 受控凭据演练；代码签名保持可选，不阻塞 unsigned 通道。
2. [x] **P1 组合动作稳定化（展示协调边界）**：运行审查、准备失败、执行结果、输出摘要、终端输出和报告时间线已提取到无 WPF 依赖的 `WorkflowExecutionPresentation`；`WorkflowEditorControl.xaml.cs` 只应用投影结果并保留对话框、剪贴板及控件状态职责。新增 4 项行为测试覆盖权限风险、失效批准、终端值和脱敏报告展示。
3. [x] **P1 统一入口集成**：新增托管工作流搜索 Provider，仅投影已保存、可读取、定义哈希稳定且针对当前插件目录预检有效的组合动作；命令中心和超级面板使用独立 `OpenWorkflowReview` 动作导航到现有批准界面，执行前再次加载和预检，不注册伪插件命令，也不直接执行。结果支持固定和最近使用排序，并明确显示步骤数、失败策略、插件数与批准提示。
4. [x] **P1 真实交互验收前三批**：命令中心和超级面板入口、安全取消、默认宽屏、`720 × 560` 紧凑布局以及长终端输出均已闭环。第三批将编辑配置区与审查/运行/结果区分离，避免进入审查时物化整套重型编辑模板；质量模式使用注册窗口消息驱动批准、确认、状态查询与清除，不遍历长内容 UIA 树，也不使用坐标点击。当前多显示器 250% 缩放环境已验证只读执行、3699 字符终端输出、限高滚动、内存清除和隔离报告，证据位于 `artifacts/quality/workflow-long-output-20260723/desktop-ui-smoke.json`。
5. [x] **P1 插件升级失效验收**：托管工作流搜索动作携带不可变状态指纹；打开审查前重新加载定义、按当前插件目录预检，并由运行会话比较工作流定义、插件版本、注册修订和能力计划。隔离桌面事务已验证 v1 审查打开后通过正式安装器升级 v2，旧审查被拒绝、零步骤执行、报告脱敏、事务目录与隔离根清理；证据位于 `artifacts/quality/workflow-plugin-upgrade-20260723/workflow-plugin-upgrade.json`。扫描器现负责释放和定点重载目录运行时，审查/执行期间的插件变化不再重建编辑器。
6. [x] **P1 文件导出拒绝验收**：稳定失败分类与确定性测试覆盖已有目标、缺失目录、重解析点、审查后目标变化、值变化、移动竞态、权限失败和取消。真实宿主进一步使用目录联接与拒绝写入 ACL 完成矩阵，验证 3699 字节成功导出、SHA-256、无覆盖、无残留、报告脱敏、ACL 恢复和联接移除；证据位于 `artifacts/quality/workflow-terminal-export-full-20260723/desktop-ui-smoke.json`。
7. [x] **P2 易用性当前批次**：组合动作复制、只读模板、命令参数预设、插件内容三态和旧 Web 插件主题迁移均已完成。`HardwareMonitor`、`PortManager` 与 `ClipboardHistory` 已接通正式宿主 API并移除模拟数据或私有主题；剪贴板历史新增原生事件桥、15 秒兜底同步、时间筛选、热键降级、串行持久化和失败回滚。桌面及最小 `620px` 宽度交互验证通过。
8. [x] **P2 Web 能力生命周期硬化**：共享剪贴板监听采用多消费者租约，首个消费者安装 Windows Hook、最后释放才移除；运行时重复启停幂等，停止失败可重试，插件卸载自动释放。`ClipboardTool`、`ClipboardHistory` 与 `QuickNotePlugin` 覆盖仓库全部 Web 本地写入点，均检查宿主响应、串行持久化并在失败时回滚。
9. [x] **P2 命令参数 Schema 基础**：G1–G5 全部完成。契约、统一验证、工作流预检/动态绑定、v2 审查指纹、类型化编辑器和声明式绑定键下拉已闭环；无 Schema 命令继续使用高级键值界面。UUID 成为首个真实内置样板，插件指南覆盖五种类型与安全边界。宽屏和 `720 × 560` 紧凑 Release 宿主均由 UI Automation 完成整数值修改、布尔切换和预设控件发现；同时修复参数子控件 DataContext 震荡造成的空白与高 CPU 重建。Release 构建 0 警告、0 错误，自动化 490 / 490 通过，证据位于 `artifacts/quality/workflow-argument-schema-g5-20260723-uia-root/desktop-ui-smoke.json`。用户自定义预设、条件分支、并行执行、云同步和后台无人值守继续延后，避免扩大协议。
10. [x] **可选 Win11 Sparse Package 一级右键菜单工程闭环**：原生命令、未签名构建、受控签名、每用户管理事务、Tool Center 双通道 UI，以及只读预检、采集、独立批准和汇总验证门禁均已落地。本机重新构建确认 x64、COM 导出和未签名 MSIX 打包通过。正式证书和干净 Windows 11 Explorer 证据仅阻断该可选集成通道，不阻断 unsigned 主产品或后续国际化开发。
11. [ ] **宿主国际化**：G1、G2 及 G16-A/B 已完成工程实现。工作台静态外壳、动态状态、稳定错误键、42 条 API、实际 32 项能力元数据、双语生成模板和可访问确认对话框均已迁移；运行时切换保留编辑及权限草稿。本地 HTML 双语宽屏/紧凑证据已通过，真实 WPF/WebView2 候选证据仍待补录。G16-C1、C2-A、C2-B、C2-C 与 C3 已收口 Manifest、安装器、市场、工作流及 Tool Center 系统集成结构化错误。
12. [ ] **G16 宿主长尾与结构化错误**：G16-C 全仓失败出口复审已完成并关闭工程范围。Sparse Package 使用 6000 段稳定错误码；Explorer 备注列、兼容旧右键菜单及 Sparse Package UI 仅显示双语资源提示，技术摘要只进入日志。插件命令和超级面板动作消息经审计确认为业务输出并保留。中英文各 828 个业务键且集合一致，Release 构建 0 警告、0 错误，全量自动化 546 / 546 通过。G16 仅剩 B2.3-B 真实 WPF/WebView2 候选视觉证据；下一工程阶段进入 G17 原生插件语言与内置 UI。
13. [ ] **G17 原生插件语言与内置 UI**：G17-A 协议基础、G17-B 双运行时样板、G17-C 高频 Web 插件及 G17-D1 轻量工具已完成。Base64Tool、FolderNotePlugin、ClipboardTool、QuickNotePlugin、TimestampConverter 与 PasswordGenerator 已接入插件资源；语言切换不改变原 Manifest、目录修订、运行状态或用户内容。最小窗口中英文无头验证通过，Release 构建 0 警告、0 错误，全量自动化 574 / 574 通过。下一批 G17-D2-A 处理命令参数/预设显示投影与 UuidGenerator。

最新完整审计、风险分级、GitHub 发布范围与后续完成定义见 `docs/开发审计与后续计划_2026-07-24.md`；`docs/开发审计与后续计划_2026-07-23.md` 和 `docs/开发审计与后续计划_2026-07-22.md` 作为上一恢复点保留。

`v1.9.0-rc.1` 标签仍固定在候选提交 `d3cd956`。其后的组合动作、命令参数 Schema、Sparse Package 和国际化代码仅存在于后续 `master`，未进入该 RC 包，也不改变其 Manifest、哈希或人工发布门禁。下一候选按 `v1.10.0` 从干净主线重新生成。

平台诊断工程截图已在暗色主题、1280×800 逻辑尺寸、144 DPI 渲染参数下通过，证据位于 `artifacts/quality/diagnostics-smoke-20260720/`。
插件管理模块化后的工程截图已使用相同尺寸与渲染参数通过，证据位于 `artifacts/quality/plugin-management-smoke-20260720/`。

## 文档口径

本文档和根目录 README 描述当前主线。`v0.x`、`v1.0-v1.8` 总结文档以及 2026-07-20 之前的质量报告均作为历史记录保留，不再代表当前发布基线。
