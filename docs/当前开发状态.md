# Long助手当前开发状态

> 最后更新：2026-08-04
> 当前主线：`master` / `origin/master` 已对齐
> 当前源码候选：`v1.11.0-rc.3`（文档合并后须重新构建，尚未创建 GitHub Release）
> 当前开发目标：从最新干净主线重建 RC3，完成真人、真实设备、Long Grid 与外部分发验收
> 历史候选：`v1.11.0-rc.2` 保持不可变，不再具备发布资格

> 2026-08-02 总审计确认：产品定位、uTools 交互参考边界、Long 自有视觉、统一工作区和
> 插件平台均未发生实质偏移；工程主体已进入收口，但 10 类人工/外部门禁仍未完成。
> 完整对照、偏移判定与后续顺序见
> [`开发总审计与后续计划_2026-08-02.md`](开发总审计与后续计划_2026-08-02.md)。

## 当前基线

2026-07-27 真实用户测试阻断 `v1.11.0-rc.2` 后，主题、按钮对比度、交互性能、插件副作用生命周期和任务栏身份等代码根因均已整改。2026-08-01 审计确认旧 RC2 标签比修复后主线落后 180 个提交，不能继续复用同一版本号，因此当前源码提升为唯一的新候选 `v1.11.0-rc.3`。基线提交 `65f92f1` 的本地 RC3 包通过机器门禁，但 0 / 25 插件具备人工批准回执；2026-08-02 文档合并会改变源码提交，因此旧本地包与未完成截图会话只保留为历史证据，必须从新的干净 `master` 重建候选。RC3 仍需完成 10 类真实人工/外部验收，尚未发布，也不具备正式版资格。

- .NET 8 WPF 宿主，支持 DLL、C# Script 和 WebView2 三种插件运行时。
- 27 个宿主能力接口，v1.8 的壁纸、亮度、网络监控等能力全部保留。
- 25 个内置插件，发布项目必须复制 25 个插件目录和 25 个 Manifest。
- Long Design System、统一命令中心、上下文搜索、超级面板和鼠标手势已进入主线。
- 可信插件市场支持签名校验、安装、升级、降级、卸载、中断恢复、离线缓存和并发控制。
- 市场发布工具支持目录生成、部署、验证和回滚。
- 当前 Release 构建为 0 警告、0 错误；全量自动化测试 1104 / 1104 通过。
- 25 / 25 个内置插件均已声明 `zh-CN` / `en-US` 资源；宿主资源为 963 / 963 个对称业务键。
- `win-x64` 自包含与框架依赖发布包均已通过宿主及真实命令冒烟，包含 25 个插件目录、25 个 Manifest、25 个唯一插件 ID 和 42 条命令，并生成双包 SHA-256。
- 25 插件在干净提交连续 5×9 秒空闲内存采样通过：中位数 198.1 MB，最大值 198.7 MB，严格低于 200 MB 门槛。

## 2026-07-30 插件开发体系审计

当前源码已通过纯快进方式整合到 `master`，并完成本批验证与文档审计；`artifacts/**` 原始证据按策略保留本机。正式插件资产为 25 个插件、42 条命令、18 个 WebView 运行时和 7 个原生运行时，其中 ClipboardHistory 使用 Web UI + 原生后台；C# Script 运行时和模板已具备，但当前没有正式内置脚本插件。

审计确认下一阶段不应优先扩张插件数量，而应按“契约与脚手架、生产级打包验证、SDK、Long Plugin UI Kit、统一目录与市场、进程级隔离评估”推进。完整现状、差距、PX1–PX6 阶段和验收标准见 [`插件开发体系审计与优化计划_2026-07-30.md`](插件开发体系审计与优化计划_2026-07-30.md)。

**PX1A C# Script 脚手架契约闭环已完成。** 审计发现权威指南和 `src/Templates/script-plugin` 已声明 `-Template script`，但 `new-plugin.ps1` 仍拒绝该参数。当前批次已开放 Script 模板、跳过不适用的解决方案/DLL 构建步骤、校验 Manifest 与脚本入口、覆盖 `.csx` 占位符替换，并修复原生模板把带连字符目录名作为 C# 标识符、x86 dotnet 抢占 PATH 和单项目构建缺少 `SolutionDir` 的问题。真实 Script/Native 脚手架冒烟均通过；全量自动化 934 / 934，Release 构建 0 警告、0 错误。下一批固定为 PX1B 权威文档与 Manifest Schema 单一来源。

**PX1B 权威 Manifest Schema 与防漂移门禁已完成。** `schemas/plugin-manifest.schema.json` 以 Draft 2020-12 固化顶层字段、三种运行时、32 项能力、命令/参数、窗口、生命周期、本地化和 Web 原生后台；`PluginManifest` 补齐此前被静默丢弃的 `description`。新增测试通过反射和枚举序列化规则对账 Schema、C# 合同与宿主能力白名单，并要求 25 个正式 Manifest 和 4 个模板同时通过宿主读取。标准 Draft 2020-12 验证器实测 29 / 29 通过；全量自动化 938 / 938，Release 构建 0 警告、0 错误。下一批固定为 PX2A 独立插件验证入口与生产规则复用。

**PX2A 独立插件验证入口与生产规则复用已完成。** 新增 `LongBetterWindows.PluginValidator` 和 `validate-plugin.ps1`，目录模式复用 `ManifestReader` 与生产兼容性/权限规则，`.lpak` 模式直接复用 `PluginPackageValidator`；两种模式都输出结构化 JSON 和稳定退出码。生产验证器现在统一要求主入口、Web 原生后台 DLL 和全部本地化资源存在且不越出插件根目录。Base64 目录、临时包和缺失入口失败路径真实 CLI 冒烟通过；定向回归 14 / 14、全量自动化 942 / 942，Release 构建 0 警告、0 错误。下一批固定为 PX2B 打包前强制验证与确定性文件清单。

**LPWP 1.0 PR-A/PR-B 代码侧连续推进中。** PR-A 已把 API/SDK 版本提升到 `1.1.0`，落地 Widget Manifest DTO、Schema、语义校验、包内资源校验和文档审计；PR-B 当前完成 Web Bridge 能力协商与基础 Widget 方法：`host.getInfo` 返回宿主绑定的协议版本、API 版本、宿主身份、surface、插件/Widget/实例身份、features 和 limits，Widget 方法只在 Widget 上下文中可用，并补齐实例状态读写、ready、settings/invalidate/badge 响应、标准事件信封序列化、注入脚本 `CustomEvent` 派发、1 MiB Bridge 消息入口限制、虚拟 HTTPS 来源、WebView2 子资源来源阻断、HTML 文档响应头级 CSP、基础 CSP meta 注入、Widget 生命周期协调器、实例状态持久化、独立 `WebWidgetSurfaceSession`、Widget 专属入口加载、网格/像素/DPI 布局与可见性输入、按清单隐藏策略延迟暂停/恢复，以及专用 WPF `WebWidgetSurfaceHost` 对真实 `ActualWidth/ActualHeight`、Per-Monitor DPI、网格跨度、加载/卸载和可见性的接线；产品目录/布局事务核心从插件注册表投影 Widget 目录，以 schema/revision 文档原子持久化最多 256 个实例，并执行单/多实例、尺寸边界、24 列网格、碰撞、自动排位、移动缩放、删除、恢复和失效清理。用户可见的 `widgets:root` 工作区现已接通管理入口与全局 Launcher，提供响应式目录、24 列实例工作台、添加/移除/移动/缩放、空态、状态播报和中英文资源；布局变更只更新既有 `WebWidgetSurfaceHost`，不会重建 WebView。`samples/LongWidgetReference` 现提供单实例“专注脉搏”和多实例“灵感计数”，覆盖受信身份、ready、实例状态、Badge、刷新请求与生命周期事件；根目录脚本通过生产验证生成带文件总账的确定性 unsigned `.lpak`，GitHub CI 同步上传参考包制品，Web SDK Mock Host 合同测试直接执行其控制器。当前仍未完成 Long Grid 跨进程接入、发布者签名参考包和真实桌面端到端验收；这些继续作为后续批次收口，不改变当前发布人工验收阻断状态。

## 2026-07-28 管理中心交互架构审计

用户以本机 uTools 管理中心真实界面复核后，确认 Long 当时最主要的交互偏差不是单页视觉，而是管理中心、市场、插件设置和插件运行尚未形成统一模块工作区。该审计发现的字符串页面路由、`EmbeddedPluginSurface` 整块切换和插件会话所有权问题已在批次 4 至 6B 完成迁移。

后续开发方向调整为“Launcher Mode + Workspace Mode”双呈现、同导航会话模型。Command Palette 继续专注快速执行，管理中心继续专注管理，但二者共享模块标识、导航意图、插件会话、搜索上下文和返回位置。管理中心、市场、插件设置、插件运行、工作流、设置和诊断都作为工作区模块，不再继续新增互相割裂的页面或宿主窗口。

详细截图拆解、官方行为证据、当前源码差距、目标契约、六阶段迁移计划和验收门禁见 [`uTools管理中心交互审计与统一工作区开发计划_2026-07-28.md`](uTools管理中心交互审计与统一工作区开发计划_2026-07-28.md)。Launcher、模块条、作用域搜索、插件目录、市场、插件设置、分离窗口、`Esc` 层级和焦点恢复的逐项规范见 [`统一工作区交互细节审计与验收规范_2026-07-28.md`](统一工作区交互细节审计与验收规范_2026-07-28.md)。

当前立即进入阶段 0：先实现无 WPF 依赖的模块键、导航状态、会话协调器、作用域搜索和分离契约，并通过旧路由适配器渐进迁移；阶段 0 至阶段 2 完成前暂停新增独立管理页面和独立插件宿主窗口。

当前代码对齐已经完成：不新增 Core/Contracts 程序集，领域状态继续放在 `LongBetterWindows.Host/Interaction`，WPF Shell 放在 `Views`；复用现有 `SearchCoordinator`、`PluginRegistry`、`PluginRuntimeCoordinator`、`MarketplaceSessionCoordinator` 和 `PluginTaskbarIdentity`。详细类映射、新增文件、批次 0A 至批次 6、测试文件和每批完成条件已写入统一工作区开发计划第 11 至 17 节。

**批次 0A 导航领域模型已完成。** 已新增 `WorkspaceModels.cs`、`WorkspaceSessionCoordinator.cs` 和 `WorkspaceSessionCoordinatorTests.cs`，建立无 WPF 依赖的规范化模块键、只读状态快照、同键去重激活、根/不可关闭模块保护、MRU 关闭回退、失败原子性和失效模块批量清理。定向测试 9 / 9、全量自动化 781 / 781 通过，Release 构建 0 警告、0 错误；没有修改 XAML 或当前用户可见行为。

**批次 0B `Esc`、搜索与返回意图已完成。** `WorkspaceEscapeRouter` 固定单层优先级，`WorkspaceSearchSession` 完成模块查询隔离、请求取消、过期抑制和释放竞态处理，`LauncherReturnIntent` 完成一次性返回状态、失效原窗口回退和敏感查询/上下文/路径/图片/句柄的非持久化约束。定向测试 18 / 18、全量自动化 799 / 799 通过，Release 构建 0 警告、0 错误；仍未接入 XAML 或改变当前用户可见行为。

**批次 0C 工作区导航接线已完成。** `ServicesInitializer` 已注册唯一协调器和模块解析器，`OpenWorkspaceModule` 通过 Command Palette 与 SuperPanel 的统一执行链进入宿主；七类地址经过白名单、字符集和动态资源存在性验证后，才由 `MainWindow` 适配现有管理中心、市场、设置、诊断、开发者、工作流和插件设置页面。重复地址只激活已有模块，页面适配失败、取消或异常会回滚导航状态；延迟实例化插件在显式打开设置时只完成实例激活，不启动插件生命周期。新增测试 24 / 24，合并导航回归定向测试 38 / 38、全量自动化 823 / 823 通过，Release 构建 0 警告、0 错误。

**批次 1A Workspace Shell 骨架已完成。** `WorkspaceShellControl` 已成为主窗口正式内容宿主，顶部模块条直接投影协调器；ToolCenter 八页通过临时模块目录统一接入，旧侧栏、`Ctrl+Tab`、质量入口与搜索入口不再维护平行导航。活动模块点击无副作用，关闭模块按 MRU 回退并恢复模块条焦点，各页滚动偏移独立保存，延迟页面仍保持首次访问创建。工作区定向测试 52 / 52、全量自动化 828 / 828 通过，Release 构建 0 警告、0 错误；三组亮暗主题、中英文和最小窗口真实渲染无重叠。

下一代码批次固定为 **1B 作用域搜索与焦点**：把 `WorkspaceSearchSession`、`WorkspaceEscapeRouter` 和 WPF 焦点书签接入可见 Shell，实现 `Ctrl+K`、模块查询隔离、请求取消、最近焦点恢复与单层 `Esc`；1B 只适配现有管理、市场和插件目录搜索，不重写业务事务。

## 发布状态

**当前不得生成正式版。** 历史 `v1.11.0-rc.2` 保持不可变以供追溯；修复后源码已提升为 `v1.11.0-rc.3`。统一工作区、插件平台、LPWP Long助手侧实现和代码侧发布门禁已经完成，机器侧可进入人工验收。25 个插件正向回执、任务栏、物理 DPI、辅助功能、管理员 WPR、下载与干净 Windows、生产 Marketplace、Long Grid E2E、Widget 桌面行为及签名参考包仍需真实执行；全部关闭前不得创建正式稳定 Release。

RC3 新增 `plan-next-plugin-validation.ps1` 人工验收规划器：它按低、中、高、关键风险输出下一项，并强制校验
Release Manifest、冻结提交、自包含 ZIP 哈希以及解压 EXE 与 ZIP 内 EXE 哈希一致；候选之后除插件批准回执外
出现任何跟踪变化都会拒绝继续。规划器只提供受测对象和命令模板，不采集证据、不生成批准，也不改变当前
`0 / 25` 人工回执状态。

随后补齐 `start-plugin-validation.ps1` 一键会话入口：它复用规划器的全部候选校验，拒绝与其他运行中的
Long助手宿主混用，在 `artifacts/quality/` 创建独占证据目录及 `validation-session.json` 后启动精确冻结 EXE。
会话记录保持 `pending_human_observation`，不截图、不操作界面、不生成批准；重复目录和不同候选均按失败处理。
`-PrepareOnly` 会话可由同一入口精确恢复，恢复前复核全部身份并原子切换为 `launching`；其他既有目录仍拒绝。
入口现通过独立的正常启动参数 `--open-plugin` 直接进入待验插件，复用产品插件打开服务，不再要求操作人手工搜索，
也不借用 `--quality-open-plugin-runtime` 的质量探针路径；启动参数同时写入会话文件供后续回执追溯。

新增 `complete-plugin-validation.ps1` 收束已启动会话：审核人逐份检查截图/录像后，只需输入姓名、真实结论、
会话目录内证据和 `-ConfirmPassed`；脚本自动绑定当前候选、插件、检查、冻结 EXE 和会话文件，再调用不可覆盖
批准器。输出仍是待审查、待单独提交的回执，不会自动宣布人工通过。

`v1.9.0-rc.1` 作为历史 unsigned 候选保持不可变；其后的命令参数 Schema、Sparse Package 和国际化功能已进入 `master`。`v1.10.0` 已从完成工程收口的主线冻结，并在独立干净工作树重新构建全新 Manifest、哈希和证据，没有复用历史 RC。正式发布仍需完成：

1. 真实 Registry/CDN、公钥和发布凭据的受控环境演练。
2. unsigned 包的真实下载/SmartScreen 观察、干净用户环境以及物理 DPI/可访问性人工签核。

2026-07-26 已从干净提交 `f3a998e6d852889a105f599f0b609ec160ec4143` 生成 v1.10.0 unsigned 工程候选。双包均通过 25 个插件、25 个 Manifest、25 个唯一插件 ID、42 条命令、真实 Base64 命令和 WebView2 零新增残留校验；Manifest 记录 `source_dirty=false` 与 `release_eligible=true`。精确哈希、已完成门禁和跨电脑交接见 [`v1.10开发审计与发布交接_2026-07-26.md`](v1.10开发审计与发布交接_2026-07-26.md)。

同日已发布 [`v1.10.0-rc.1` unsigned prerelease](https://github.com/Longyuyeee/Long_BetterWindows/releases/tag/v1.10.0-rc.1)。标签解析到冻结提交 `f3a998e6d852889a105f599f0b609ec160ec4143`；自包含包、框架依赖包、Manifest 和 `SHA256SUMS.txt` 四个远端资产的字节数与 GitHub SHA-256 digest 均和本地候选一致。该 RC 只用于外部验收，未完成的人工门禁状态不因上传而改变。

真实 250% DPI 连续采集发现 RC1 物理证据脚本对 WebView2 冷启动使用固定 30 秒预算，独立视图约 10 秒可正常出图，但连续采集会被误判超时。PR #9 将预算改为默认 90 秒并允许 30–180 秒配置，随后从干净提交 `54baa08d683e1d88315d69ea7a310a7524aabe0c` 重建并发布 [`v1.10.0-rc.2`](https://github.com/Longyuyeee/Long_BetterWindows/releases/tag/v1.10.0-rc.2)。RC2 四个远端资产与本地 SHA-256、字节数一致，并在真实 250%（240 DPI）显示器完成亮暗主题、四类视图共 8 / 8 自动采集；人工视觉签核仍为 `pending`。RC2 取代 RC1 进入后续外部验收，RC1 保持不可变。

2026-07-22 发布决策调整为公开 `unsigned` 通道：代码签名作为未来可选增强，不再阻塞 v1.9。Manifest 必须如实保留 `signed=false`、`publisher_identity=unverified` 和安全提示；只有干净源码及全部自动门禁通过时才能写入 `release_eligible=true`。`v1.9.0-rc.1` Prerelease 已发布，标签精确指向候选提交 `d3cd956`，四个远端资产的 GitHub SHA-256 digest 与本地一致。自包含包已通过真实浏览器下载，80,569,737 字节和 SHA-256 与 Manifest 一致，并保留 Windows `Zone.Identifier` 的互联网区域 `ZoneId=3`；脱敏证据位于 `artifacts/quality/release-download-v1.9.0-rc.1/download-evidence.json`。下载门禁已形成不可变的采集、独立批准、汇总验证三段式，批准文件必须锁定原证据哈希并要求操作人与审核人不同。当前未生成正式批准文件，因此 SmartScreen、杀毒软件或首次启动仍未人工签核。默认生产令牌变量仍未配置，候选内 Marketplace Registry 仍是本地包配置且发布者信任库为空；RC 完成人工门禁后再决定正式 `v1.9.0`。

## 设计对齐审计

2026-07-23 再次对照最初产品愿景、插件通信设计、UI/UX 规范和交互平台路线完成主线审计。当前实现继续遵循 Windows 原生、本地优先、插件能力化、统一入口、上下文匹配、契约优先和可信分发等核心边界，产品方向保持一致。组合动作、市场事务和 Web 生命周期均复用宿主能力与审查边界，没有形成绕过插件命令契约的第二套执行系统。

当前偏差集中在工程收口，而不是产品方向：

1. `ClipboardHistory`、`DevToolkit`、`FileOrganizer`、`HardwareMonitor`、`PortManager` 随 v1.8 主线保留时没有声明统一命令、窗口和生命周期契约；该缺口已在首批收口中补齐，源码与新候选包基线均为 25 插件、42 条命令。旧候选包记录的 37 条命令仅作为合并时历史证据保留。
2. 新 `MarketplaceControl` 已成为唯一正式市场入口；旧 `MarketPanel` 及旧下载、安装、更新服务已在确认无运行时引用和迁移责任后移除，并由架构测试防止双实现回流。
3. `PerformancePanel` 已迁移到 Long Design System，并通过独立刷新协调器接入 ToolCenter“平台诊断”；`CapabilityDetailPanel` 已迁移为数据模板视图，可从每个插件卡片查看权限和本地调用记录。
4. 宿主大型类正在增量拆分：性能刷新、插件管理、启动参数、质量运行时和插件启动协调均已形成独立模块，`App.xaml.cs` 从 672 行降至约 430 行；市场运行时、展示投影与会话协调已拆分，`MarketplaceControl` 不再保存安装/卸载待确认状态，并通过协调器处理加载取消、操作防重入和结果生命周期；超级面板搜索、动作、分组、拖拽、键盘和窗口生命周期已形成独立模块。组编辑会话、二级动作菜单以及上下文摘要/动作结果视图投影均已迁出窗口；`SuperPanelWindow.xaml.cs` 当前 501 行，只保留 Popup/ContextMenu 控件应用和事件转接。插件来源发现、目录运行时装载、单文件脚本生命周期与热重载监控均已拆分，`PluginScanner.cs` 从 731 行降至 286 行。Web 插件运行与呈现链路已完成协议、参数、宿主分发、WebView 生命周期和呈现协调分层，`WebPluginRuntime.cs` 与 `WebPluginAdapter.cs` 分别降至 111 行和 114 行。

## 2026-07-20 审计结论与开发决策

本次审计覆盖 `master` 与 `origin/master` 同步状态、累计源码差异、宿主主要类规模、自动化测试、视觉证据以及最初 UI/UX 设计目标。结论如下：

1. **方向对齐**：统一命令、上下文匹配、超级面板、插件生命周期和可信市场已经构成 Windows 效率中枢，不需要在 v1.9 发布前新增更多独立工具。
2. **架构进度健康**：启动、插件装载、Web 运行时、性能刷新和超级面板的状态/行为已形成独立模块；旧市场双实现已移除。当前拆分应以事务边界和可测试行为为依据，不以文件行数归零为目标。
3. **市场交互风险已收口**：新增 `MarketplaceSessionCoordinator`，旧目录加载会被新请求取消，重复下载/预览/确认被拒绝；失败保留有效确认态，成功后清理状态并刷新列表。`MarketplaceControl` 只消费协调结果并维护 WPF 展示与文件选择器。
4. **最高发布风险位于外部环境**：真实 Registry/CDN、正式公钥与发布凭据、unsigned 包真实下载提示、干净用户环境、物理 DPI 和可访问性无法仅靠源码测试替代，必须作为独立发布门禁留在 v1.9 范围内。Windows 代码签名已降为可选增强。
5. **当前证据可信**：审计期间发现上下文 Provider 与整套测试共享线程池时，慢同步实现可能令快速 Provider 在超时前无法获得调度；现已改为独立长任务调度，超时仍从提交时开始，并完成 5 轮目标压力复测。Release 构建为 0 警告、0 错误，298 / 298 测试通过。基于干净提交 `d3cd956` 的新候选包记录 `distribution_channel=unsigned`、`publisher_identity=unverified`、`signed=false`、`release_eligible=true`，并包含双包 25 / 25 插件、42 条命令、真实命令退出码 0 和 WebView2 零新增残留；双包实际 SHA-256 与 Manifest、SHA256SUMS 完全一致，ZIP 内各有 25 个标准 `/` 路径 Manifest 且无反斜杠条目。真实浏览器下载证据进一步锁定自包含包哈希、字节数、HTTPS 来源主机和 `ZoneId=3`，且不记录 GitHub 临时 URL 查询参数。市场协调器真实隔离事务再次覆盖签名安装、拒绝、升级、回退、卸载和重启恢复；本地市场发布演练进一步覆盖一次性 RSA 签名、基线部署、候选预演、覆盖与回滚，并确认回滚后旧 Registry 所引用包仍存在、私钥已删除；平台诊断与插件管理截图均有渲染元数据。

## 远端分支审计

2026-07-24 已重新抓取、比较并整合全部远端功能分支：

1. `agent/development-audit-20260724` 唯一领先提交 `df841c8` 已快进并入 `master`，带入命令参数 Schema、Win11 Sparse Package 和宿主国际化里程碑。
2. `agent/v1.9-release-hardening` 与 `agent/workflow-audit-hardening` 无独有提交，均已被主线完整包含。
3. `agent/release-candidate-handoff` 的 `6aef0cb` 从历史主线分叉；功能整合基线 `df841c8` 相对它为 110 个独有提交对 1 个旧交接提交。该提交的大包内容已由后续主线按模块演进覆盖，不再合并，避免旧宿主结构和历史发布口径回流。
4. 唯一开发主线仍为 `master`。四个 `agent/*` 分支只保留为远端历史引用，不作为后续开发起点；确认不再需要历史对照后可在 GitHub 删除。

开发顺序已经确定：

1. [x] **P0 市场交互收口**：市场会话/操作协调器已覆盖加载取消、重复点击、确认态清理、成功刷新和失败保留，并通过纯逻辑单元测试、架构门禁与真实隔离事务。
2. [x] **P0 v1.9 本地发布门禁**：已从干净提交 `d3cd956` 重新生成 `unsigned` 双候选包；两种包均通过 25 个唯一插件、42 条命令、真实 Base64 命令、宿主退出、标准 ZIP 路径和 WebView2 差集验证，SHA-256 与机器可读 Manifest 已固定在本地候选目录。
3. **P0 v1.9 外部发布门禁**：本地签名、部署、覆盖与回滚全链演练已完成；生产演练器已改为在写入线上 Registry 前强制执行候选 Dry Run、Registry 最后提交检查和现网公开基线验收，并核对实际部署与预演 Release ID。干净 Windows 环境和真实下载现均已建立采集、独立人工批准和哈希汇总三段式门禁，并要求证据显式绑定 `unsigned` 或 `signed` 通道。统一决策器进一步绑定 Release Manifest、下载、干净环境、物理 DPI、可访问性和完整 HTTPS Registry 演练，拒绝包哈希错配、预检冒充完整演练及覆盖既有决策。Windows Authenticode 工具链保留为可选增强。`d3cd956` unsigned 候选已经重建并通过独立复核；真实浏览器下载、哈希和互联网来源标记已自动验证。下一步由人工执行 SmartScreen、杀毒软件、首次启动和完整生命周期观察，再由不同审核人批准，并使用真实 Registry/CDN 完成演练。
4. **P1 体验签核**：当前主线桌面 UI 已重新完成键盘执行、菜单、窗口交接、Web 插件分离返回、市场确认取消和三种可访问性模式冒烟；核心 Window/Edit/List/Button 的可访问名称与类型均已由 UI Automation 读取验证，动态状态已补充 Live Region。真实辅助功能和物理 DPI 均已建立采集、人工批准、汇总三段式门禁，采集要求干净受跟踪源码并重建指定提交，汇总拒绝混用不同提交的证据；当前机器处于普通主题、动画开启且未运行读屏器，因此不伪造人工签核。物理 100%/125%/150%/200% DPI、真实 Windows 设置和实际屏幕阅读器朗读仍需人工设备签核。
5. **P1 v1.9 冻结发布**：固定版本、哈希、命令与插件清单，冻结源码并发布；不把外部凭据写入仓库。
6. **v1.9 之后**：组合动作编排首轮闭环已经完成，包括安全持久化、Tool Center 编辑、结构化输入与参数、外部导入审查、权限批准、顺序执行、取消、逆序补偿、脱敏报告和工作流导出。工作流现采用 Schema v3，v2 以同来源、空绑定语义迁移；规范化定义哈希与授权指纹均包含绑定结构。`PluginCommandResult` 已增加防御性复制的 `Text` / `Path` 类型化输出；执行器按成功步骤保存隔离快照，解析文本、路径和参数绑定，允许补偿引用自身主命令输出，并统一限制 64 个输出、64 字符键和 65,536 字符值。Manifest 命令可声明输出键、类型和说明，读取、预检与执行均校验声明；文件夹备注助手返回 `selected-folder` Path。Tool Center 基于执行位置和 Manifest 声明提供来源下拉与类型兼容目标，失效声明会保留并阻止保存。Web 命令桥使用请求 ID 等待页面异步结果，支持取消、30 秒超时、64 个待处理请求上限以及结果大小/类型校验。Base64、URL、JSON、时间戳和 UUID 共八条 Web 命令现返回真实 `result` Text。执行结果默认只显示不含值的输出元数据；本次权限确认显式批准后，成功工作流可显示、复制或清除未消费的终端输出。文件导出再执行一次独立批准，审查目标绝对路径、UTF-8 字节数与值 SHA-256；写入采用同目录临时文件、写穿透和无覆盖原子移动，并拒绝不存在的目录、重解析点路径、审查后值/目标变化及已有目标。执行报告 Schema v1 保持不变，不保存摘要、终端值或导出记录。详细契约见 `docs/组合动作编排协议.md`。

## 下一开发阶段

当前主线已完成全部功能分支整合。最近完成的代码基线为 640 / 640 自动化测试通过、Release 构建 0 警告 / 0 错误；按以下顺序继续推进：

1. **P0 外部发布门禁**：完成人工 SmartScreen/杀毒软件、干净 Windows、物理 DPI、真实辅助功能及 Registry/CDN 受控凭据演练；代码签名保持可选，不阻塞 unsigned 通道。
2. [x] **P1 组合动作稳定化（展示协调边界）**：运行审查、准备失败、执行结果、输出摘要、终端输出和报告时间线已提取到无 WPF 依赖的 `WorkflowExecutionPresentation`；`WorkflowEditorControl.xaml.cs` 只应用投影结果并保留对话框、剪贴板及控件状态职责。新增 4 项行为测试覆盖权限风险、失效批准、终端值和脱敏报告展示。
3. [x] **P1 统一入口集成**：新增托管工作流搜索 Provider，仅投影已保存、可读取、定义哈希稳定且针对当前插件目录预检有效的组合动作；命令中心和超级面板使用独立 `OpenWorkflowReview` 动作导航到现有批准界面，执行前再次加载和预检，不注册伪插件命令，也不直接执行。结果支持固定和最近使用排序，并明确显示步骤数、失败策略、插件数与批准提示。
4. [x] **P1 真实交互验收前三批**：命令中心和超级面板入口、安全取消、默认宽屏、`720 × 560` 紧凑布局以及长终端输出均已闭环。第三批将编辑配置区与审查/运行/结果区分离，避免进入审查时物化整套重型编辑模板；质量模式使用注册窗口消息驱动批准、确认、状态查询与清除，不遍历长内容 UIA 树，也不使用坐标点击。当前多显示器 250% 缩放环境已验证只读执行、3699 字符终端输出、限高滚动、内存清除和隔离报告，证据位于 `artifacts/quality/workflow-long-output-20260723/desktop-ui-smoke.json`。
5. [x] **P1 插件升级失效验收**：托管工作流搜索动作携带不可变状态指纹；打开审查前重新加载定义、按当前插件目录预检，并由运行会话比较工作流定义、插件版本、注册修订和能力计划。隔离桌面事务已验证 v1 审查打开后通过正式安装器升级 v2，旧审查被拒绝、零步骤执行、报告脱敏、事务目录与隔离根清理；证据位于 `artifacts/quality/workflow-plugin-upgrade-20260723/workflow-plugin-upgrade.json`。扫描器现负责释放和定点重载目录运行时，审查/执行期间的插件变化不再重建编辑器。
6. [x] **P1 文件导出拒绝验收**：稳定失败分类与确定性测试覆盖已有目标、缺失目录、重解析点、审查后目标变化、值变化、移动竞态、权限失败和取消。真实宿主进一步使用目录联接与拒绝写入 ACL 完成矩阵，验证 3699 字节成功导出、SHA-256、无覆盖、无残留、报告脱敏、ACL 恢复和联接移除；证据位于 `artifacts/quality/workflow-terminal-export-full-20260723/desktop-ui-smoke.json`。
7. [x] **P2 易用性当前批次**：组合动作复制、只读模板、命令参数预设、插件内容三态和旧 Web 插件主题迁移均已完成。`HardwareMonitor`、`PortManager` 与 `ClipboardHistory` 已接通正式宿主 API并移除模拟数据或私有主题；剪贴板历史新增原生事件桥、15 秒兜底同步、时间筛选、热键降级、串行持久化和失败回滚。桌面及最小 `620px` 宽度交互验证通过。
8. [x] **P2 Web 能力生命周期硬化**：共享剪贴板监听采用多消费者租约，首个消费者安装 Windows Hook、最后释放才移除；运行时重复启停幂等，停止失败可重试，插件卸载自动释放。`ClipboardTool`、`ClipboardHistory` 与 `QuickNotePlugin` 覆盖仓库全部 Web 本地写入点，均检查宿主响应、串行持久化并在失败时回滚。
9. [x] **P2 命令参数 Schema 基础**：G1–G5 全部完成。契约、统一验证、工作流预检/动态绑定、v2 审查指纹、类型化编辑器和声明式绑定键下拉已闭环；无 Schema 命令继续使用高级键值界面。UUID 成为首个真实内置样板，插件指南覆盖五种类型与安全边界。宽屏和 `720 × 560` 紧凑 Release 宿主均由 UI Automation 完成整数值修改、布尔切换和预设控件发现；同时修复参数子控件 DataContext 震荡造成的空白与高 CPU 重建。Release 构建 0 警告、0 错误，自动化 490 / 490 通过，证据位于 `artifacts/quality/workflow-argument-schema-g5-20260723-uia-root/desktop-ui-smoke.json`。用户自定义预设、条件分支、并行执行、云同步和后台无人值守继续延后，避免扩大协议。
10. [x] **可选 Win11 Sparse Package 一级右键菜单工程闭环**：原生命令、未签名构建、受控签名、每用户管理事务、Tool Center 双通道 UI，以及只读预检、采集、独立批准和汇总验证门禁均已落地。本机重新构建确认 x64、COM 导出和未签名 MSIX 打包通过。正式证书和干净 Windows 11 Explorer 证据仅阻断该可选集成通道，不阻断 unsigned 主产品或后续国际化开发。
11. [x] **宿主国际化工程收口**：工作台、命令中心、超级面板、搜索投影、动作反馈、插件窗口、工作流审批、Sparse Package 对话框和直接文件夹备注均已接入动态双语资源。英文运行时不再依赖中文标题识别工作流拒绝状态；中英文各 950 个业务键且集合一致。
12. [x] **G16 宿主长尾与结构化错误**：Manifest、安装器、市场、工作流、系统集成、搜索与面板长尾均已完成源码侧收口。真实外部错误摘要继续只进入日志，界面显示稳定本地化提示。
13. [x] **G17 原生插件语言与内置 UI**：25 / 25 个插件已声明双语资源。`MacroPlugin` 的活动 Overlay 与设置页支持原位切换，且语言生命周期不注册热键、不录制、不播放；`SamplePlugin` 已成为推荐式 SDK 双语样板。全量 Manifest、资源键和副作用门禁通过。

## 2026-07-24 收口批次

本轮从 `8f2de70` 建立 `agent/release-closure-20260724`，每批均在 Release 构建、全量测试和差异检查通过后独立推送：

1. `c463b61`：宿主插件窗口、工作流审批、Sparse Package 与搜索偏好反馈资源化。
2. `f6b2ef0`：MacroPlugin 语言生命周期和 Overlay / 设置投影收口。
3. `4e51808`：SamplePlugin 双语 SDK 样板收口，插件覆盖达到 25 / 25。
4. `2f2ea33`：宿主搜索、动作反馈、直接文件夹备注和插件加载错误收口。
5. `0ab171d`：真实 UI 审计发现并修复命令中心、超级面板英文外壳和分组长尾。

真实 WPF 矩阵已覆盖中英文、深浅主题、`1280 × 800` 宽屏、`720 × 560` 紧凑、高对比度、减少动画、命令中心与超级面板。源码侧 G17/G4 收口完成；正式候选仍必须在干净 Windows、物理 DPI 和读屏器环境完成独立人工签核。

最新完整审计、风险分级、GitHub 发布范围与后续完成定义见 `docs/开发审计与后续计划_2026-07-24.md`；`docs/开发审计与后续计划_2026-07-23.md` 和 `docs/开发审计与后续计划_2026-07-22.md` 作为上一恢复点保留。

`v1.9.0-rc.1` 标签仍固定在候选提交 `d3cd956`。其后的组合动作、命令参数 Schema、Sparse Package 和国际化代码仅存在于后续 `master`，未进入该 RC 包，也不改变其 Manifest、哈希或人工发布门禁。下一候选按 `v1.10.0` 从干净主线重新生成。

平台诊断工程截图已在暗色主题、1280×800 逻辑尺寸、144 DPI 渲染参数下通过，证据位于 `artifacts/quality/diagnostics-smoke-20260720/`。
插件管理模块化后的工程截图已使用相同尺寸与渲染参数通过，证据位于 `artifacts/quality/plugin-management-smoke-20260720/`。

## 2026-07-27 性能收口进展

- 命令中心和工作流按需启动插件时不再持久写入 `auto_start=true`；只有插件管理中的显式启用继续改变下次启动策略。
- QuickLaunch 首次开始菜单发现已移出 UI 线程；文件名搜索支持逐候选取消并限制扫描量，内容搜索限制候选数和单文件大小。
- Release 构建 0 警告、0 错误；定向测试 38 / 38、全量测试 662 / 662 通过。
- 最新真实空闲探针为工作集 206.4 MB、私有内存 172.8 MB；25 个插件运行时就绪约需 6.45 秒。该数据高于此前工作集最大 181.4 MB 的历史样本，因此性能问题仍列为 P0，不能发布新的 RC。
- 下一开发批次为自动启动设置来源迁移、非后台插件延迟实例化、重复性能门禁和 QuickLaunch 大目录 P50/P95 采样。

### 自动启动第二批

- 自动启动配置已增加来源审计；旧无来源配置保守保留，新的用户启停写入 `auto_start_source=user`。
- 10 个纯工具插件改为按需启动，干净环境自动启动项从 25 个降为 7 个。
- 修复 Web 插件增量构建可能把开发机 `config.json` 带入发布物的问题；当前 Release 输出为 25 个插件、0 个运行时配置文件。
- 插件运行时就绪从约 6.45 秒降至约 4.46 秒，改善约 31%；空闲工作集仍为 206.1 MB，因此 P0 下一步是运行时实例延迟创建。
- Release 构建 0 警告、0 错误，全量自动化 675 / 675 通过；按需 Base64 真实命令退出码 0，且未产生自动启动配置。

### 运行时延迟实例化

- 25 个插件在扫描阶段只注册 Manifest、本地化投影和 42 条命令；默认关闭插件在首次启用、打开或命令调用时才创建运行时实例。
- 并发首次启动只激活一次；语言切换、插件管理、热重载与卸载均已适配延迟实例生命周期。
- Base64 真实命令验证为“先注册、调用时激活”，退出码 0，且未写入自动启动配置。
- 3 次 3 秒空闲探针的插件就绪中位数为 4.105 秒；工作集中位数 209.8 MB、私有内存中位数 181.0 MB。另一次工作集为 196.2 MB，但重复样本尚未稳定低于发布阈值，性能 P0 保持未关闭。
- 下一步聚焦 WPF 宿主基础服务和 7 个必要常驻插件，逐项测量初始化耗时、工作集增量及停用后的资源释放，再评估哪些服务可进一步延迟。
- Release 构建 0 警告、0 错误，全量自动化 678 / 678 通过；Release 输出保持 25 个插件目录、42 条命令和 0 个 `config.json`。

### 宿主隐藏页面延迟加载

- 空插件与完整插件各 3 次对照证明插件仅增加约 3.3 MB 工作集，当前性能主因是宿主主窗口和管理页面。
- 插件管理改为首次进入页面时构造；系统页的注册表、启动项和 Sparse Package 检查，以及设置页的更新检查与手势投影均改为首次导航时执行。
- 插件扫描期的连续目录事件合并为 Dispatcher 空闲批处理，避免重复构造 25 份卡片数据。
- 完整插件概览页 3 次工作集平均 197.9 MB、中位数 195.9 MB、最大 202.4 MB；私有内存平均 174.1 MB。相较整改前平均工作集减少约 7.9 MB，但最大值仍未严格低于 200 MB，性能 P0 不关闭。
- 插件页 3 次平均工作集 214.5 MB、私有内存 246.3 MB，下一轮继续处理卡片页面成本和离页回收。
- ClipboardHistory 当前只在 Web 页面首次初始化后才真正监听剪贴板，与 Manifest 后台语义不一致；下一批需拆出不依赖 WebView2 的原生后台执行层。
- Release 构建 0 警告、0 错误，全量自动化 678 / 678 通过；概览、插件、系统和设置页命名导航退出码均为 0。

### 原生后台与插件页资源整改

- ClipboardHistory 已采用通用 `Web UI + Native Background` 结构：原生组件负责宿主启动后的剪贴板监听、历史持久化和 `Ctrl+Shift+V`，WebView 仅在打开页面时创建。隔离冒烟确认后台写入成功且空闲 WebView 子进程为 0。
- `StorageService` 已修复写锁内嵌套读锁导致的持久化失败，并在写盘失败时回滚内存状态；服务卸载会释放锁资源。
- 插件管理页离开后不再永久缓存：全局事件、Dispatcher 刷新、数据源和 ContentControl 引用均被释放，再次进入时按需重建。
- 卡片能力标签改为单摘要投影，高 DPI 列表卡片移除离屏阴影合成。5 次插件页采样的工作集平均由 209.8 MB 降至 206.4 MB、最大由 222.0 MB 降至 208.6 MB；私有内存平均由 258.7 MB 降至 209.8 MB。
- Release 构建 0 警告、0 错误，全量自动化 685 / 685 通过。工程截图位于 `artifacts/quality/plugin-page-reclamation-20260727/`。
- 性能 P0 仍未关闭：下一步建立插件页进入/离开弱引用回收探针，并处理 ClipboardHistory 后台与页面并发存储一致性；在重复工作集最大值稳定低于门槛前不生成新 RC。

### 页面回收与存储一致性

- 插件页质量探针已可重复执行真实进入、离开、完整 GC 和弱引用检查；Release 报告确认页面对象已回收，不再被 ToolCenter、全局事件或 Dispatcher 队列持有。
- `IStorageService` / `long.storage` 新增原子 `compareExchange`。陈旧快照返回 `data=false`，调用方重新读取并有限重试；文件提交失败仍回滚内存状态。
- ClipboardHistory 原生后台和 Web 页面均迁移到 CAS 更新，消除两条独立读改写队列之间的最后写入覆盖。16 路相同快照竞争测试确认只有一个写入者成功。
- 真实隔离验证覆盖后台剪贴板记录、页面命令、热键注销和 WebView2 释放；Release 构建 0 警告、0 错误，全量自动化 689 / 689 通过。
- 下一步继续拆分概览页 WPF 视觉层与基础服务成本，执行相同环境的 5 次 A/B；性能 P0 与新 RC 仍保持阻断。

### 概览页合成成本 A/B

- 默认概览页已改用局部轻量卡片样式，5 张首屏卡不再各自创建离屏阴影表面；全局 `LongCard` 和其他页面不受影响。
- 相同 Release、`1120×760`、9 秒采样条件下，5 轮私有内存均值由 173.7 MB 降至 149.6 MB，下降约 13.9%；CPU 时间均值下降约 3.7%。
- 工作集均值由 200.6 MB 波动至 203.1 MB，最大值 211.1 MB，因此只认定实际提交内存改善，性能 P0 和新 RC 仍未关闭。
- Release 构建 0 警告、0 错误，690 / 690 测试通过；视觉与原始样本证据位于 `artifacts/quality/overview-render-cost-20260727/`。
- 下一步先为服务初始化、主窗口构造、插件目录装载和运行时就绪建立分段质量测量，再只优化测量确认的主要成本，并保持宿主能力注册与插件首次调用契约。

### 启动分段质量探针

- 新增仅由 `--quality-startup-report` 启用的结构化启动时间轴，覆盖服务、主题、窗口构造/Loaded、事务恢复、包安装、插件扫描和运行时就绪；报告完成后应用自行退出。
- 5 轮基线确认服务初始化平均 89.6 ms、资源增量很小；主窗口构造到 Loaded 平均 1206.0 ms，是当前首启主要阶段，因此没有冒险修改宿主服务注册。
- ToolCenter 的 5 张概览卡与 16 张其他管理卡统一使用无阴影描边样式。整改后运行时就绪均值由 2427.5 ms 降至 2147.3 ms，中位数由 2084.9 ms 降至 1970.6 ms；工作集和私有内存基本持平。
- 两组按顺序运行，结果会受到文件缓存影响，当前只认定观察到启动改善，不宣称全部降幅都由样式调整造成，也不关闭 9 秒空闲内存 P0。
- Release 构建 0 警告、0 错误，691 / 691 测试通过；10 份原始报告、汇总及系统/设置页截图位于 `artifacts/quality/startup-segments-20260727/`。
- 下一步采用交错 A/B 并继续拆分首次布局/渲染和插件扫描子阶段，再决定是否延迟系统、开发者和设置视觉树。

### 同二进制交错性能复核

- 新增仅质量模式使用的 `--quality-management-card-shadows` 对照开关，并在报告中记录实际命中数；有效探针确认恢复旧阴影时命中 21 / 21 张卡。
- 同一 Release 二进制按平衡顺序交错运行 12 次。无阴影相对旧阴影的运行时就绪中位数下降约 15.1%，累计 CPU 中位数下降约 16.5%，私有内存均值下降 22.9 MB、约 13.4%。
- 工作集仍然高波动，最大值仅由 202.2 MB 变为 200.3 MB，严格 9 秒空闲门槛和性能 P0 不关闭。
- 首次布局分段确认主要差异在 WPF 视觉树；插件目录发现约 15 ms，不值得优化。Manifest 与插件注册受缓存和系统负载影响，当前不改扫描及自动启动语义。
- 旧阴影截图显示效果作用于卡片整个子树并造成文字重影；无阴影版本的文字、按钮和边界更清晰。Release 0 警告、0 错误，691 / 691 测试通过。
- 有效交错报告、截图和汇总位于 `artifacts/quality/startup-interleaved-final-20260727/`。下一步把系统、开发者和设置面板拆成首次导航创建的独立视图。

### 管理页面首次导航创建

- 开发者工作区已在 `4564db1` 拆为独立 `DeveloperPageControl`；工作台、设计系统、开发文档和关于信息 4 张卡不再参与默认启动视觉树。
- 本批把系统集成工作区拆为独立 `SystemIntegrationPageControl`。Explorer 备注列、Sparse Package、兼容右键菜单、开机自启和全局热键 5 张卡仅在首次进入页面时创建；语言事件和异步状态归页面所有，Tool Center 卸载时显式解除订阅。
- 系统页改用顶部对齐的响应式 Grid，`1440×900` 英文浅色双列与 `800×700` 中文深色单列均通过截图审阅。全局按钮禁用态透明度由 0.42 调整为 0.62，修复深色异步检查期间的文字对比不足。
- Sparse Package 操作在成功但未携带状态时会先退出 busy 状态再重新查询，避免原有保护条件跳过刷新。
- 默认启动管理卡计数由 17 张降为 12 张，确定延迟 5 张系统卡。4 轮顺序 A/B 的私有内存中位数由 152.0 MB 降至 150.8 MB，工作集由 198.85 MB 降至 197.65 MB；启动时间受本机负载影响上升，因此不声明耗时改善，也不关闭性能 P0。
- Release 构建 0 警告、0 错误，691 / 691 测试通过。原始启动报告、明暗截图和汇总位于 `artifacts/quality/system-page-lazy-20260727/`。
- 设置工作区已拆为独立 `SettingsPageControl`，语言、主题、减少动画、安全更新、鼠标手势和隐私清理 6 张卡仅在首次进入设置页时创建。更新服务、语言事件和异步状态由页面所有，离开 Tool Center 时显式解除订阅并释放服务。
- 设置页使用顶部对齐的响应式布局，`1440×900` 英文浅色与 `800×700` 中文深色均通过截图审阅；窄屏动作控件移至独立行，按钮、禁用态文字和长说明未出现低对比、重叠或横向溢出。
- 默认启动管理卡计数由 12 张降为 6 张，确定延迟 6 张设置卡。4 轮顺序 A/B 的主窗口加载中位数由 2370.85 ms 降至 1323.30 ms，插件运行时就绪由 3455.90 ms 降至 1944.95 ms；对应私有内存中位数分别下降 3.85 MB 和 4.80 MB，工作集分别下降 9.50 MB 和 10.85 MB。
- 开发者、系统集成和设置三个管理页的首次导航创建已经完成。顺序样本只作为本批回归证据，下一步重新执行平衡交错性能矩阵与 9 秒空闲内存门禁，确认改善可重复后再决定性能 P0 是否降级。
- Release 构建 0 警告、0 错误，691 / 691 测试通过。原始启动报告、明暗截图和汇总位于 `artifacts/quality/settings-page-lazy-20260727/`。

### 懒加载后性能门禁复验

- 同一 Release 二进制按 `A/B/B/A/B/A/A/B/A/B/B/A` 重新执行 12 轮交错矩阵。默认无阴影相对 6 张旧阴影对照，运行时就绪中位数下降约 16.9%，累计 CPU 中位数下降约 16.6%，私有内存中位数下降 27.3 MB；阴影优化继续有效。
- `1120×760`、运行时就绪后等待 9 秒的 5 次独立概览样本为 195.0、202.7、193.3、202.2、192.7 MB，最大值 202.7 MB，严格 `<200 MB` 门禁仍未稳定通过。
- 同条件插件页样本为 207.1、216.1、220.3、211.0、213.4 MB，5 / 5 超过门槛。插件列表改为紧凑操作行，每项只常驻一个操作按钮，打开、设置、权限与启停在点击后按需创建菜单；深色热键徽标改用语义前景色。
- 紧凑列表复测为 214.5、214.3、217.9、215.1、214.7 MB，没有工作集收益，说明主要瓶颈不在列表按钮数量。本次改动只计为插件页结构和主题对比度改善，性能 P0 与新 RC 阻断继续保持。
- 宽屏英文浅色和窄屏中文深色截图通过；原始启动报告、15 份空闲样本、截图和汇总位于 `artifacts/quality/lazy-pages-performance-gate-20260727/`。下一步为插件页进入链增加结构化分段，区分页面视觉树、插件投影与 WPF 提交内存后再优化。
- Release 构建 0 警告、0 错误，最终全量测试 691 / 691 通过。全量过程中本地文件搜索曾出现一次已记录的异步空结果，定向连续 5 / 5 及随后全量均通过，继续作为稳定性观察项保留。

### 插件页分段与注册刷新去抖

- 新增仅由 `--quality-plugin-page-performance-report` 启用的结构化报告，记录页面导航、构造、插件投影、Loaded、首个 ContextIdle、插件运行时就绪和 9 秒空闲；每个样本包含 CPU、工作集、私有内存、托管堆、GC 提交量、线程、项目数、已实现容器数和视觉节点数。
- 首份同二进制报告确认页面构造约 38 ms、初始投影约 19 ms，但插件注册期间发生 22 次完整投影。列表从 0 项反复重建到 25 项，ContextIdle 级合并无法跨注册间隔去重。
- `PluginsChanged` 刷新改为 150 ms 可取消去抖；页面释放时取消等待并断开事件，用户主动刷新、搜索和插件操作完成后的直接刷新语义不变。5 轮报告的完整投影次数降为 3、2、2、2、2，最终均为 25 项、10 个已实现容器、254 个视觉节点。
- 整改后 5 轮运行时就绪中位数为 3071.5 ms，9 秒工作集为 212.0、209.8、214.1、213.8、209.8 MB，平均 211.9 MB、最大 214.1 MB；相对上一批紧凑列表样本，工作集均值下降 3.4 MB、最大值下降 3.8 MB，私有内存均值下降约 7.7 MB。
- 工作集仍未通过严格 `<200 MB` 门禁。除首轮外，运行时就绪到 9 秒之间私有内存继续增加 32.6–36.5 MB、累计 CPU 增加约 1.8–2.9 秒，而容器和视觉节点不变；下一步对 7 个自动启动插件及宿主后台活动增加分插件归因，性能 P0 与新 RC 继续阻断。
- 深色 `1120×760` 截图确认 25 项最终投影、热键对比、滚动和操作入口正常；弱引用回收探针退出码 0、`plugin_page_collected=true`，确认去抖任务没有造成离页持有。原始报告、截图和汇总位于 `artifacts/quality/plugin-page-segments-20260727/`。

### 自动启动插件与宿主空闲归因

- 新增可重复传入的 `--quality-skip-auto-start-plugin <id>`，只抑制指定插件的自动启动，不影响 Manifest 注册、命令注册、插件列表投影或正常运行语义。报告同时记录抑制列表和实际 Running 插件 ID，避免把“请求停用”误当成“实际未运行”。
- 七个自动启动插件逐项成对排除后，没有任何一个插件同时稳定改善 CPU、工作集和私有内存；单项结果受运行顺序、文件缓存和本机负载影响明显，不支持把问题归因给某个插件。
- 全部七个插件停止自动启动的两次样本最终工作集为 209.3 MB、210.1 MB，完整基线为 204.1 MB；Running 数量从 7 降为 0 仍未降低工作集，自动启动插件不是当前内存门槛的主因。
- 探针增加运行时就绪后 1 秒、3 秒、9 秒检查点。托管堆和 GC 提交量在长空闲段保持稳定，工作集也会回落，现有证据不支持持续托管泄漏。
- 新增仅质量模式使用的 `--quality-hide-window-during-idle`。同一 Release 二进制、相同插件集合的可见/隐藏对照中，3–9 秒累计 CPU 分别只增加 46.9 ms 和 15.6 ms，两组都进入真正空闲；高 CPU 集中在启动后前三秒的插件投影、WPF 布局与 JIT 预热，而不是持续动画或渲染循环。
- 性能 P0 暂不关闭：插件页 9 秒工作集仍可能高于严格 `<200 MB` 门槛，且前三秒启动突发仍会造成主观卡顿。下一轮优先分解运行时就绪前后的 UI Dispatcher、首次容器实现和注册完成投影成本，再进行启动削峰；不再继续用逐插件停用作为主要优化方向。
- 原始 21 份报告与机器可读审计摘要位于 `artifacts/quality/auto-start-attribution-20260727/`。

### 注册批处理与 WPF 线程归因

- `PluginRegistry` 新增可嵌套的变更批处理事务。目录扫描仍逐项完成 Manifest、命令、语言、延迟实例和自动启动状态写入，但全局 `PluginsChanged` 在批次结束只发布一次一致快照；空批次不发布，嵌套批次只由最外层提交。
- 插件页探针修正了运行时完成与页面稳定混用的问题：`plugin_runtime_ready` 保留后台完成时刻，新增 `plugin_page_settled` 等待 150 ms 去抖和 ContextIdle 后确认 25 项投影；1/3/9 秒空闲从稳定页面开始计算。
- 三轮报告均从运行时就绪时 0 项稳定到 25 项，耗时 340.3–389.0 ms、CPU 187.5–250.0 ms；完整投影固定为初始与最终两次。注册批处理减少其他全局订阅者的重复工作，但相对上一批插件页去抖后的两次投影没有进一步下降，因此不宣称插件页渲染收益。
- 质量报告新增 WPF UI 原生线程 ID 与每个节点 CPU 最高的五个线程。异常可见样本在 3–9 秒增加 1703.2 ms CPU，其中 UI 线程增加 1046.8 ms、另一高占用线程增加 562.4 ms；稳定可见样本只增加 46.9 ms，隐藏样本只增加 78.1 ms。
- 结论进一步收窄：后台插件、文件监控和托管泄漏不是间歇性长尾主因，剩余活动位于可见 WPF Dispatcher/渲染链。当前源码未发现插件页无限动画，列表已使用 Recycling 虚拟化且卡片无阴影，不做无调用栈依据的样式删除。
- 当前机器没有 `dotnet-trace`、WPR/WPA。下一批应在具备 ETW 或 Visual Studio Profiler 的环境采集异常轮次 UI/渲染线程调用栈，再针对高占用方法整改；严格 `<200 MB`、性能 P0 和新 RC 阻断保持不变。
- 原始报告与机器可读汇总位于 `artifacts/quality/registry-batch-20260727/`。

### 托管调用栈、窗口消息与动画时钟排除

- 在系统临时目录安装 `dotnet-trace 9.0.661903`，使用 `dotnet-sampled-thread-time` 捕获两轮可见插件页；工具和缓存不进入仓库。
- 高 CPU 轮次在 3–9 秒增加 1812.5 ms。对应 UI 托管栈在分析区间约 5987.6 ms 停在 `Dispatcher.GetMessage`，实际 Dispatcher 回调约 81.3 ms，质量视觉树遍历约 33.2 ms；没有发现持续执行的业务方法、布局方法或插件回调。
- WPR 的 `CPU.Verbose.File` 内核采样被 Windows 以 `0xc5585011` 拒绝，原因是当前会话没有系统性能分析权限。仓库不提升应用权限，也不把缺失的原生调用栈伪装为已完成。
- 新增仅质量模式使用的主窗口消息检查点，挂接与移除 `HwndSourceHook` 均受探针生命周期约束。两轮可见样本在 3–9 秒主窗口消息增量均为 0，排除 `WM_PAINT`、`WM_TIMER`、鼠标和窗口位置消息风暴。
- 现有视觉树采样新增动画属性计数，覆盖 `UIElement.HasAnimatedProperties` 与动画中的 `RenderTransform`。最终样本 25 项、254 个视觉节点、动画属性计数为 0，排除插件页源码动画时钟。
- 当前结论：剩余间歇性 CPU 活动位于托管 Dispatcher 之外的原生 WPF 合成、D3D/DWM 或显示驱动链。下一步需在授权的诊断环境以管理员 WPR 同时采集 `CPU` 与 `DesktopComposition`，确认模块和原生栈后再修改；性能 P0 与新 RC 继续阻断。
- 结构化报告和脱敏汇总位于 `artifacts/quality/wpf-stack-20260727/`；大型临时 `.nettrace`、Speedscope 和失败的 ETL 不提交。

### 分离窗口任务栏身份闭环

- 修复 AppUserModelID 归一化碰撞：ID 现在包含按不区分大小写插件语义计算的 12 位 SHA-256 摘要，并限制在 128 字符内；`plugin-a` 与 `plugin_a` 不再合并，仅大小写不同的同一插件仍保持同一身份。
- Shell 属性写入失败会记录告警，质量路径可从真实 HWND 读回 `PKEY_AppUserModel_ID`。新增 `--quality-taskbar-identity-report`，正常启动不启用。
- 真实 Windows 探针创建两个无 Owner 分离窗口，期望/实际 AppUserModelID 一致且互异，图标 SHA-256 互异，两个窗口均为 `ShowInTaskbar=true`；报告 `passed=true`，位于 `artifacts/quality/taskbar-identity-20260727/taskbar-identity.json`。
- 任务栏身份的机器可验证契约已闭环；Explorer 最终视觉分组仍保留人工观察。性能原生栈、主题对比度、高副作用插件和新 RC 阻断不变。
- Release 构建 0 警告、0 错误，全量自动化 695 / 695 通过，任务栏身份探针退出码 0。

### 插件 UI 能力主题契约

- 正式 `IUICapability` 的 Confirm、Prompt、Select 已从固定暗色迁移到动态语义画刷和 Long 控件样式，并补充中英文按钮资源与 AutomationId。系统 `MessageBox` 不再绕过宿主主题。
- 自定义 HTML 窗口使用 `data-long-theme`、`color-scheme` 和语义 CSS 变量；包装页与完整 HTML 均在导航完成后应用当前主题，运行时订阅主题变化并在关闭时解除。
- 删除零生产引用的旧 `ToastNotification`、`SkeletonCard`；后者的无限呼吸动画不再留在可编译宿主中。
- 新增 `--quality-ui-service-theme-report`。同一真实 Prompt 窗口完成暗色到亮色原位切换，报告 `passed=true`；暗色/亮色最低对比度分别为 `4.73:1`、`5.51:1`，两张 PNG 无重叠、裁切或主题残留。证据位于 `artifacts/quality/ui-service-theme-20260727/`。
- 插件 UI 能力的自动主题门禁已闭环；全应用 hover、focus、禁用态仍需人工视觉验收，性能原生栈和高副作用插件正向流程继续阻断新 RC。
- Release 构建 0 警告、0 错误，全量自动化 696 / 696 通过，主题探针退出码 0。

### 代表性插件交互状态

- 五类原生插件共用的 `HotkeySettingsControl` 已移除固定蓝、灰、红、绿色画刷，输入框、应用按钮、标签、提示及状态改用动态语义资源和 Long 控件模板；hover、focus、pressed、disabled、高对比度与主题切换不再由插件设置页自行实现。
- DevToolkit 执行动作与辅助动作分别声明 `btn-primary`、`btn-secondary`；标签页和快捷卡片修正 Web UI Kit 注入后的样式优先级，并在减少动画模式关闭位移。
- 正式复合命令在真实 WebView2 完成英文暗色与中文亮色两轮 1792 × 1188 预览，退出码均为 0；主次按钮、标签页、输入区和结果区无重叠、裁切、同色文字或主题残留。证据位于 `artifacts/quality/dev-toolkit-theme-20260727/`。
- Release 构建 0 警告、0 错误，全量自动化 696 / 696 通过。五个原生插件设置窗口仍保留真实打开抽查；下一轮统一整改 ScriptCreationDialog 与其余宿主 `MessageBox`。

### 共享主题消息框

- 新增 `ThemedMessageDialog`，统一使用 `LongWindowChrome`、语义画刷、Long 按钮样式、中英文按钮资源和稳定 AutomationId；支持信息、成功、警告、危险四种语义。
- ScriptCreationDialog、超级面板、系统集成、工作流编辑器与调用参数编辑器的 17 处生产消息框已全部迁移，宿主生产源码中的 `MessageBox.Show` 扫描归零。
- 删除、卸载、覆盖草稿和受保护导出均使用明确风险语义；确认对话框默认聚焦“取消”，`Esc` 与关闭按钮也统一返回取消，降低误操作风险。
- 新增 `--quality-themed-message-dialog-report` 和发布阻断回归测试。真实窗口在同一进程内完成深浅主题切换，报告 `passed=true`、`runtime_theme_changed=true`；暗色/亮色最低对比度分别为 `4.73:1`、`5.51:1`。
- 深浅两张 PNG 已逐张检查，无同色文字、重叠、裁切或主题残留。证据位于 `artifacts/quality/themed-message-dialog-20260727/`。
- Release 构建 0 警告、0 错误，全量自动化 697 / 697 通过。

下一开发方向保持原审计优先级：先真实打开抽查五类原生插件设置窗口及共享消息框的长文本/中文场景；性能 P0 等待管理员 WPR 的 CPU 与 DesktopComposition 原生栈证据后再定点优化；随后继续逐插件审计尚未闭环的正向功能，不以视觉整改掩盖不可用能力。新 RC 仍被性能门槛和人工桌面验收阻断。

### 五类原生插件设置窗口真实验收

- 新增 `--quality-plugin-settings-report`，通过正式插件实例和生产设置窗口工厂依次打开 ColorPicker、FolderNote、Macro、Screenshot、WindowManager，不复制测试专用设置 UI。
- 首轮真实截图发现 Macro 第三组热键在固定 `520×420` 窗口内被裁切；设置窗口现统一使用 16px 内边距和语义滚动承载，Macro 可滚动到第三组，暗色滚动条不再显示系统白底。Macro 与 Screenshot 的重复外边距已移除。
- 热键冲突规则从“不同插件才冲突”修正为“除当前旧热键外，任何已注册目标都冲突”。`ChangeHotkeyAsync` 先注册新键，再撤销旧键；目标冲突、系统注册失败或旧键撤销失败时保留原注册。
- 真实 HWND 回归测试验证同插件冲突时两组旧键均保留，成功替换后才移除旧键；运行时实际 `Ctrl+Alt+F6 → Ctrl+Alt+F7` 冲突识别通过。
- 五个窗口共 8 组热键输入与应用按钮在深浅主题下全部可访问，主题均原位变化；暗色/亮色主按钮最低对比度为 `4.73:1`、`5.51:1`。证据位于 `artifacts/quality/plugin-settings-20260727/`。
- 共享消息框同步补测中文长文本和 `380px` 紧凑宽度，实际尺寸 `380×215`，深浅截图与布局门禁通过；滚动区也使用共享语义滚动条。
- Release 构建 0 警告、0 错误，全量自动化 698 / 698 通过，两个最终 GUI 探针退出码均为 0。

下一轮转入插件正向功能矩阵，优先验证设置页实际应用后的持久化、冲突恢复和重启注册，再逐项覆盖截图/取色剪贴板结果、FolderNote ADS 写入回读、窗口管理恢复及 Macro 录制停止/回放取消。性能原生栈与新 RC 阻断状态不变。

### 插件热键持久化与恢复

- 新增宿主托管的 `IPluginSettingsService`。服务在插件初始化上下文中绑定插件 ID，只能读写自身 `PluginEntry` 配置，不要求插件声明额外文件/存储能力，也不向插件暴露目录。
- `PluginEntry` 使用同目录临时文件加替换原子保存，失败时恢复内存值；配置文件只写显式用户覆盖键，不再把 Manifest 默认 `auto_start` 混入用户配置来源。
- ColorPicker、FolderNote、Macro、Screenshot、WindowManager 在初始化时读取配置，设置成功后写回对应键。Macro 的录制、单次播放、循环播放三组设置分别绑定真实回调，不再使用空操作。
- 共享热键设置流程现在为：注册新键、持久化、提交插件内状态；持久化失败时撤销新键并恢复旧注册。旧键原本未注册时会注销新键，不尝试把“命令中心”文本注册为热键。
- 新增 `--quality-plugin-settings-persistence-report`。在系统 Temp 的 25 插件隔离副本中，通过真实输入框和应用按钮完成 8 / 8 次应用、Windows 注册、JSON 写入、重新构造配置读取与恢复，全部通过。
- 最终隔离配置共 5 个文件，仅包含 8 个允许的热键键名，`unexpected_persisted_keys=[]`，没有 `auto_start` 污染；证据位于 `artifacts/quality/plugin-settings-persistence-20260727/report.json`。
- Release 构建 0 警告、0 错误，全量自动化 701 / 701 通过。

下一轮进入高副作用插件正向功能：先验证 Screenshot/ColorPicker 的真实图像与剪贴板结果，再验证 FolderNote ADS 写入回读和失败回滚，随后处理 WindowManager 状态恢复及 Macro 录制停止、播放取消与异常清理。性能原生栈与新 RC 阻断继续保持。

### 截图与取色物理屏幕链路

- Screenshot 删除插件内重复 GDI，实现统一收口到宿主 `IScreenCaptureService`；原生插件与 Web 能力不再各自维护坐标和资源释放逻辑。
- 全屏范围改为 Win32 物理虚拟屏幕；区域选择用 `GetCursorPos` 记录物理起止点，并在关闭遮罩、等待 Dispatcher 空闲后截图，修复混合 DPI 偏移和遮罩进入截图的问题。
- 宿主截图检查全部 GDI 句柄与调用结果，恢复旧位图对象后释放资源，输出位图冻结后再跨线程；区域越界和无效尺寸返回结构化失败。
- ColorPicker 使用新增的宿主 `ScreenColorSampler` 读取真实屏幕像素，浮窗位置复用多显示器转换帮助器；复制回调异常时通过 `finally` 停止捕获并关闭窗口。
- 当前 Windows 会话的真实全屏位图、物理像素采样和记录型剪贴板交付均通过；定向矩阵 8 / 8。测试没有覆盖用户系统剪贴板，也没有模拟桌面点击。
- Release 构建 0 警告、0 错误，全量自动化 709 / 709 通过。区域拖拽、点击取色、实际粘贴仍保留人工桌面验收，不把底层探针等同于完整交互验收。

下一开发批次进入 FolderNote ADS 写入、回读、覆盖及失败回滚，然后处理 WindowManager 操作前状态恢复和 Macro 录制停止、播放取消、异常 Hook/Overlay 清理。性能原生栈、新 RC 与其余人工门禁继续保持阻断。

### FolderNote ADS 事务与恢复

- 修正 x64 `INVALID_HANDLE_VALUE` 常量，失败的 `CreateFileW` 句柄不再被误当作有效句柄继续读写。
- ADS 服务现在先验证目标、流名和 1 MiB 内容上限；NTFS 写入检查完整字节、刷盘并回读校验，失败时恢复写入前快照。
- 权限、共享冲突和一般 I/O 错误不再静默写入 `long_note.json`。只有明确的非 NTFS 卷使用原子侧车；非默认目标按路径与流名生成独立摘要，避免同目录数据串写。
- 读取失败不再返回部分内容；FolderNote 插件和 Explorer 右键入口只把 `StreamNotFound` 当作空备注，其他读取错误会停止编辑并给出本地化提示。
- 回滚日志记录实际新旧存储路径和旧值存在状态，能恢复空流及 ADS/侧车迁移；日志写入原子化，记录失败与业务修改同回滚，部分回滚失败保留记录供重试。
- 隔离真实 NTFS 矩阵覆盖文件/目录、大于 4 KB 中文、覆盖/删除回滚、空流、共享冲突、缺失目标、超限内容、日志写失败恢复及失败记录重试，定向 14 / 14 通过。
- Release 构建 0 警告、0 错误，全量自动化 718 / 718 通过；测试只操作随机临时目录，没有修改用户文件夹备注。

下一开发批次进入 WindowManager 操作前状态捕获与失败恢复，然后处理 Macro 录制停止、播放取消及异常 Hook/Overlay 清理。性能原生栈、新 RC 与人工桌面门禁继续保持阻断。

### 2026-07-27 主线阶段审计与后续方向

- 审计起点为 `master` 提交 `8f20dc05fcab57fe903c66f8778763e715e21ca3`，审计时本地与 `origin/master` 差异为 `0 / 0`，工作区干净。远端 10 条 `agent/*` 分支中 9 条已是主线祖先；唯一不在主线祖先链上的 `agent/release-candidate-handoff` 只保留 1 个历史压缩提交，而主线相对它已有 194 个提交，不是更远的新功能分支，不应直接合并。
- 当前源码基线包含 25 个正式插件和 4 个插件模板，共声明 42 条命令。Release 构建复核为 0 警告、0 错误，全量自动化 718 / 718 通过。
- 与最初设计对齐结果：统一命令协议、上下文与搜索、核心 UI、插件生命周期、分离窗口、市场基础设施及插件迁移阶段已经完成；`v0.6-v0.8` 交互平台路线和 UI/UX 阶段 0-4 已落入主线。项目现处于“质量收口与发布恢复”阶段，不再以新增插件数量作为主要进度。
- 严重问题整改中，主题运行时切换、共享对话框对比度、插件设置与持久化、任务栏身份机器契约、Screenshot/ColorPicker 物理屏幕核心链路、FolderNote ADS 事务已形成自动化闭环。它们仍分别保留真实桌面点击、系统剪贴板、Explorer/HUD 和最终任务栏视觉人工验收，不能据此解除发布阻断。
- 性能调查已排除单个自动启动插件、持续托管 Dispatcher 回调、窗口消息风暴、动画时钟和明显托管泄漏。剩余间歇性 CPU 活动指向原生 WPF 合成、D3D/DWM 或显示驱动链；严格 `<200 MB` 工作集门槛仍可能失败。下一次性能代码修改必须以管理员 WPR 的 `CPU + DesktopComposition` 原生栈为依据。

后续开发按三批推进：

1. **WindowManager 事务化**：把前台窗口获取、位置/尺寸、显示状态和置顶状态捕获收口到可测试的宿主服务；检查每个 Win32 返回值；命令只在真实成功时返回成功；失败时恢复操作前状态，并补齐临时真实 HWND 的成功、无效句柄、部分失败和恢复矩阵。
2. **Macro 输入安全**：统一录制停止与单次/循环播放取消的等待语义；跟踪已按下的键和鼠标键并在取消/异常的 `finally` 中释放；覆盖 Hook 部分安装失败、Overlay 关闭、后台任务异常观察和重复启动/停止。
3. **质量与候选重建**：对账 25 插件正向功能和人工桌面矩阵，完成 Translate 离线、QuickLaunch 大目录、主题状态、任务栏分组及高副作用插件实机签核；性能门槛有证据通过后，才从干净 `master` 重建新的 unsigned RC、Manifest 与哈希。

因此，内部功能开发还剩两批高副作用插件整改和一批发布对账；外部并行阻塞项是管理员性能采样与人工桌面验收。历史 handoff 分支保留作追溯即可，后续开发、提交和发布基线继续统一放在 `master`。

### WindowManager 事务化与失败恢复

- 窗口布局和置顶操作已从插件中的直接 P/Invoke 收口到宿主 `IWindowInfoService`。插件只提交稳定的 `WindowLayout` 意图，并声明既有 `window.info` 能力；不存在新增兼容层或 uTools 插件兼容范围。
- 宿主在变更前捕获窗口位置、尺寸、显示状态和置顶状态。最小化或最大化窗口先规范化为可布局状态；布局使用目标显示器工作区，负坐标和奇数尺寸按实际剩余宽高计算。
- `SetWindowPlacement`、`SetWindowPos`、窗口样式读取及变更后校验均检查真实返回值。失败结果保留原始 Win32 错误；若已进入变更阶段则恢复原快照，恢复失败会在同一结构化结果中同时保留恢复错误。
- WindowManager 的 11 条有副作用命令不再固定返回成功。命令中心和热键都复用同一事务服务；失败 HUD 会区分“操作失败但已恢复”和“操作失败且恢复失败”。
- 可注入原生接口矩阵覆盖多显示器工作区、无前台窗口、最大化规范化、部分变更后失败、恢复成功、恢复失败和置顶切换。隔离临时真实 HWND 测试额外发现并修复了托管 `Native...` 方法名未映射实际 User32 入口的问题，避免编译通过但运行时 `EntryPointNotFoundException`。
- WindowManager 插件版本由 `2.1.0` 升至 `2.1.1`，本地市场目录和文档目录同步。定向事务矩阵 9 / 9 通过；Release 构建 0 警告、0 错误，全量自动化 727 / 727 通过。

审计结论：WindowManager 的自动化完成标准已经关闭，但仍保留真实桌面热键、最大化/最小化、多显示器和不可操作高权限窗口的人工正向验收；临时 HWND 自动化不能替代用户桌面交互签核。下一开发批次进入 Macro 录制停止、单次/循环播放取消、按下输入释放及异常 Hook/Overlay 清理。管理员 WPR、严格内存阈值、新 RC 和其余人工发布门禁继续阻断。

### Macro 可等待取消与异常输入清理

- Macro 的 Hook、光标和 `SendInput` 已收口到可注入 `IMacroNativeApi`；键盘回放由无法检查结果的 `keybd_event` 改为可验证返回数量和 Win32 错误的 `SendInput`。
- 单次与循环播放统一持有后台任务和取消源。`StopPlayAsync`、插件 `StopAsync` 和循环切换会等待真实任务退出，不再只发送取消信号后立即释放引擎或关闭 Overlay。
- 引擎在每次成功发送鼠标/键盘按下后登记待释放输入，正常抬起后移除。取消或异常的 `finally` 会补发抬起并最多重试 3 次；仍失败的输入保留在引擎中，插件停止和异步释放会继续清理且不会静默报告成功。
- Hook 部分安装失败会立即卸载已安装部分；卸载失败进入 `Faulted` 并保留句柄供下一次停止重试。只有 Hook 与待释放输入均清理完成，插件才进入 `Stopped`。
- 循环播放任务捕获并观察原生输入异常，通过 `PlaybackFailed` 写入日志和本地化 HUD；状态观察者自身异常不会中断引擎的 `finally` 清理。Overlay 关闭时会停止隐藏计时器和动画。
- Macro 版本由 `1.1.0` 升至 `1.1.1`，Manifest、双语资源和文档插件目录同步。故障注入覆盖鼠标/键盘按下后取消、抬起失败与重试、循环停止等待、输入失败观察、Hook 部分安装、Hook 卸载失败重试和观察者异常；定向门禁 10 / 10 通过。
- Release 构建 0 警告、0 错误，全量自动化 736 / 736 通过。全部新测试使用可注入假原生层，没有安装真实全局 Hook、移动用户鼠标或发送真实键盘输入。

审计结论：Macro 的源码与自动化安全整改项已经关闭，但仍需人工验证 F6/F7/F8 热键、录制 Overlay、单次/循环停止时机和真实目标应用输入结果。至此两批高副作用插件源码整改均完成；下一阶段进入 25 插件正向功能与人工桌面矩阵对账，再根据管理员 WPR、严格内存门槛和人工签核决定是否允许从干净主线重建新 unsigned RC。

### 25 插件正向功能矩阵与发布阻断

- 新增唯一机器矩阵 `docs/plugin-positive-function-matrix.json`，逐项覆盖源码 25 个正式插件、42 条命令、7 个原生运行时、18 个 Web 运行时、风险等级、自动证据和人工正向检查。
- 新增 `verify-plugin-positive-matrix.ps1`。校验器从源码一级插件目录重新读取 Manifest，不信任矩阵自报数量；插件/命令/运行时不一致、证据文件或符号失效、命令没有人工检查、重复 ID 和非法状态都会返回失败。
- 每个正式插件至少绑定一项现有 `contract`、`component` 或 `integration` 自动证据；绑定必须解析到实际测试文件和符号。自动证据只说明对应层级，不会自动提升为桌面正向通过。
- 25 项必需人工检查覆盖全部 42 条命令。有效 `passed` 必须来自矩阵内联原始证据，或来自绑定提交、Manifest、宿主与原始证据 SHA-256 的可移植批准回执；发布模式还会拒绝 `pending`、`blocked`、`failed` 以及已跟踪源码未提交状态。
- 当前机器结果：25 / 25 插件、42 / 42 命令、29 条自动证据绑定均有效，契约有效；人工通过 0、待处理 25、失败 0，因此 `release_eligible=false`。这准确保持新 RC 阻断。
- 人工执行顺序、隔离要求、证据回写格式和发布命令已写入 [`插件正向功能验收矩阵_2026-07-27.md`](插件正向功能验收矩阵_2026-07-27.md)。
- 当前矩阵及人工回执定向门禁 12 / 12 通过；Release 构建 0 警告、0 错误，全量自动化 772 / 772 通过。

下一开发批次优先把 11 个低风险插件的纯转换命令接入隔离真实命令执行器；当前 25 项人工检查不能由矩阵契约自身关闭。管理员 WPR、严格内存阈值、任务栏最终分组、物理桌面交互和新 RC 继续阻断。

### 11 个低风险插件隔离真实命令闭环

- 新增 `--quality-command-report`，真实启动命令会写入 Schema v1 结构化报告：完整命令键、插件/命令 ID、输入类型、输入长度与 SHA-256、成功状态、输出、耗时、退出码，以及命令期间实际触发的能力和宿主 API。报告不保存原始输入文本。
- 新增 [`low-risk-plugin-command-cases.json`](low-risk-plugin-command-cases.json) 和根目录 `verify-low-risk-plugin-commands.ps1`。每个案例使用新的宿主进程和临时插件根，根内只复制目标插件；完成后验证路径并删除临时根，不读取或修改用户插件配置。
- 机器矩阵覆盖全部 11 个低风险插件、14 条命令和 18 个案例，其中 14 个成功案例覆盖每条命令，4 个失败案例覆盖无效 Base64、JSON、时间戳和 URL 输入。
- Base64、JSON、URL 和时间戳检查确定性输出；密码检查 20 字符安全随机结果；UUID 按 Manifest 默认参数检查 10 个 UUID v4；Markdown、Regex 和 TextDiff 现在向宿主明确返回“命令已就绪”，不再依赖 `undefined` 被桥接层隐式当作成功。
- 除 HardwareMonitor 的 `performance.getSystemInfo/getCpuUsage/getMemoryInfo/getDiskInfo/getTopByMemory` 只读调用外，其他案例的 `api_method_calls` 均为空；没有剪贴板、文件、存储、进程修改、桌面移动或输入注入。
- 隔离真实执行 18 / 18 通过；Release 构建 0 警告、0 错误；全量自动化 743 / 743 通过；本地证据位于 `artifacts/quality/low-risk-plugin-commands-20260727/report.json`，证据目录仍按仓库策略不提交。

审计结论：11 个低风险插件已从“静态契约存在”提升为“真实 Release 宿主可执行、成功/失败可观察、宿主 API 副作用可审计”。这减少了功能不确定性，但不替代视觉、键盘、复制按钮、主题切换和窗口生命周期的人工签核，因此 25 项人工状态仍保持 `pending`，新 RC 仍被阻断。

下一批按风险继续推进：

1. 对 ClipboardHistory、ClipboardTool、DevToolkit、QuickNote、Translate 五个中风险插件建立隔离数据夹具，先覆盖不写系统状态的命令路径；Translate 同时区分在线成功、离线失败和恢复。
2. FileOrganizer、FileRenamer、FolderNote、PortManager 等高风险插件只在随机临时目录或本轮临时监听进程中执行，并为每次变更验证回滚和零残留。
3. ColorPicker、Screenshot、QuickLaunch、WindowManager、Macro 继续保留真实桌面人工验收；管理员 WPR、严格内存阈值、任务栏最终分组和物理桌面矩阵仍独立阻断候选重建。

### 5 个中风险插件隔离数据与网络闭环

- 新增显式 `--quality-command-fixture`。只有提供该参数的质量进程才安装夹具；Web 能力分发会在权限检查和调用审计后，把 `clipboard.*`、`storage.*`、`http.*` 路由到进程内状态，不接触用户剪贴板、真实插件存储或公网。
- 夹具支持剪贴板读写/监听租约、本地存储读写与 compare-exchange、HTTP GET 成功/失败和 URL 约束。命令报告附带夹具快照；聚合证据只保存剪贴板/存储值的长度与 SHA-256，不保存安全夹具原文。
- 新增 [`medium-risk-plugin-command-cases.json`](medium-risk-plugin-command-cases.json) 和根目录 `verify-medium-risk-plugin-commands.ps1`。正向总账绑定全部 5 个中风险插件和 5 条命令，防止新增或改名后遗漏执行。
- ClipboardHistory、ClipboardTool 和 QuickNote 现在等待初始化结果；存储读取失败会返回命令失败。ClipboardTool 隔离历史写入失败不再固定成功，内存投影仍由既有逻辑回滚。
- Translate 成功路径现在返回标准 `result` Text 输出，Manifest 和双语资源同步；在线/离线均使用固定 HTTP 夹具，不依赖 Google 服务实时状态。
- 最终真实 Release 宿主：5 / 5 插件、5 / 5 命令、9 / 9 案例通过，其中 5 个成功路径、4 个失败路径。低风险矩阵回归仍为 18 / 18。
- Release 构建 0 警告、0 错误；全量自动化 745 / 745；本地证据位于 `artifacts/quality/medium-risk-plugin-commands-20260727/report.json`，不提交运行产物。

审计结论：中风险命令的用户数据、存储和公网依赖已被确定性夹具隔离，初始化失败和网络失败能传回统一命令协议。人工矩阵仍需验证真实剪贴板按钮、历史监听生命周期、通知、真实网络恢复、视觉和键盘体验，因此发布资格继续为 false。

下一批进入高风险插件，但按副作用拆分：

1. 先在随机临时目录和本轮临时监听进程中验证 FileOrganizer、FileRenamer、FolderNote、PortManager，要求成功、拒绝、回滚和零残留。
2. QuickLaunch 先做隔离大目录发现、连续输入和取消，不自动启动真实第三方程序。
3. ColorPicker、Screenshot 以及关键级 WindowManager、Macro 继续依赖物理桌面人工验收；管理员 WPR、严格内存阈值、任务栏分组、DPI/辅助功能仍阻断新 RC。

### 4 个高风险插件临时事务闭环

- 启动命令新增可重复的 `--command-path` 路径上下文；命令报告只记录路径数量与 SHA-256，不落盘原始输入路径。单文件、文件集合、文件夹和 Explorer 选区会按 Manifest 接受类型解析，不兼容输入固定拒绝。
- FileOrganizer 收到文件夹命令后自动生成只读预览并返回 `planned-count`；不存在目录会返回失败。实际整理改为整批预检，任何确定性冲突都会在首次移动前拒绝；执行中异常会逆序恢复已移动文件并清理本轮空目录。
- FileRenamer 返回 `selected-count` 预览结果。确认执行后不再逐文件调用，而是通过新的 `shell.renameBatch` 一次提交宿主；宿主先验证全部源、文件名、目标冲突和重复项，再整批重命名，异常时逆序回滚。
- FolderNote 使用随机临时目录执行真实原生命令，存在目录成功返回 `selected-folder`，缺失目录明确失败；命令只读取空备注并打开 HUD，不自动写 ADS。既有真实 NTFS 14 / 14 事务矩阵继续负责写入、覆盖、删除和失败恢复。
- PortManager 会等待首次端口快照后再处理命令；合法数字查询返回标准 `query` 输出，`70000` 等越界端口明确失败。自动案例只查询矩阵 PowerShell 持有的随机 TCP 监听，不调用 `process.kill`。
- PortManager 的终止确认已改用 `process.killVerified`。端口快照携带进程启动身份，宿主在执行前复核 PID、名称和身份，进程变化时拒绝操作；后续优先检查 7 个高风险插件的真实桌面副作用与取消/回滚结果。
- FileOrganizer、FileRenamer、PortManager 版本分别升至 `1.0.1`、`1.1.1`、`1.0.1`；本地市场中的 FileRenamer 版本与事务说明同步。
- 新增 [`high-risk-plugin-transaction-cases.json`](high-risk-plugin-transaction-cases.json) 和根目录 `verify-high-risk-plugin-transactions.ps1`。4 / 4 插件、4 / 4 命令、7 / 7 成功/拒绝案例通过；每个随机工作区执行前后 SHA-256 指纹一致。
- 低风险 18 / 18、中风险 9 / 9 回归通过；Release 构建 0 警告、0 错误；全量自动化 750 / 750 通过。本地运行证据位于 `artifacts/quality/high-risk-plugin-transactions-20260727/report.json`，不提交运行产物。

审计结论：四个目标插件的路径上下文、只读命令结果、输入拒绝、文件批次预检/回滚和零残留已形成机器闭环。人工状态仍保持 `pending`，因为自动化没有点击最终确认、写入真实 Explorer 备注或终止进程。下一批按顺序处理 QuickLaunch 隔离大目录的发现、连续查询取消和无害目标启动边界；ColorPicker、Screenshot、WindowManager、Macro 继续保留物理桌面签核。

### QuickLaunch 大目录、取消与安全启动边界

- 窗口内不可测试的递归扫描已拆为生产 `QuickLaunchDiskSearchEngine`。文件搜索最多检查 5000 个候选，内容搜索最多 500 个且单文件不超过 1 MiB；上限跨全部根目录计算，不再每个根重新放大。
- 搜索枚举跳过系统项与重解析点，目录消失、权限变化或单文件读取失败不会破坏整次查询；取消令牌在根目录、候选文件和内容读取后持续检查。
- 查询新增单调代际。即使输入发生 `A → B → A`，首个 A 的后台结果也不能凭文本相同覆盖最新 A；窗口关闭会使所有未完成代际失效。
- 动态目标执行统一经过 `QuickLaunchTargetPolicy`。应用只允许真实开始菜单根中的现有 `.lnk`；普通文件必须存在且不是重解析点；链接只允许 HTTP(S)；未知分类、缺失目标和伪造路径固定拒绝。
- 普通文件搜索和执行同步屏蔽 `.exe`、`.cmd`、`.ps1`、`.msi`、`.lnk`、脚本等潜在可执行扩展。应用启动只能走受约束的开始菜单快捷方式；宿主打开或剪贴板写入失败会返回命令失败，不再固定成功。
- 新增根目录 `verify-quick-launch-isolation.ps1`。6 / 6 案例覆盖 1201 文件随机大目录发现、全局候选上限、取消、同文本过期请求、允许目标形态和伪造/缺失目标拒绝；工作区指纹验证搜索零写入，自动化没有启动任何程序。
- QuickLaunch 版本升至 `1.1.1`，源码 Manifest、运行时版本和文档插件目录同步。Release 构建 0 警告、0 错误；全量自动化 757 / 757；低风险 18 / 18、中风险 9 / 9、高风险文件/端口 7 / 7 回归通过。

审计结论：QuickLaunch 的大目录有界发现、连续输入取消、过期结果抑制和启动目标安全边界已形成机器闭环。人工状态仍保持 `pending`，因为自动化没有真实打开窗口、操作键盘选择或启动用户指定的无害应用。下一阶段不再扩插件功能，进入 ColorPicker/Screenshot Overlay 与系统剪贴板、WindowManager/Macro 物理桌面交互和管理员性能证据的发布收口。

### ColorPicker / Screenshot Overlay 取消与交付边界

- 审计确认原有物理坐标、遮罩关闭后截图和 GDI 资源释放链路仍然有效，但插件停止与异步交付之间缺少统一边界：晚到的像素或位图结果仍可能在 Overlay 已关闭、插件已停止后写入剪贴板。
- 新增宿主 `AsyncDeliveryBoundary`。载荷生产完成后必须再次检查所属生命周期令牌，只有仍有效时才能调用剪贴板交付；启动前取消不生产，生产期间取消不交付，正常路径只交付一次。
- ColorPicker 的采样循环改为持有独立取消源；窗口关闭会取消可等待延迟并退出循环，不再依赖布尔轮询自然结束。插件停止会先取消交付生命周期，再关闭窗口；晚到色值不会覆盖用户剪贴板。
- Screenshot 的全屏命令、区域选择和插件停止共享交付生命周期；命令取消继续向调用方传播，插件停止取消则静默终止晚到交付。全屏与区域截图均复用同一捕获、校验、剪贴板写入管线。
- ColorPicker 与 Screenshot 版本均升至 `1.1.1`，源码 Manifest、运行时投影、文档注册表和内置市场版本同步。
- 新增根目录 `verify-capture-delivery-isolation.ps1`，3 / 3 覆盖正常单次交付、启动前取消和生产期间取消。报告明确记录未触碰系统剪贴板、未生成指针输入；正向总账为两个插件绑定新增组件证据，人工状态保持 `pending`。
- 回归同时发现宿主重建 Web 插件目录后未显式恢复 `ClipboardHistory.Background.dll`，导致中风险矩阵依赖 MSBuild 并行顺序。宿主复制目标现会从已构建输出显式回填后台 DLL，连续宿主重建后中风险矩阵稳定为 9 / 9。
- Release 构建 0 警告、0 错误；全量自动化 760 / 760；低风险 18 / 18、中风险 9 / 9、高风险文件/端口 7 / 7、QuickLaunch 6 / 6、截图交付 3 / 3 全部通过。

审计结论：Overlay 关闭、插件停止与晚到剪贴板写入之间的源码不确定性已关闭，但自动化没有移动鼠标、覆盖系统剪贴板或模拟最终粘贴，因此真实取色点击、区域拖拽、Esc 取消和粘贴图像仍需用户在物理桌面签核。下一批按发布收口顺序处理任务栏最终分组与物理 DPI/辅助功能证据；WindowManager/Macro 真实交互和管理员 WPR 继续独立阻断新 RC。

### 25 插件任务栏身份与物理证据准备

- 原任务栏 HWND 探针只覆盖两个专用碰撞样本，能够证明属性写入机制，却不能证明 25 个正式插件的 ID、当前本地化标题和动态图标投影全部唯一。
- `--quality-taskbar-identity-report` 现从生产 `PluginRegistry` 读取 25 个正式插件，同时创建不抢焦点的真实分离窗口，从每个 HWND 读回 `PKEY_AppUserModel_ID`，并检查无 Owner、`ShowInTaskbar=true`、身份唯一和图标 SHA-256 唯一。
- 新增根目录 `verify-taskbar-identity.ps1`。正式报告要求跟踪工作区干净，绑定完整 Git 提交和宿主 EXE SHA-256；25 / 25 窗口、25 / 25 AppUserModelID、25 / 25 图标通过。
- 普通 DPI 非感知 PowerShell 预检曾被系统虚拟化为 100%；干净提交上的 PerMonitorV2 宿主实际读到当前显示器为 240 DPI（250%）。100% 捕获被物理门禁正确拒绝。250% 可作为额外兼容证据，但不能代替发布矩阵要求的 100%、125%、150%、200%。
- 首次 250% 正式捕获进一步发现插件视图命令没有提供 Manifest 要求的文本输入，宿主因此没有创建活动 WebView2。采集脚本现通过 `--command-text` 提交固定无敏感测试文本，确保插件截图验证真实表单视图，而非空窗口或失败路径。
- 修复输入后生成的首批 8 张截图在机器检查上通过，但视觉复核发现 PowerShell 进程参数链中的中文测试文本发生乱码；界面本身的中文标题和控件正常。该批保持 `pending` 且不批准，固定采集文本改为 ASCII，避免把参数编码噪声混入 DPI 视觉证据。
- ASCII 重拍又确认 `Start-Process -ArgumentList` 会把未加引号的空格参数拆分，插件只收到首词 `Long`。最终固定载荷改为无空格单令牌 `LongAssistantDpiReview`，使输入与编码结果可在截图中完整对账。
- Release 最终回归保持 0 警告、0 错误、760 / 760；桌面 UI 烟测覆盖命令面板、超级面板、工作流、分离插件、市场，以及 high-contrast、reduced-motion、combined 三种自动模式，全部通过。自动模式只证明语义与渲染路径，不替代真实 Windows 设置和屏幕阅读器批准。
- 任务栏机器契约至此扩展到全部正式插件；Explorer 实际合并/拆分按钮、图标观感和固定/取消固定行为仍必须肉眼确认。DPI 和辅助功能批准脚本继续要求当前提交、真实 Windows 设置、原始哈希与审阅人，不接受历史 250% 截图替代。

审计结论：本批关闭了“只有两个虚构窗口通过，但正式插件可能碰撞”的证据缺口，并修复了物理 DPI 插件截图缺少命令输入、中文参数乱码和空格截断三项采集缺陷。干净提交 `c22d678` 上任务栏 25 / 25、250% 浅/深色 8 / 8 自动捕获通过；插件输入与输出完整显示 `LongAssistantDpiReview`。250% 人工状态仍为 `pending`，发布矩阵仍缺 100%/125%/150%/200% 物理显示环境、三种辅助功能配置与至少一次 Narrator/NVDA 公告签核，以及最终任务栏肉眼分组检查。

### WindowManager / Macro 停止事务与全局副作用回收

- 审计发现两个关键插件在 `UnregisterAsync` 失败时仍会清空全部本地登记并返回停止成功。这样宿主可能卸载插件程序集，而 Windows 全局热键仍持有旧回调；失败项也失去重试依据。
- WindowManager 和 Macro 现在逐项注销并只移除成功项。失败热键保留所有权和内部登记，插件进入 `Error`，再次执行停止会仅重试残留项；WindowManager 在无 WPF `Application` 的隔离环境中也能安全收口。
- Macro 的首选与回退热键改为显式携带 `com.long.macro` 所有权，避免依赖调用栈推断。即使热键注销失败，宏引擎仍继续停止、释放 Hook/按键/鼠标按钮并解除事件订阅；只有引擎清理成功后才关闭 Overlay。
- 两插件版本分别升至 WindowManager `2.1.2`、Macro `1.1.2`，源码 Manifest、运行时投影和文档注册表同步；正向功能总账绑定新增生命周期案例，但真实桌面人工项保持 `pending`。
- 新增根目录 `verify-desktop-side-effect-lifecycle.ps1`。11 / 11 案例覆盖宏按键/鼠标释放、循环取消、Hook 部分安装/卸载失败、观察者异常，以及两个插件的热键注销失败与二次停止恢复；报告明确记录未改变真实窗口、未注入真实输入、未安装真实全局 Hook。
- Release 构建 0 警告、0 错误；全量自动化 762 / 762。高风险临时事务 7 / 7、截图交付 3 / 3、QuickLaunch 6 / 6 回归通过。

审计结论：WindowManager/Macro 的可自动验证停止事务已经闭环，不再把热键注销失败伪报为卸载成功，也不会因单个注销失败跳过 Macro 引擎清理。真实窗口布局、跨显示器、置顶/高完整性拒绝，以及在可丢弃目标中的录制、单次播放、循环停止仍必须由用户完成物理桌面签核；管理员 WPR、严格 `<200 MB`、DPI/辅助功能矩阵和干净 RC 继续阻断发布。

### 管理员原生性能证据与严格工作集门禁

- 当前账户属于 Administrators 和 Performance Log Users，但本会话令牌为中等完整性，管理员 SID 为 deny-only。WPR 与 WPA Exporter 均已安装，`CPU`、`DesktopComposition` 配置存在；预检准确返回 `administrator=false`、`ready=false`，本轮没有启动内核录制或弹出 UAC。
- 新增 `capture-native-performance-evidence.ps1`：正式采集要求管理员令牌、干净跟踪工作区、Release 宿主和 25 个唯一插件，同时录制 `CPU.Light` 与 `DesktopComposition.Verbose`，运行 `1120×760` 插件页 9 秒探针，并绑定提交、宿主/ETL/性能 JSON SHA-256。
- 原始 ETL 固定写入 `analysis_status=pending_analysis`、`release_gate_passed=false`；异常只取消本脚本成功启动的 WPR 会话。新增 `verify-native-performance-evidence.ps1` 校验提交、管理员状态、配置、插件数、文件哈希和最终空闲样本，禁止把“捕获完成”冒充“性能通过”。
- `measure-plugin-memory.ps1` 现在默认拒绝脏跟踪工作区，报告新增提交、脏状态、Release 宿主 SHA-256、样本数和空闲时长；`-AllowDirty` 仅供开发诊断。新增 `verify-plugin-memory-evidence.ps1`，正式口径要求至少 5 次、每次至少 9 秒、25 个插件且所有工作集严格 `<200 MB`。
- 开发态复测 5 次工作集为 197.9、198.1、199.3、199.1、198.3 MB，中位数 198.3 MB、最大 199.3 MB。工具提交后，干净提交 `6608247` 的正式 5 次样本为 198.7、198.6、198.1、197.7、197.8 MB，中位数 198.1 MB、最大 198.7 MB；提交、`source_dirty=false`、Release 宿主哈希、25 插件和 9 秒口径均由新验证器通过。
- 新增 [原生性能 WPR 采样验收](原生性能_WPR_采样验收.md)；Release 构建 0 警告、0 错误，全量自动化 765 / 765。

审计结论：严格 `<200 MB` 工作集门禁已在干净提交上正式关闭，不再作为新 RC 阻断。原生性能项从“缺工具且无法复现”推进到“可预检、可管理员采集、可校验、不可自动误批”，但当前会话未提升，WPR/WPA 原生归因仍未完成；真实桌面人工矩阵、DPI/辅助功能和干净 RC 继续阻断发布。

### 插件人工验收可移植回执

- 审计发现旧矩阵要求 `passed` 项引用当前检出目录中的原始证据文件，但仓库策略又明确不提交 `artifacts/quality/`；因此换电脑或 CI 无法复核人工通过状态。
- 新增 `approve-plugin-manual-evidence.ps1` 与 `docs/plugin-manual-approvals/`：原图、录像和报告继续留在本地，仓库只提交包含审阅人、提交、Manifest/Release EXE 哈希、命令集合与原始证据哈希元数据的批准回执。
- `verify-plugin-positive-matrix.ps1` 会消费有效回执，并拒绝重复、游离、身份错误或来源失效的回执。批准后只要 `src/`、Manifest 或命令集合变化，旧回执即失效。
- 新增 [插件人工证据验收](插件人工证据验收.md)，明确操作、批准、替换和撤销步骤。本轮未执行真实桌面副作用，也未自动批准任何检查；25 项人工状态仍为待处理，发布资格保持 `false`。
- Release 构建 0 警告、0 错误；全量自动化 767 / 767，人工回执与正向矩阵定向测试 10 / 10。

审计结论：人工验收从“只能在原机器口头确认”推进为“原始证据本地留存、批准结论可提交复核、产品变更自动失效”。下一步需要用户按风险顺序完成 25 项真实桌面操作；管理员 WPR/WPA、任务栏肉眼分组、物理 DPI、辅助功能/Narrator 和干净 RC 仍独立阻断发布。

### 最终收口聚合与人工交接

- 新增 `verify-final-closure.ps1`，一次执行 Release 构建、全量测试、25 插件正向矩阵和管理员 WPR 只读预检，并把机器阻断与人工阻断分开输出。
- `ready_for_human_validation=true` 只表示代码和自动门禁已经完成，可以开始人工验收；只有所有人工项关闭时，`release_eligible` 才能为 `true`。
- 新增 [最终收口与人工验收交接](最终收口与人工验收交接_2026-07-27.md)，把插件、任务栏、物理 DPI、辅助功能、WPR、干净 Windows/下载和真实 Registry/CDN 七类外部工作固定为唯一剩余清单。
- 新增 [用户最终验收操作手册](用户最终验收操作手册.md)，按“当前分支功能体验”和“最终提交设备/发布”两轮组织，避免合并产生新提交后使 DPI、辅助功能、WPR 或安装包证据失效。
- 发布候选检查表已纠正过期的内存失败、旧测试数和原始证据必须提交的口径；历史章节仍保留其发生时的数据，不作为当前结论。
- 依赖漏洞复核发现旧 xUnit 链路向测试项目带入 `System.Net.Http 4.3.0` 与 `System.Text.RegularExpressions 4.3.0` 两项 High 公告。测试基础包升级后，全解决方案直接/传递依赖扫描为 0 个已知易受攻击包；当前完整回归为 772 / 772。

审计结论：本轮之后不再存在需要继续扩功能才能开始验收的已知项。机器侧正式报告必须在本提交落地、跟踪工作区干净后重跑；随后进入用户/外部环境验证、PR 合并和新 RC 重建。

### 一键启动与候选打包入口

- 旧 `启动.bat`、`开发模式.bat`、`打包发布.bat` 使用损坏的本地编码和固定 v1.8 文案；旧打包入口只复制 `bin` 目录，不生成安装器、便携 ZIP、Manifest 或哈希，已不能代表当前发布流程。
- 三个批处理文件已重写为纯 ASCII 薄包装，分别调用 `start-long.ps1` 和 `package-long.ps1`，从任意当前目录双击都能定位仓库根目录。
- 启动入口支持 Debug/Release、跳过已有构建、等待退出、应用参数和只读预检。打包入口从项目读取版本，要求干净跟踪工作区，检查 .NET SDK/Inno Setup，安全确认同版本目录替换，并复核 Release Manifest 与 Setup.exe。
- 正式打包仍复用唯一 `release.ps1`，因此完整执行 Release 构建、全量测试、双 ZIP、真实命令/WebView2 冒烟、Setup.exe 和 SHA-256；批处理不再维护第二套简化打包逻辑。
- 启动与打包预检均返回 `ready=true`；Release 构建 0 警告、0 错误，新增入口定向测试 3 / 3、全量自动化 772 / 772，Release 启动冒烟正常退出。

### 统一工作区批次 4A：插件会话所有权与分离

- 新增 `PluginWorkspaceSession` 与进程级 `PluginWorkspaceSessionManager`，以独立领域状态保存 session ID、插件 ID、嵌入/分离/隐藏/结束放置、最近可见放置和修订号；同插件活动会话去重，结束后才允许创建新 session。
- `WebPluginPresentationCoordinator` 改为消费会话状态。嵌入、`Ctrl+D` 分离、独立窗口返回按钮和 `Esc` 返回均迁移同一个 WebView 与 session ID，不创建第二运行时。
- 返回与关闭不再共用语义：返回请求迁回主窗口且不触发生命周期；普通关闭隐藏会话并继续调用既有 Manifest 后台/停止策略；停止过程中的窗口关闭事件被关闭路由忽略。
- 主窗口和独立窗口新增显式“结束运行”，统一经过 `PluginRegistry.StopPluginAsync(..., persistAutoStart: false)` 释放插件与宿主资源；关闭视图不再等同于结束运行。
- 分离窗口仍无 Owner，并保留每插件 AppUserModelID 与动态图标。生产预探针通过：25 / 25 正式插件窗口、25 / 25 唯一任务栏身份、25 / 25 唯一图标，退出码 0。
- Release 构建 0 警告、0 错误；新增会话与关闭路由定向测试 8 / 8，全量自动化 859 / 859。任务栏正式报告将在提交落地且跟踪区干净后绑定提交与宿主哈希重跑。
- 当前批次 4 状态为 `in progress`，其中 4A 已完成。下一步 4B 新增 `plugin-runtime:<id>:<session>` 工作区模块，把稳定会话投影到模块条，并统一 Launcher、插件设置和插件目录的运行动作；不重写插件 API、市场事务或 WebView2 桥接协议。

### 统一工作区批次 4B：Web 插件运行模块

- `plugin-runtime:<id>:<session>` 已接入 Workspace Shell；嵌入与默认分离启动都保留模块条和插件目录。
- 设置、分离、返回、关闭模块、普通关窗和结束运行已分开处理；停止或 session 结束会清理失效模块。
- Release 构建 0 警告、0 错误，全量自动化 868 / 868；任务栏预探针 25 / 25；1120×760 明亮和 720×620 暗色紧凑渲染通过。

恢复后第一时间检查：

1. `completed`：Web 插件嵌入、分离、返回、关闭模块、重新打开和结束运行确定性探针；
2. `completed`：`background` 关窗、隐藏会话、恢复运行、复用 WebView 和显式结束；
3. `completed`：WebView 输入、PageDown 滚动与 Windows `Ctrl+D`，迁移后 DOM、session 和 WebView 保持；
4. `completed`：停止、扫描器升级重载和卸载后的宿主资源、session、运行模块清理与 MRU 回退；
5. `completed`：默认分离原生插件保持插件自有 surface，不创建空 session 或运行模块；QuickLaunch 真实开关窗验证通过。

探针结果：同一 session 与同一 WebView 跨分离/返回保持；升级形成新注册与新 session，卸载移除注册项，两条路径均清理旧模块并回到 `settings:root`。

批次 4 已完成。下一步进入批次 5，先检查市场列表/详情的现有 master-detail 状态和模块地址，再实施模块内路由。

### 统一工作区批次 5A：市场模块内路由

- 市场保持单一 `marketplace:catalog` 顶层模块，新增 `catalog` 与 `detail:<pluginId>` 子路由；详情不再创建额外模块。
- 紧凑布局返回保留选中项和列表实例，`Esc` 与返回按钮先退详情；目录过滤移除当前条目时安全回到 catalog。
- Release 构建 0 警告、0 错误，全量自动化 870 / 870；720×620 与 1120×760 真实详情渲染通过。

下一步检查市场空、错误、离线、更新和操作中状态，再处理信息密度与完整主题/DPI/辅助功能签核。

### 统一工作区批次 5B：市场目录状态

- 市场目录已区分加载、正常、来源降级、目录为空、筛选无结果和完全失败；降级继续显示可信数据，失败留在市场模块内并可重试。
- 紧凑布局隐藏宣传横幅，将本地包导入保留在工具区；720×620 暗色错误页与 1120×760 亮色空目录页首屏可操作。
- Release 构建 0 警告、0 错误，全量自动化 874 / 874。

下一步检查更新可用、安装/升级/卸载进行中、成功、取消和失败后的模块内反馈与焦点恢复。

### 统一工作区批次 5C：市场操作反馈

- 安装、更新、降级、重装和卸载已使用独立审查标题、主动作、进行中提示和成功结果；更新不再被笼统显示为安装。
- 事务进行时关闭、取消和确认统一禁用；完成后只向可见且可用的详情动作恢复焦点，紧凑卸载不再回落到隐藏列表。
- 确认层按实际内容宽度响应，720×620 暗色中文和 1120×760 亮色英文更新审查通过；Release 0 警告、0 错误，自动化 882 / 882。

下一步检查市场信息密度、完整键盘路径、辅助功能名称/状态，以及主题和 DPI 组合。

### 统一工作区批次 5D：市场键盘与辅助功能签核

- 市场列表、详情返回和动态确认动作已补齐自动化名称与状态；紧凑列表支持 `Enter` / `Space` 打开详情，`Alt+Left` 返回，确认层优先消费 `Esc`。
- UIA 专项探针通过，确认层取消后焦点恢复到原操作；探针未执行安装、更新或卸载写操作。
- 720×620 高对比度 200% DPI 与 1280×800 亮色 150% DPI 审查通过，无文字重叠、裁切或不可辨按钮；Release 0 警告、0 错误，自动化 888 / 888。

批次 5 已完成。恢复后第一时间检查批次 6 的旧路由使用点、正式入口覆盖和可删除边界，再按“架构测试、删除旧实现、端到端回归”的顺序推进。

### 统一工作区批次 6A：强类型管理页路由

- 已移除 `ShowPage(string)`、字符串侧栏 Tag 和 Legacy 页面目录；8 个管理页统一使用 `WorkspaceManagementPage` 与 `WorkspaceManagementModuleCatalog`。
- 质量启动入口和 ToolCenter 侧栏均先进入 `WorkspaceSessionCoordinator`，架构测试阻止旧字符串路由回流。
- 六个正式质量入口桌面回归通过；Release 0 警告、0 错误，自动化 888 / 888。

恢复后第一时间审计 `EmbeddedPluginSurface` 的调用方、原生插件生命周期和 WorkspaceShell 承载能力，再决定迁移或保留有明确边界的兼容适配器。

### 统一工作区批次 6B：旧嵌入 Surface 清理

- 生产调用图确认 `EmbeddedPluginSurface` 无调用方，Web 插件已由 WorkspaceShell 会话承载，原生插件继续持有独立窗口；旧 XAML、回调和双状态分支已删除。
- 通用桌面冒烟改用正式 Workspace runtime 自动化入口；UIA 分离与返回通过，官方会话探针同时验证同 session、同 WebView、后台恢复、升级卸载清理和原生窗口边界。
- Release 0 警告、0 错误，自动化 888 / 888。

恢复后第一时间检查页面私有搜索状态、重复生命周期判断和剩余旧结构引用，再执行 Launcher、Workspace、SuperPanel 与分离窗口最终端到端回归。

### 统一工作区批次 6C：插件打开入口收口

- 页面搜索状态审计确认由 `WorkspaceSearchSession` 统一持有，不存在需要迁移的页面私有副本。
- 工作区与插件目录统一通过 `PluginMainUiLauncher` 打开插件；后台态不再重复启动，旧的 `ToolCenter.Visibility` 写入已移除。
- Release 0 警告、0 错误，自动化 891 / 891；官方插件会话探针通过。通用桌面冒烟仍在命令面板 `Shift+Enter` 复制校验处阻断。

恢复后第一时间检查该桌面冒烟阻断，再完成 Launcher、Workspace、SuperPanel 与分离窗口的最终端到端回归。

### 统一工作区批次 6D：最终桌面回归收口

- 桌面冒烟改用仅由质量参数启用的确定性窗口消息；默认产品运行时不启用该通道，报告明确记录自动化传输方式。
- 修复剪贴板短暂占用、增量搜索选择、紧凑市场详情和宽窄工作流测试基线。
- Release 0 警告、0 错误，自动化 891 / 891；完整桌面冒烟与官方插件会话探针均通过。

恢复后第一时间审计 Workspace 插件目录与 ToolCenter 管理导航是否重复，再检查默认 1120 宽度的信息密度和真实键鼠交互。

### 统一工作区批次 7A：管理导航收口

- 删除 ToolCenter 常驻 220px 管理侧栏，八个管理目的地收进管理中心首页；打开后继续使用独立工作区模块，左侧只保留插件目录。
- 管理中心搜索改为筛选首页入口；默认宽屏入口为 4 列，紧凑模式为 2 列。
- 修复插件目录首次宽屏保持 `Auto`、被英文名称撑宽的问题，宽屏固定 220px，紧凑模式固定 64px。
- `1120×760` 中/英文亮暗主题及 `720×560` 紧凑渲染通过；Release 0 警告、0 错误，自动化 891 / 891，完整桌面冒烟通过。

恢复后第一时间执行真实鼠标/键盘验收：右上角进入管理中心、首页入口开页、模块标签切换/关闭/MRU、管理搜索与物理 DPI；自动化通过不替代该人工签核。

### 统一工作区批次 7B：管理交互门禁

- 八个管理入口、模块标签和关闭动作新增不随语言变化的稳定 UIA ID；标签状态公开活动、可关闭和模块键，关闭动作提供模块标题帮助文本。
- 桌面冒烟新增管理搜索、市场开页、设置关闭、市场再激活/关闭及管理中心 MRU 回退；全程使用 UIA Value/InvokePattern，不使用坐标点击。
- 质量窗口通道增加精确结果选择，解决多宿主长会话中的 WPF UIA 选择缓存失真；该通道仍只由显式质量参数启用。
- Release 0 警告、0 错误，自动化 891 / 891，包含新管理场景的完整桌面冒烟通过。

恢复后第一时间由用户执行物理鼠标/键盘与物理 DPI 签核；若无问题，再审计管理首页 Tab 顺序、焦点可见性和 Narrator 朗读结果。

### 统一工作区批次 7C：焦点与朗读语义

- 管理首页八个入口显式固定 0–7 TabIndex；UIA 逐项聚焦顺序通过，未使用坐标点击。
- 模块标签焦点环统一为 `Long.Brush.Focus` 2px；模块关闭动作改为本地化“关闭 + 模块名”，不再只朗读通用“关闭”。
- `1120×760` 高对比度管理中心真实渲染通过；Release 0 警告、0 错误，自动化 891 / 891，完整桌面冒烟通过。

恢复后第一时间由用户执行物理 Tab/Shift+Tab、Enter/Space、Narrator 朗读和物理 DPI 签核；自动化报告明确未替代这些人工结果。

### 统一工作区批次 7D：物理验收证据收口

- 辅助功能与物理 DPI 证据升级为 v2；旧清单不能继续通过新汇总门禁。
- 辅助功能批准明确要求八入口顺序、Enter/Space 开页、标签关闭/MRU，以及读屏器对模块关闭动作的完整朗读。
- 物理 DPI 证据必须包含管理中心主视图，并记录管理布局与模块标签可读性。

恢复后第一时间按操作手册采集真实物理证据；源码侧不代替用户签署物理键盘、Narrator/NVDA 和 100%/125%/150%/200% DPI 结果。

### 统一工作区批次 7E：证据门禁可执行回归

- 新增真实调用 PowerShell 汇总器的测试夹具：辅助功能覆盖三种配置，物理 DPI 覆盖 100%/125%/150%/200% 和 32 个哈希锁定文件。
- 六种路径分别验证完整 v2 通过、v1 证据拒绝，以及管理中心专项人工清单缺失拒绝。
- 回归发现并修复完整 DPI 矩阵在 `capture_count` 汇总阶段无法读取有序字典键的问题。

统一工作区代码侧批次已经完成。恢复后第一时间只检查真实物理键盘、Narrator/NVDA、四档 DPI 及任务栏视觉结果；这些外部签核未完成前不生成新 RC。

### 最终发布链批次 8A：v2 物理证据绑定

- 最终外部门禁不再只检查物理证据摘要的分类、通过状态和提交号；现在强制要求 schema v2。
- DPI 摘要必须包含 100%/125%/150%/200% 四档、每档 8 张且总计 32 张；辅助功能摘要必须包含三种配置和至少一次读屏批准。
- 最终决策 JSON 记录所消费的两类 schema 版本、DPI 捕获数和读屏批准数，便于发布复核。
- 专项回归覆盖正常候选、旧 DPI、缺少 DPI 捕获、旧辅助功能摘要及无读屏批准。

恢复后第一时间仍是采集真实物理证据；本批只关闭最终聚合可能接受旧摘要的漏洞，不改变人工阻断状态。

### 最终发布链批次 8B：外部证据来源锁定

- 下载与干净 Windows 摘要在最终决策层强制校验 schema、环境/下载主机和原始证据哈希字段；干净环境签名状态必须与发布通道一致。
- Marketplace 演练摘要升级为 v2，只有完整部署回滚通过时才记录五份底层报告的文件名与 SHA-256。
- 最终决策器从演练摘要同目录重新计算五份报告哈希，拒绝旧摘要、缺失报告、篡改内容或不完整生命周期。
- 专项回归覆盖有效候选、下载/干净环境摘要不完整、旧 Marketplace schema 和底层部署报告篡改。

恢复后第一时间仍需在受控凭据环境执行真实 HTTPS 演练；本批没有访问线上 Registry/CDN，也没有解除该人工阻断。

### 最终发布链批次 8C：候选 Manifest 身份收口

- 新构建的 Release Manifest 增加 schema v1；签名工具继续保留该 schema，但代码签名仍是可选通道。
- 最终门禁强制要求 `Long Assistant`、有效版本、`win-x64`、可解析生成时间、指定提交和 `source_dirty=false`。
- 每个 ZIP 必须使用唯一安全文件名和合法种类，具备有效 SHA-256/字节数，并保持 25 个插件、25 个 Manifest、25 个唯一 ID、42 条命令、命令冒烟成功和零新增 WebView2 进程。
- unsigned 候选必须保留发布者未验证与 SHA-256 提示；最终决策记录 Manifest schema、版本、runtime、源码状态和包数量。
- 专项回归覆盖有效双包、旧 Manifest、脏源码和异常包清单。

恢复后不得复用缺少 schema 的旧 RC Manifest；真实人工门禁完成后，从最终 `master` 重新构建新候选。

### 最终发布链批次 8D：候选包实体完整性

- 最终外部门禁从 Release Manifest 同目录逐项读取所有候选 ZIP，重新核对文件存在性、实际字节数和 SHA-256，不再只信任 Manifest 中的自报字段。
- 任一候选包被删除、截断或替换都会在写入最终决策前拒绝；8E 后该结论统一记录为 `candidate.artifact_files_verified=true`。
- 专项回归覆盖有效双包、主包篡改和第二包缺失；本批不生成新 RC，也不改变真实桌面、物理设备和线上凭据门禁状态。

恢复后仍先完成真实人工门禁；生成新候选后，必须保留 Manifest 与全部 ZIP 在同一目录供最终聚合器复核。

### 最终发布链批次 8E：发布附件总账

- Release Manifest 的可选安装器现限制为至多一个当前用户级 Inno Setup，要求无提权、25 个插件、42 条命令以及有效文件名、尺寸和 SHA-256。
- 最终门禁统一复核 Manifest 中全部 ZIP 与 Setup.exe，并要求同目录 `SHA256SUMS.txt` 无重复、无额外条目且与附件集合及哈希精确一致。
- 最终决策记录安装器数量、附件实体复核和校验清单复核状态；专项回归覆盖有效三附件、ZIP/安装器篡改、附件缺失和清单不完整。

恢复后仍先执行真实人工门禁；新候选目录必须完整保留 Manifest、全部附件和 `SHA256SUMS.txt`，不得拆散后再生成最终决策。

### 最终发布链批次 8F：下载与干净环境来源复核

- 下载批准摘要和干净 Windows 摘要升级为 schema v2，分别记录原始下载证据、独立批准文件或干净环境证据清单的安全文件名与 SHA-256。
- 生成摘要时强制摘要与来源文件位于同一目录；最终聚合器从摘要目录重新读取并复算来源哈希，拒绝缺失、移动替换或批准后篡改。
- 旧 schema v1 摘要不能复用，必须保留原始证据并重新运行专用验证器；专项回归覆盖有效来源、下载证据篡改和干净环境清单缺失。

恢复后先检查下载与干净环境证据包是否完整同目录，再生成 v2 摘要；DPI 与辅助功能多目录证据的可移植来源复核是下一批代码审计项。

### 最终发布链批次 8G：物理矩阵便携来源包

- DPI 与辅助功能矩阵汇总器继续接收 schema v2 原始证据，但输出摘要升级为 schema v3，并在摘要旁生成 `<摘要名>.sources/`。
- 来源包只复制四份 DPI 与三份辅助功能已批准 Manifest；摘要使用安全相对路径和 SHA-256 引用，不保存采集机绝对路径，也不复制大体积截图。
- 最终门禁重新计算七份便携 Manifest 哈希，并核对来源 schema、分类、提交、档位/配置和批准状态；旧 v2 摘要、来源缺失、篡改或内容错配均拒绝。

恢复后应把 v3 摘要与对应 `.sources` 目录作为一个整体移交；原始截图和日志仍留在采集机，生成来源包前由矩阵汇总器完整复核。

### 最终发布链批次 8H：最终门禁只读预检

- `verify-external-release-gate.ps1 -PreflightOnly` 执行与正式决策完全相同的候选、附件、来源包、人工摘要和部署回滚校验，但不接受 `OutputPath`、不创建文件。
- 预检只向标准输出返回 `external_release_gate_preflight` JSON，并显式记录 `preflight_only=true`；正式模式仍要求不存在的输出路径并生成 `external_release_gate_decision`。
- 安装升级手册已修正干净 Windows v2 摘要的同目录输出示例，并固定“先预检、后生成不可变决策”的恢复顺序。

恢复后先使用最终门禁 `-PreflightOnly` 对整套外部输入排错；只有输出通过且人工确认输入冻结后，才执行正式决策命令。

### 最终发布链批次 8I：来源内容交叉校验

- 最终门禁在复算下载证据、独立批准和干净 Windows 清单哈希后继续解析 JSON，不再只把“哈希与摘要一致”当作内容可信。
- 下载链交叉核对分类、通过状态、提交、发布通道、包名/哈希、下载主机、操作人、审核人及批准所引用的原证据；干净环境链核对版本、提交、通道、包哈希、签名状态、环境标签、自动检查和人工批准。
- 即使同时替换来源文件并更新摘要中的 SHA-256，只要来源内容与摘要身份不一致，预检和正式决策都会拒绝。

恢复后先确认来源包本身可解析且身份字段一致，再运行 `-PreflightOnly`；不要通过手工重算哈希掩盖来源内容差异。

### 最终发布链批次 8J：Marketplace 演练报告语义复核

- 最终门禁在复算 Marketplace 五份报告 SHA-256 后继续解析 JSON，强制预检与实际部署使用同一 Release ID、HTTPS 目标和文件计划。
- 三份公开验证报告必须指向同一 Registry，包清单与总字节数必须自洽；部署后必须观察到 Registry 变化，回滚后必须恢复演练前基线。
- Dry Run、基线验证、部署、部署后验证和回滚后验证的时间必须按顺序落在摘要声明的演练窗口内；成套替换报告并更新摘要哈希不能绕过语义校验。

恢复后先使用 `-PreflightOnly` 检查完整演练目录；真实 HTTPS Registry/CDN、正式信任库和受控令牌仍须由发布环境提供，本批不把测试夹具当作线上演练通过。

### 最终发布链批次 8K：正式决策原子落盘

- 正式外部门禁先把完整 JSON 写入目标目录内的唯一临时文件，再使用同卷 `File.Move` 原子提交，不再直接向正式路径执行 `Set-Content`。
- 目标已存在或并发生成时绝不覆盖；只有一个写入者能够成功，失败写入者保留既有决策并清理自己的临时文件。
- 专项回归覆盖正常写入无残留、已有决策内容不变和两个独立 PowerShell 进程竞争同一路径；`-PreflightOnly` 继续完全不落盘。

恢复后仍按“只读预检通过、冻结输入、指定全新决策路径”的顺序执行；原子落盘保证文件完整，不代表真实设备和线上凭据门禁已经完成。

### 最终发布链批次 8L：外部摘要统一原子输出

- 新增共享 `release-evidence-io.ps1`，下载、干净 Windows、DPI、辅助功能摘要及最终决策统一使用同目录临时 JSON 和不可覆盖原子重命名。
- 干净环境摘要不再覆盖已有文件；下载摘要在检查后发生并发竞争也只允许一个写入者成功，失败路径清理自己的临时文件。
- DPI 与辅助功能先在唯一临时目录组装 `.sources`，再提交来源目录和摘要；并发进程不能删除另一进程已经提交的来源包。
- 专项回归覆盖正常便携来源、临时资源清理和两个独立 DPI 汇总进程竞争同一输出。

恢复后生成任何外部摘要都必须使用全新路径，并把 v3 摘要与 `.sources` 作为整体移交；不要删除旧摘要后复用名称。

### 最终发布链批次 8M：人工批准原子提交

- 下载人工批准文件使用共享不可覆盖原子创建，不再直接写正式路径。
- 干净 Windows、DPI、辅助功能及可选 Sparse Explorer 的原地批准使用独占审核锁、验证前 Manifest SHA-256 和 `File.Replace` 原子替换。
- 审核脚本先记录原始哈希再读取内容，提交时复核；期间证据变化会拒绝。只有 `pending` 证据可批准，两个审核进程竞争时只有一个能提交，另一方不能静默覆盖审核人或结论。
- 临时 JSON、替换备份和进程锁均在正常与失败路径清理；专项测试覆盖成功替换、陈旧哈希拒绝、并发审核及 Sparse Explorer 完整批准链。

恢复后每份人工证据只执行一次正式批准；需要重做时重新采集新证据，不通过并发运行或直接编辑 JSON 更换审核结论。

### 最终发布链批次 8N：采集 Manifest 原子提交

- 下载、干净 Windows、DPI、辅助功能和可选 Sparse Explorer 的采集 Manifest 统一使用共享不可覆盖原子创建。
- 截图、日志和自动检查可先写入新证据目录；只有所有采集步骤完成并成功原子提交 Manifest 后，该目录才可进入人工批准。
- 采集进程中断不会留下半份正式 JSON；残留目录因缺少完整 Manifest 无法批准或汇总，必须保留作诊断或弃用并以新目录重采。
- Sparse Explorer 的只读预检报告同样原子创建，仍不能替代真实注册、Explorer 操作和独立批准。

恢复后先检查证据目录是否存在完整且可解析的预期 Manifest；若只有截图或日志，不要手工补写 Manifest，直接使用新的输出路径重新采集。

### 最终发布链批次 8O：Marketplace 演练摘要原子提交

- 正式 HTTPS Marketplace 演练摘要统一使用共享不可覆盖原子创建，不再直接写入 `rehearsal-summary.json`。
- 中断或并发执行不会留下可被最终门禁误认的半写摘要；缺少完整摘要的演练目录只能用于诊断，正式重试必须使用新的证据目录。
- 自动回归只验证脚本结构与共享写入器契约；真实 Registry/CDN 演练仍需在受控凭据环境执行。

### 最终发布链批次 8P：Sparse Explorer 验证摘要原子提交

- 可选 signed/Sparse Package Explorer 三段式门禁的最终验证摘要改用共享不可覆盖原子创建。
- 目标已存在或多个验证进程竞争同一路径时只允许一个成功，不覆盖既有审核结果，也不留下半写 JSON 或临时文件。
- 本批只增强证据完整性；正式证书、干净 Windows 11 和真实 Explorer 人工验收仍属于可选 signed 通道外部条件，不阻断 unsigned 主产品。

### 最终发布链批次 8Q：插件人工回执并发安全

- 25 项插件人工验收的首次回执使用共享不可覆盖原子创建，检查后并发写入不再覆盖另一审阅人的结论。
- `-Replace` 只允许重审已存在的回执；脚本记录旧回执 SHA-256，并在独占锁内复核后原子替换。旧回执变化或另一审核进程先提交时拒绝本次替换。
- 本批不生成或批准任何人工回执；25 项真实桌面操作仍需用户逐项执行并审阅原始证据。
- 全量高负载复现文件搜索首轮 600ms 索引尚未完成时测试偶发空快照；两个结果断言改为在 10 秒测试边界内轮询，产品运行时的 600ms 非阻塞返回策略保持不变。

### 最终发布链批次 8R：原生性能采集 Manifest 原子提交

- 管理员 WPR 采集只在 ETL 和插件页性能报告完整生成、哈希完成后，使用共享不可覆盖原子写入器提交 `native-performance-evidence.json`。
- 中断采集可以保留 ETL 和日志用于诊断，但缺少完整 Manifest 的目录不能进入完整性验证或人工 WPA 签核；正式重试必须使用新的输出目录。
- 本批没有提升当前会话、启动 WPR 或生成正式原生性能证据；管理员采集与 WPA 人工归因仍待外部执行。

### 最终发布链批次 8S：signed 验证摘要原子提交

- `verify-signed-release.ps1 -OutputPath` 已接入共享 `release-evidence-io.ps1`，代码签名独立验证摘要现在使用不可覆盖原子创建。
- 目标摘要已存在时拒绝覆盖；验证过程仍会先完整复核候选 Manifest、ZIP、安装器、Authenticode 签名、证书指纹和 SignTool 时间戳验证。
- 本批只增强可选 signed 通道的验证摘要完整性；正式代码签名证书仍是外部条件，不阻断 unsigned 主产品。

### 最终发布链批次 8T：unsigned 候选总账原子提交

- `release.ps1` 已接入共享 `release-evidence-io.ps1`；`SHA256SUMS.txt` 使用 `Write-NewTextFileAtomically`，`release-manifest.json` 使用 `Write-NewJsonFileAtomically`。
- 候选目录仍先经过版本匹配、Release 构建、932 项自动化、双发布形态、命令冒烟、WebView2 清理、安装器构建与哈希计算，再原子提交总账文件。
- 本批只增强候选总账落盘完整性；已存在候选目录仍需显式 `-Force`，真实下载、干净 Windows、DPI、辅助功能和 Registry/CDN 外部门禁不变。

### 最终发布链批次 8U：signed staging 总账原子替换

- `sign-release.ps1` 已接入共享 `release-evidence-io.ps1`；私有 staging 内既有 `release-manifest.json` 通过 `Update-JsonFileAtomically` 按原始 SHA-256 原子替换。
- `SHA256SUMS.txt` 已支持文本原子替换：共享写入器新增 `Update-TextFileAtomically`，在替换前复核 staging 副本哈希，并清理临时与备份文件。
- 本批不改变证书、SignTool、时间戳、签名目标枚举、独立复验或最终目录移动语义；正式代码签名证书仍是可选 signed 通道外部条件。

### 最终发布链批次 8V：任务栏身份证据原子注释

- `verify-taskbar-identity.ps1` 已接入共享 `release-evidence-io.ps1`；输出路径存在时拒绝复用，避免覆盖旧任务栏身份矩阵。
- 宿主质量模式生成原始报告后，验证脚本先记录原始 SHA-256，再用 `Update-JsonFileAtomically` 注入 `source_commit`、`source_dirty=false` 和宿主 EXE SHA-256。
- 本批只增强 25 个插件任务栏身份矩阵证据的落盘完整性；真实任务栏视觉、固定/取消固定和人工观察仍需用户在物理桌面签核。

### 最终发布链批次 8W：视觉矩阵 manifest 原子创建

- `capture-visual-matrix.ps1` 已接入共享 `release-evidence-io.ps1`；`visual-matrix.json` 改为 `Write-NewJsonFileAtomically` 创建，避免工程视觉矩阵 manifest 部分写入或被旧文件复用。
- 视觉矩阵仍保持工程基线定位：自动截图只记录主题、视图、DPI、辅助功能状态、尺寸与截图 SHA-256，不替代物理显示器和人工视觉签核。
- 本批补充质量门禁，要求脚本继续使用共享原子写入器，并禁止恢复到 `Set-Content` 写入 manifest。

## 文档口径

本文档和根目录 README 描述当前主线。`v0.x`、`v1.0-v1.8` 总结文档以及 2026-07-20 之前的质量报告均作为历史记录保留，不再代表当前发布基线。

## 恢复后简明交接

当前 `master` 已通过 975 / 975 全量测试，Release 构建为 0 警告、0 错误。发布证据的采集、批准、汇总和最终决策已基本统一为不可覆盖原子提交；正式代码签名和 Sparse Package 仍是可选 signed 通道，不阻断 unsigned 主产品。

接手后按以下顺序继续：

1. 先执行 `git fetch origin`、`git status` 和 `git rev-list --left-right --count master...origin/master`，确认主线对齐且没有受跟踪改动；不要提交 `artifacts/quality/` 下的历史本地证据。
2. `completed`：`measure-plugin-memory.ps1` 已接入共享 `release-evidence-io.ps1`，`plugin-memory-report.json` 现在使用不可覆盖原子创建；结构回归禁止恢复为直接 `Set-Content`。
3. `completed`：可选 signed 验证摘要、signed staging 总账、unsigned 候选总账、任务栏身份矩阵与工程视觉矩阵 manifest 已接入共享原子写入体系；下一批继续扫描剩余正式 JSON/Manifest 输出。
4. 代码侧收口后，由用户依次完成 25 项插件真实桌面验收、任务栏分组、四档 DPI、辅助功能/读屏、管理员 WPR/WPA、真实下载与干净 Windows 验收。
5. 最后在冻结提交上重建新的 unsigned RC，完成真实 Registry/CDN 回滚演练，先运行最终门禁 `-PreflightOnly`，输入冻结后再生成正式决策。

## 2026-07-30 插件开发体系 PX2B

- 插件打包已从普通目录压缩升级为“产前生产验证、内容安全审计、确定性 ZIP、逐文件 SHA-256 总账、产后生产验证”的完整链路。
- 新 `.lpak` 包含 `package-files.json` Schema v1；安装验证器交叉核对插件身份、版本、文件集合、大小和哈希，同时继续兼容没有总账的历史包。
- 打包器阻断缓存、构建目录、重解析点、`.env*`、证书/密钥和临时文件；Windows 路径按大小写不敏感规则拒绝重复，避免解压覆盖。
- Base64 Web 插件已完成两次真实打包，两个产物 SHA-256 完全一致；PX2C 将补齐 Web、Script、Native、Hybrid 四种样板矩阵与分发资格摘要。
- 全量自动化 947 / 947，Release 构建 0 警告、0 错误。
- 本批不会改变正式发布判断：25 项桌面人工验收、物理 DPI、辅助功能、管理员性能采集、真实下载/干净 Windows 和 Registry/CDN 演练仍保持待用户或受控环境验证。

## 2026-07-30 插件开发体系 PX2C

- Web、C# Script、Native、Hybrid 四种运行时已经进入同一真实发布矩阵；每个样板均执行目录生产验证、确定性打包、文件总账复核和成品生产验证。
- 验证 JSON 新增权限摘要和分发资格：纯 Web 包可在发布者签名后进入远程市场；Script、Native、Hybrid 只允许本地导入并显示完全信任提示。
- 修复 Script 曾被误当作低信任运行时，以及 Hybrid 可借 `runtime=webview` 绕过远程市场限制的两个边界错误。
- `verify-plugin-runtime-matrix.ps1` 输出 `long_plugin_runtime_matrix` Schema v1，支持不可覆盖原子报告，执行后不会在仓库留下包或临时插件。
- 全量自动化 951 / 951，Release 构建 0 警告、0 错误。
- PX2 生产验证与打包阶段已经代码侧收口；下一开发批次为 PX3A，建立版本化 Web TypeScript 声明和不启动 WPF 宿主的 Mock Host。
- 外部人工发布门禁维持原状态，本批不会自动批准真实桌面操作、物理设备、辅助功能或生产 Registry/CDN。

## 2026-07-30 插件开发体系 PX3A

- `@long-assistant/plugin-sdk` v1.0.0 已落盘，Web 插件获得全局 `long.*` TypeScript 声明、宿主消息类型和真实返回模型。
- Mock Host 可以在 Node 中独立测试调用编排、剪贴板、插件存储 CAS、热键和剪贴板事件，不启动 WPF/WebView2，也不操作真实系统。
- 生产注入脚本的 127 个 bridge method 与 Mock 总账已由 C# 门禁逐项对账；API 漂移会阻断全量测试。
- SDK 使用 TypeScript 5.9.3 严格类型测试和 Node 内置行为测试；依赖锁定在 `sdk/web/package-lock.json`，`node_modules` 不进入 Git。
- 基础能力 API 手册不再是待录入占位文档，现已明确 API v1.0.0、26 个命名空间、权限、特殊返回形态和变更流程。
- SDK 类型编译通过，Node Mock 行为测试 5 / 5；npm dry run 仅包含 5 个发布文件。全量自动化 952 / 952，Release 构建 0 警告、0 错误。
- 下一批为 PX3B：拆出稳定的 .NET Contracts SDK，设计并迁移原生插件对宿主构建目录的直接引用。

## 2026-07-30 插件开发体系 PX3B

- 新增 `LongBetterWindows.PluginSdk` v1.0.0，将 `Contracts`、`Core`、`Capabilities` 从宿主 EXE 的编译边界移入独立程序集；宿主现在单向引用 SDK，并在 XAML 中显式解析 SDK 合同类型。
- SDK 可生成包含 DLL、XML 文档和 README 的 NuGet 包，程序集三段版本与 `ApiVersion.Current` v1.0.0 自动对账；本批只验证包结构，没有向公共 NuGet 源发布。
- `ClipboardHistory.Background` 已作为首个生产原生组件完全移除宿主项目引用，并显式声明自身 Serilog 依赖；empty、hotkey、full 三个原生脚手架模板全部改为引用 SDK。
- 插件加载上下文固定复用宿主已加载的 `LongBetterWindows.PluginSdk`，避免插件目录携带同名 SDK 时产生类型身份分裂。
- 新增 4 项边界门禁，验证合同程序集归属、宿主单向依赖、迁移组件不引用宿主、原生模板不回退宿主引用。
- 仍有 7 个内置原生插件直接使用宿主 HUD、设置控件、取色实现或搜索模型，因此继续保留宿主引用；这属于已识别的内置 UI/交互耦合，不计为 SDK 完全迁移。
- 下一批为 PX3C：提供不启动 WPF 宿主的 .NET 插件合同测试工具与 CI 样板，并设计内置 HUD、设置 UI 和搜索扩展的稳定 SDK 接口，逐步迁移剩余原生插件。

## 2026-07-30 插件开发体系 PX3C

- 新增 `LongBetterWindows.PluginSdk.Testing` v1.0.0；该程序集只引用 Contracts SDK，不引用或启动 WPF 宿主，提供 `PluginTestHost`、`PluginContractHarness`、能力拒绝、测试替身注册、身份、生命周期、命令、语言和重复资源释放验证。
- 生产 `HostProvider` 与测试宿主统一复用 SDK 内 `HostCapabilityCatalog`；29 个服务入口映射到 27 个唯一能力，删除宿主内部第二份手工映射。
- empty、hotkey、full 三套原生模板补齐独立 Serilog 依赖、xUnit 合同测试项目和源码排除规则；`new-plugin.ps1` 会重命名测试项目、加入解决方案并执行测试。三个模板均已在隔离临时仓库真实生成和通过。
- 新增 Native、C# Script、Web 三类 GitHub Actions CI 样板，均接入生产验证；Script/Web 同时接入确定性打包，Native 先执行无宿主合同测试。
- SamplePlugin 已从 `FloatingHudWindow` 迁移到 `INotificationService`，成为第二个只依赖 SDK 的正式原生项目；直接宿主项目引用从 7 个降至 6 个。
- `native-plugin-boundary-audit.json` 机器清单与实际 `.csproj` 交叉门禁，详细迁移接口和顺序见 [`.NET 原生插件扩展边界设计（PX3C）`](.NET原生插件扩展边界设计_PX3C.md)。
- 两个 SDK v1.0.0 NuGet 包均只包含目标 DLL、XML 文档和 README；四运行时生产矩阵 Web、Script、Native、Hybrid 4 / 4 通过。全量自动化 969 / 969，Release 构建 0 警告、0 错误。
- 下一批为 PX3D-A：抽出 WPF Plugin UI Kit 与通用异步投递边界，迁移 FolderNote、Macro、WindowManager 及 Screenshot/ColorPicker 的共享 HUD/热键设置依赖。
- 外部人工发布门禁不变；Mock 测试不能替代真实剪贴板、Explorer、网络、文件写入、系统操作和 WebView2 体验验收。

## 2026-07-30 插件开发体系 PX3D-A

- 新增可独立构建与打包的 `LongBetterWindows.PluginSdk.Wpf` v1.0.0；共享热键设置控件和多显示器/DPI 定位工具不再属于 Host 程序集。
- `HotkeySettingsControl` 移除对 `ServicesInitializer` 的隐式依赖，五类插件均从自身 `IHostApi` 显式注入热键能力；主题与高对比度继续复用 `Long.*` 动态语义资源。
- 通用 `AsyncDeliveryBoundary` 已进入 Contracts SDK 的 Core 编译边界；Macro、Screenshot、WindowManager 的普通 HUD 提示迁移到 `INotificationService`。
- 三个插件已完全移除 Host 项目引用，机器审计的宿主耦合从 6 项降到 3 项：ColorPicker 仅剩采样/旧 HUD，FolderNote 仅剩锚点编辑浮层，QuickLaunch 仅剩搜索合同。
- WPF UI Kit NuGet 结构验证通过；四运行时生产验证与确定性打包矩阵 4 / 4；全量自动化 973 / 973；Release 解决方案构建 0 警告、0 错误。
- 下一批为 PX3D-B：建立屏幕颜色采样能力与锚点编辑 Surface，迁移 ColorPicker、FolderNote；随后 PX3D-C 迁移 QuickLaunch 搜索合同。
- 本批工程自动化不能替代真实桌面抽查；热键冲突与回滚、主题/高对比度/200% DPI、三类通知提示仍需用户在物理桌面签核。

## 2026-07-30 插件开发体系 PX3D-B

- Contracts SDK 新增 `IScreenColorSampler` 与 RGB/Hex 数据合同；生产 GDI 实现只返回 `HostApiResponse`，Win32 句柄和错误不会泄漏到插件边界。
- 颜色采样复用 `system.screenshot` 权限。生产 Host 与无宿主测试工具共享同一能力映射，当前 29 个服务入口仍对应 27 个唯一能力。
- WPF UI Kit 新增 `AnchoredTextEditorWindow`，统一锚点位置、Long 主题/动效、`Ctrl+Enter` 保存、`Esc` 取消、失焦延迟保存、错误留窗和关闭取消。
- ColorPicker 与 FolderNote 已改用正式能力/Surface 和 `INotificationService`，并完全移除 Host 项目引用；直接宿主耦合由 3 项降至 1 项。
- 两个真实插件发布目录均通过生产验证；WPF UI Kit NuGet 结构通过；四运行时矩阵 4 / 4；全量自动化 975 / 975；Release 构建 0 警告、0 错误。
- 下一批为 PX3D-C：迁移 QuickLaunch 使用的搜索 DTO、Provider 与结果动作合同，清空机器审计中的最后一个 Host 引用。
- 真实跨屏取色精度、物理 DPI、Explorer 锚点、保存失败、主题/高对比度和语言切换仍需用户在物理桌面签核。

## 2026-07-30 当前开发总审计

- 代码审计基线为 `eab1b85a2d542789c2fea3cfdb012c32efec93ac`；该提交已纯快进整合到本地 `master`，整合来源为 Draft PR #12，但仓库当前仍未报告 GitHub Checks。
- 当前保持 25 个正式插件、42 条命令；八个原生生产组件已全部成为 SDK-only，机器审计中的 `host_coupled_projects` 为空。
- 本次重新验证 Release 解决方案构建为 0 警告、0 错误，全量自动化为 976 / 976，四运行时矩阵为 4 / 4。
- 产品目标和主要工程目标已基本对齐，但 25 项真实桌面人工验收仍为 0 passed / 25 pending / 0 failed，物理 DPI、任务栏、辅助功能、管理员性能、真实下载/干净 Windows 和 Registry/CDN 回滚也未全部签核，因此仍不能声明正式发布收口。
- 后续固定顺序为 PX3D 总审计与仓库 CI、用户/受控环境验收、冻结提交重建 RC。完整证据、风险、退出条件和用户协作项见 [`开发现状审计与后续收口计划_2026-07-30.md`](开发现状审计与后续收口计划_2026-07-30.md)。

## 2026-07-30 主线快进整合

- `master` 已通过 `--ff-only` 整合 `agent/window-manager-transactional` 的 17 个提交，没有生成合并提交，也没有重复实现已完成的插件内存和发布证据原子化。
- 整合后的 Release 构建为 0 警告、0 错误，.NET 全量测试 975 / 975、Web SDK 类型与 Mock Host 5 / 5、四运行时生产验证和确定性打包矩阵 4 / 4 全部通过。
- 主线现在包含 Manifest Schema、生产 Validator、确定性 `.lpak`、Web/.NET/Testing SDK 与 WPF UI Kit；八个原生生产组件中七个已解除 Host 编译依赖。
- PX3D-C 已完成：QuickLaunch 搜索 Query、Result、Action 和 Provider 合同已迁入 SDK，`host_coupled_projects` 已清空。

## 2026-07-30 PX3D-C 搜索边界收口

- 公共搜索合同不暴露宿主 `ContextSnapshot`；宿主适配器统一处理结果 ID、插件偏好 ID、动作白名单和内部搜索模型映射。
- QuickLaunch 已删除 Host 项目引用，并显式声明自身 Serilog 依赖；八个原生生产组件全部进入 SDK-only 清单。
- Release 构建 0 警告、0 错误；全量自动化 976 / 976；搜索边界定向测试 24 / 24；四运行时矩阵 4 / 4。
- 下一步先审计 SDK 版本、包结构和 Loader 共享程序集规则，再建立仓库 GitHub Actions；真实桌面人工状态保持不变。

## 2026-07-30 PX3D 总收口与仓库 CI

- Contracts、WPF、Testing、Web SDK、lockfile、API 与机器审计版本统一为 `1.0.0`。
- Loader 共享程序集固定为 Contracts SDK 与 WPF UI Kit，Testing SDK 不进入生产加载上下文。
- `.github/workflows/build.yml` 提供 `Build and test`、`Plugin platform` 两个检查，覆盖 Release、全量测试、Web SDK、四运行时和三个 .NET SDK 包。
- 本地等价门禁全部通过；GitHub Actions 运行 `30509425367` 的两个 Job 也全部通过。保持直接 `master` 协作，不强制改为 PR 流程。
- PX3D 与仓库 CI 已完成，下一阶段进入用户与受控环境验收，不扩张新功能。

## 2026-07-30 用户验收预检

- 提交 `afd5e68c3bdfa24566e864bb89d43a37f722aecd` 的统一预检通过：源码干净、机器阻断为 0、`ready_for_human_validation=true`。
- Release 构建与全量测试通过，25 个插件、42 条命令的机器合同有效；人工状态保持 `0 passed / 25 pending / 0 failed`，`release_eligible=false`。
- WPR、所需 Profile 和 WPA Exporter 已安装，正式性能采集需要管理员 PowerShell。
- 下一步先按用户操作手册验收 11 个低风险插件，同时观察主题、按钮对比度、卡顿、任务栏分组和焦点返回；继续采用 unsigned 通道，不再等待代码签名证书。

## 2026-07-30 低风险插件 UI 预审

- 11 个低风险插件的代码边界已逐项复核：10 个 Web 插件统一使用 Long Web UI Kit，示例插件使用正式 SDK；主题切换会实时同步到现有 WebView。
- 硬件监控在页面隐藏时停止两秒轮询，并通过单一 Promise 阻止刷新重入，未发现新的持续后台负载。
- 修复亮色主题主按钮悬停色与白字仅约 `4.39:1` 的对比度缺口，WPF 与 Web 现统一为至少 `4.5:1`；Web 主按钮补齐独立按下色。
- 对比度/主题专项 16 / 16、设计系统与本地化 120 / 120、低风险命令矩阵 18 / 18、全量测试 980 / 980 通过；Release 构建 0 警告、0 错误。
- 自动预审不替代人工验收，11 个低风险插件仍需用户确认真实主题切换、文字可读性、键盘路径、复制结果和主观流畅度。

## 2026-07-30 中风险插件状态与性能预审

- ClipboardHistory、ClipboardTool、DevToolkit、QuickNote、Translate 五个中风险插件已完成监听、持久化、网络竞态和后台负载代码审计。
- ClipboardHistory 事件监听成功后不再同时启动降级轮询，避免后台模式持续重复读取剪贴板；监听失败时仍保留定时同步。
- Translate 使用请求代次拒绝原文、目标语言或清空操作之后返回的过期响应，旧翻译不会重新覆盖当前界面。
- ClipboardTool 隐藏暂停轮询且保存失败回滚，QuickNote 串行保存并恢复旧快照，DevToolkit 无后台轮询或额外越权能力。
- 中风险定向测试 137 / 137、隔离命令矩阵 9 / 9、全量测试 981 / 981 通过；Release 构建 0 警告、0 错误。
- 自动预审不改变人工状态，五个中风险插件仍需用户完成真实剪贴板、重启持久化、在线/离线恢复和交互流畅度签核。

## 2026-07-30 高风险插件边界预审

- 新增统一入口 `verify-high-risk-plugin-boundaries.ps1`，覆盖全部 7 个高风险插件；隔离命令、文件/ADS/专用进程事务、屏幕能力、取消交付和 QuickLaunch 隔离共 43 个受控案例。
- QuickLaunch 现在使用插件级生命周期令牌；停止插件会取消后台搜索和旧窗口迟到的目标执行，重启时创建新的生命周期。
- Screenshot Overlay 改用共享 `VirtualScreenHelper` 按 HWND 物理像素覆盖完整虚拟桌面，并显式获取键盘焦点；截图服务复用同一边界源。ColorPicker 的启动点击防误触、物理采样、每屏 DPI 定位和 Esc 取消未发现新缺口。
- Release 构建 0 警告、0 错误，全量自动化 987 / 987，高风险统一门禁 43 / 43。自动预审不改变人工状态；下一步仍需在真实桌面检查跨屏 Overlay、系统剪贴板粘贴与 Esc 取消，再继续关键桌面副作用验收。

## 2026-07-30 关键桌面副作用预审

- 修复 Macro 插件成功停止后再次启动时未重建执行引擎的问题；重启后录制与播放入口不再处于无引擎状态。
- 命令面板执行上下文相关命令前会先恢复打开面板时捕获的前台窗口，避免 WindowManager 等命令误操作短暂抢占焦点的窗口。
- 桌面副作用生命周期门禁扩展到停止与重启场景，12 / 12 通过；测试不移动真实窗口、不注入真实输入、不安装真实全局 Hook。
- 接手后依次检查：命令面板焦点返回、WindowManager 布局/置顶/拒绝回滚、Macro 单次/循环播放与取消后无残留输入。人工状态在真实桌面签核前保持 pending。

## 2026-07-30 Macro 输入语义修复

- 修复录制只保存按下事件导致 `Ctrl+C` 等组合键回放为独立点击的问题；新录制保留 KeyDown/KeyUp 顺序，旧 `KeyPress` JSON 继续兼容。
- 录制延迟改为在对应动作前等待，不再整体错位；停止录制的热键及其尾部修饰键会从动作列表剔除，不会发送给目标应用。
- 宿主允许单独注册 F1-F12，Macro 默认 F6/F7/F8 不再静默退回组合键；普通按键仍要求修饰键。Macro 版本同步升至 `1.1.3`。
- 桌面副作用生命周期门禁 15 / 15、全量自动化 992 / 992 通过；Release 构建 0 警告、0 错误。接手后仍需在可丢弃目标中人工确认组合键、单次回放、循环停止和无残留输入，状态保持 pending。

## 2026-07-30 Macro 鼠标语义修复

- 修复录制只保存鼠标按下并强制回放成短点击的问题；新录制保留左右键 MouseDown/MouseUp、按住时长和释放坐标，旧 `MouseClick` JSON 继续兼容。
- 左键拖拽和右键动作按原事件顺序回放；拖拽到达 MouseUp 前取消会在原按下坐标补发释放，避免残留鼠标按钮。
- Macro 版本同步升至 `1.1.4`，正向证据与用户手册明确要求验证组合键、左键拖拽、右键动作和按键/拖拽期间取消。人工状态保持 pending。
- 桌面副作用生命周期门禁 18 / 18、全量自动化 995 / 995 通过；Release 构建 0 警告、0 错误。搜索首批 `<100ms` 性能门禁补齐不计入指标的 JIT/线程池预热，阈值保持不变并连续 5 / 5 通过。

## 2026-07-30 WindowManager 最大化语义修复

- `window.maximize` 与 `Ctrl+Alt+↑` 改为 Windows 系统最大化，不再只是把普通窗口铺满工作区；重复执行保持幂等。
- 最大化调用失败或验证不一致时恢复原矩形、窗口放置状态和置顶状态，插件版本升至 `2.1.3`。
- WindowManager 专项 15 / 15、关键桌面副作用门禁 21 / 21、全量自动化 998 / 998 通过；Release 构建 0 警告、0 错误。
- 接手后检查真实普通/最小化/最大化窗口、从最大化切换其他布局、多显示器、置顶保持和高权限窗口拒绝；人工状态继续保持 pending。

## 2026-07-30 WindowManager 权限拒绝语义

- 窗口快照、布局/置顶变更和恢复链中的 Win32 error 5 统一映射为 `PermissionDenied`，不再与普通原生故障混为 `Win32Error`。
- 原始操作和恢复操作分别保留自己的错误分类；高权限目标拒绝提示明确包含 Windows 权限拒绝语义，插件版本升至 `2.1.4`。
- WindowManager 专项 17 / 17、关键桌面副作用门禁 24 / 24、全量自动化 1000 / 1000 通过；Release 构建 0 警告、0 错误。
- 接手后用非重要的管理员窗口确认拒绝提示和原状态保持；不要使用系统设置、密码管理器或重要远程会话窗口。人工状态继续保持 pending。

## 2026-07-30 WindowManager 普通布局状态校验

- 最小化窗口执行半屏、角落或三分布局时先恢复为普通状态；布局成功现在必须同时满足目标矩形、`DisplayState=Normal` 和置顶状态保持。
- 若原生调用后矩形正确但窗口仍处于最大化状态，服务会判定失败并恢复操作前快照，不再误报成功；插件版本升至 `2.1.5`。
- WindowManager 专项 19 / 19、关键桌面副作用门禁 27 / 27、全量自动化 1002 / 1002 通过；Release 构建 0 警告、0 错误。
- 接手后检查最小化窗口直接布局，以及从系统最大化切换半屏/角落/三分布局；多显示器和真实置顶仍保持人工 pending。

## 2026-07-30 WindowManager 多显示器与置顶审计

- 按 Microsoft `MonitorFromWindow` 合同确认：目标显示器按窗口相交面积选择，最小化窗口使用最小化前矩形；现有负坐标工作区与奇数尺寸余量计算保持不变。
- 置顶切换新增矩形和显示状态读回校验；若置顶调用连带改变窗口位置、尺寸或最小化/最大化状态，则判定失败并恢复原快照。插件版本升至 `2.1.6`。
- WindowManager 专项 20 / 20、关键桌面副作用门禁 29 / 29、全量自动化 1003 / 1003 通过；Release 构建 0 警告、0 错误。
- 接手后在真实双屏环境检查跨屏窗口归属和工作区边界，并确认普通/最小化/最大化窗口切换置顶时位置与显示状态不变；人工状态继续保持 pending。

## 2026-07-30 WindowManager 前置拒绝边界

- `ApplyForegroundLayout` 与内部事务入口都先验证 `WindowLayout`，未知枚举返回 `InvalidArgument`，不再进入前台窗口读取或抛出未处理异常。
- 显示器缺失、`GetMonitorInfo` 权限拒绝和无效工作区均在窗口变更前结束；对应测试确认没有位置或 `WINDOWPLACEMENT` 写入。插件版本升至 `2.1.7`。
- WindowManager 专项 24 / 24、关键桌面副作用门禁 33 / 33、全量自动化 1007 / 1007 通过；Release 构建 0 警告、0 错误。
- 接手后不需要人为制造这些故障；真实双屏工作区、置顶和高权限拒绝仍按上一批清单人工检查。

## 2026-07-30 Macro 停止录制并发边界

- 修复键盘或鼠标 Hook 回调已在途时，`StopRecording` 返回后仍可能追加动作的问题；回调现在只在持锁且状态仍为 `Recording` 时提交事件。
- Macro 版本升至 `1.1.5`；键盘与鼠标并发回归均通过，关键桌面副作用门禁 35 / 35、全量自动化 1009 / 1009，Release 构建 0 警告、0 错误。
- 接手后在可丢弃目标中检查停止录制瞬间不会多录一个按键或鼠标动作，再按原清单验证组合键、拖拽、单次/循环播放与取消后无残留输入；人工状态继续保持 pending。

## 2026-07-30 Macro 录制会话隔离

- 修复停止录制后立即开始新录制时，上一轮仍在读取原生数据的键盘或鼠标 Hook 回调可能串入新宏的问题；每次成功录制现在使用独立会话令牌。
- Macro 版本升至 `1.1.6`；旧实现下两条复现均得到一个错误动作，修复后 Macro 17 / 17、桌面副作用门禁 35 / 35、全量自动化 1009 / 1009，Release 构建 0 警告、0 错误。
- 接手后在可丢弃目标中执行“停止后立即重录”，确认新录制不包含上一轮尾部按键或鼠标事件；其他 Macro 人工检查与 pending 状态不变。

## 2026-07-30 主线开发方向复审

- 当前权威主线为 `master` @ `021ff28`，与远端 0 / 0；旧远端分支均不作为继续开发入口，`agent/release-candidate-handoff` 的旧压缩提交不整体合并。
- 最初“统一入口、宿主能力、插件生命周期、可信市场、模块化 SDK”方向已经对齐。当前没有需要继续扩张的新主干功能，正式状态仍为 25 项人工检查全部 pending、`release_eligible=false`。
- 接手后先检查主题/对比度、流畅度、管理中心与模块导航、任务栏身份和焦点返回；再按低风险、中风险、高风险与关键桌面副作用完成插件验收；之后处理物理 DPI/多显示器、辅助功能、WPR/WPA、干净 Windows 和 Registry/CDN 回滚，最后冻结并重建 unsigned RC。
- 只有真实复现的问题才进入下一代码批次；每批继续执行专项门禁、全量测试、Release 构建、文档更新、主线推送和 GitHub Actions 复核。

## 2026-07-30 LPWP PR-A Widget 共享契约

- 按 `docs/protocol/LONG_WIDGET_PROTOCOL_V1.md` 启动 LPWP 1.0 第一批代码实现，仅覆盖共享契约、Manifest Schema、语义校验和资源存在性校验，不包含 Widget Bridge、沙箱、IPC Broker 或 UI Surface。
- `ApiVersion.Current`、Contracts SDK、WPF UI Kit、Testing SDK、Web SDK 与 `native-plugin-boundary-audit.json` 同步升至 `1.1.0`；旧 `min_api_version: 1.0.0` 插件仍按同 major/minor 兼容算法继续可用。
- `PluginManifest` 新增 `widgets` 和无 WPF 依赖的 Widget DTO；ManifestReader 现在拒绝非 `webview` runtime、缺失/低于 `1.1.0` 的 `min_api_version`、重复或非法 widget id、越界尺寸、非法刷新策略、非法设置声明和不安全资源路径。
- 包/目录验证现在要求每个 Widget 的 HTML 入口和可选图标真实存在且位于插件根目录内；远程/本地发布链不会因为只声明字段而绕过资源总账。
- 定向门禁 `CoreTests`、`PluginManifestSchemaTests`、`PluginPackageValidatorTests` 和 `PluginSdkBoundaryTests` 共 102 / 102 通过；Release 构建 0 警告、0 错误，全量自动化 1017 / 1017，Web SDK 6 / 6，四运行时矩阵 4 / 4。GitHub Actions 仍需在推送后复核；人工验收状态不变，仍为 `0 passed / 25 pending / 0 failed`。

## 2026-07-31 LPWP PR-C-A IPC 共享基础

- 新增 `LongBetterWindows.PluginIpc` v1.1.0 独立项目，Long Grid 可直接消费 IPC 契约、帧协议和客户端，不需要引用 Long助手 Host、WPF 或任何插件实现。
- 固化 `long.plugin.ipc/1.0` 请求/响应信封、完整标准错误码、4 字节小端长度前缀 UTF-8 JSON、1 MiB 单帧上限和 100–120000 ms deadline 合同；未知错误码一律降级为 `internal_error`。
- Pipe 名称由协议主版本和当前用户 SID 的 SHA-256 截断摘要组成，不泄露原 SID；客户端连接后必须先完成 `host.hello`，并对协议、请求 ID、响应类型和结果形状失败关闭。
- 真实 Named Pipe 回归覆盖握手与 `health.ping`，同时覆盖握手前调用拒绝、非法帧长度、截止时间边界和工程依赖隔离；专项门禁 32 / 32，Release 构建 0 警告、0 错误。
- 仓库 CI 现会运行 `PluginIpcFoundationTests` 并把 `LongBetterWindows.PluginIpc.1.1.0.nupkg` 纳入 `plugin-sdk-packages`。下一批 PR-C-B 接入 Host 同用户 ACL/完整性级别认证、连接级握手状态和只读 `health.ping` / catalog 端点；命令调用、取消和 `plugin.open` 后置，避免绕过既有能力检查。

## 2026-07-31 开发现状总审计

- 新的权威事实源为 [`开发现状审计与后续计划_2026-07-31.md`](开发现状审计与后续计划_2026-07-31.md)。该文档核对了当前提交、串联 PR、主线差异、CI、远端制品、25 插件/42 命令规模、LPWP 里程碑、发布阻断和后续退出条件。
- 当前 PR #13 为 `master ← agent/lpwp-widget-contracts`，PR #14 为 `agent/lpwp-widget-contracts ← agent/lpwp-widget-bridge`；当前分支比 `master` 领先 15 个提交且无反向分叉，但尚未进入主线。
- PR #14 因 Base 不是 `master`，没有自动 PR Check Rollup；当前可信远端证据是手动触发且全部通过的 Repository CI `30568879734`。先整合 PR #13、再让 #14 面向更新后的 `master` 并获得自动 Checks，是继续扩大 PR-C 服务端范围前的首要协作动作。
- 产品主体和自动化工程目标基本对齐，但 PR-C Host Broker、PR-D 发行门禁、真实桌面和外部发布环境仍未关闭，人工状态仍为 `0 passed / 25 pending / 0 failed`，因此 `release_eligible=false`。
## 2026-08-04 PX4 Web UI Kit 视觉矩阵

- Web UI Kit 现会在插件首个模块脚本执行前公开 `window.LongUI` 与版本，并在 DOM 就绪后安装主题、辅助功能状态和公共样式。
- 宿主会把明暗主题、高对比度和 Reduced Motion 初始值及运行中变化同步到已有 WebView；高对比度主按钮使用独立前景令牌。
- 新增 `capture-web-ui-kit-matrix.ps1`，覆盖 2 个主题、4 种辅助模式和 920/640 两种宽度。实跑 16 / 16 张成功，元数据错配 0、空尺寸 0，代表截图人工抽查通过。
- Release 构建 0 警告、0 错误；全量自动化 1110 / 1110、Web SDK 10 / 10 通过。
- 接手后先运行 Web UI Kit 矩阵并抽查明亮普通、暗色窄屏、暗色高对比度组合；工程截图不能替代物理 DPI、Windows forced-colors 和辅助技术验证。

## 2026-08-04 PX4 Web UI Kit 环境桥

- Long Web UI Kit 升至 `1.2.0`，新增语言上下文与 viewport 快照，以及可退订的 `onLanguageChanged` / `onViewportChanged` 公开 API。
- WebView 在首脚本前注入宿主语言基线；无本地化资源的插件也会收到语言变化，声明本地化的插件继续由原消息补充解析语言和资源表。
- 参考样板直接展示版本、语言和真实 CSS viewport。`en-US` 窄视图烟测通过；完整亮暗、高对比度、Reduced Motion、920/640 矩阵 16 / 16 通过。
- 该批次的 `1.2.0` 基线确认最低要求 `1.1.0`、`1.2.0` 可用，未来的 `1.3.0` 当时会被拒绝；Release 构建 0 警告、0 错误，全量自动化 1113 / 1113、Web SDK 10 / 10 通过。

## 2026-08-04 PX4 Web UI Kit 反馈组件

- Long Web UI Kit 升至 `1.3.0`，新增内容区 `LongUI.showToast` 与串行 `LongUI.confirm`；统一安全 DOM、反馈队列、`Esc`、确认/取消语义和关闭后的焦点恢复。
- 参考插件已移除私有确认框事件，改为直接消费公共 API；`long.ui.showToast/confirm` 继续只承担宿主或系统层反馈，两类 Surface 边界不变。
- 真实 WebView 键盘验收发现并修复确认框 `Esc` 同时关闭宿主模块的问题；模态状态现在同步到嵌入和分离宿主，首次 `Esc` 只取消确认，下一次才执行工作区退出层级。
- 最终门禁为 Release 0 警告、0 错误，全量自动化 1123 / 1123、Web SDK 10 / 10、WebView 工程矩阵 16 / 16；两次 4228 字节参考包哈希均为 `2CA5B1D8F3E0894D2617C10DC96DC16223E86C6731BCC228187AAA311286E94F`。
- 接手后先检查 Toast 的亮暗/高对比度/窄屏显示，以及确认框的键盘、焦点恢复和连续调用顺序；物理 DPI、forced-colors 与读屏器仍需人工签核。
- 接手后先检查公共 Toast/确认交互覆盖；真实设置内动态切换语言、Narrator/NVDA、forced-colors 和物理 DPI 仍需人工验证。

## 2026-08-04 PX5 权威插件目录合同

- 新增 `catalog/plugin-catalog.json` 与 JSON Schema，覆盖 27 个源码及参考 Manifest；当前分类为 25 个内置、5 个远程市场、7 个本地可信、1 个样例和 2 个参考插件，市场展示为 5 个远程加 3 个本地。
- 目录只声明分发与展示元数据，ID、版本、名称、作者、运行时和能力仍由 Manifest 唯一负责；自动化测试会阻止漏项、重复项、技术字段复制和信任/市场边界错配。
- PX5 目录合同专项 3 / 3、全量自动化 1126 / 1126 通过；Release 构建 0 警告、0 错误。
- 接手后先做 PX5A-2：由该合同生成并校验宿主 `Marketplace/registry.json` 与文档目录，再移除陈旧的手写 `docs/plugin-registry.json`；完成迁移前不要把现有 Registry 当作权威源。

## 2026-08-04 PX5 目录派生收口

- 新增独立 `LongBetterWindows.PluginCatalogGenerator` 和 `sync-plugin-catalog.ps1`；宿主 8 项本地展示 Registry 与 `docs/plugin-catalog.md` 现在由权威目录和生产 Manifest 确定性生成，`-Check` 可只读阻止漂移。
- 删除陈旧的 `docs/plugin-registry.json`；内置目录不再伪造发布时间、发布说明、下载地址、签名或哈希，正式远程发布流水线边界保持不变。
- 市场分类改用稳定机器值筛选，并在展示层提供中英文标签；无生成时间的内置目录显示为内置 Schema，不再展示公元 0001 年日期。
- 审计发现截图插件调用宿主屏幕捕获却漏报权限，Manifest 已补充 `system.screenshot` 并升至 `1.1.2`。接手后进入 PX5B，检查安装/升级页的发布者、权限差异、兼容性和回滚点信息。
- 目录与市场专项 106 / 106、全量自动化 1130 / 1130 通过；Release 构建 0 警告、0 错误，生成器 `-Check` 无漂移。

## 2026-08-04 PX5 安装与升级审查

- 安装/升级确认现在只使用已验证包事实，完整展示当前到目标版本、Manifest 发布者、签名密钥、实际兼容要求、权限变化和事务恢复语义；新装失败清理残留，替换失败恢复原版本，成功后不保留长期备份。
- 确认框改为固定标题与操作区、正文滚动，高 DPI 实机截图确认按钮不再被裁切。
- 审计发现宏、截图和窗口管理调用通知服务却漏报 `system.notification`，现已补齐 Manifest 并同步代码版本；新增自动化门禁校验原生插件的通知能力与版本一致性。
- 最终门禁为 Release 0 警告、0 错误，全量自动化 1135 / 1135，目录生成器 `-Check` 与 UTF-8 JSON 校验通过。
- 接手后先人工检查已签名/未签名包、新装/升级/卸载三种确认文案，以及键盘滚动和焦点恢复；再进入 PX5C，审计发布 Dry Run、签名、上传、验证与回滚编排，unsigned 主通道保持不变。

## 2026-08-04 PX5 发布准备编排

- 新增 `prepare-marketplace-release.ps1` 和发布准备管线，一次完成签名 Bundle 生成、独立签名复核、部署 Dry Run 与机器可读交接摘要，不读取上传凭据、不修改目标。
- Bundle、证据和包源目录不可覆盖且双向隔离；失败会清理本批新产物。摘要绑定 Release ID、发布者指纹、Registry、底层报告和完整文件计划哈希。
- 插件包 RSA 发布密钥与 Windows 代码签名证书无关，当前 unsigned 应用分发仍不受阻断。
- 最终门禁为 Release 0 警告、0 错误，全量自动化 1140 / 1140，发布与部署专项 20 / 20，目录生成器 `-Check` 与 PowerShell 语法检查通过。
- 接手后先复核准备摘要与两份底层报告的一致性；下一步 PX5C-2 增加显式批准后的上传、无凭据公开验证及验证失败自动回滚。

## 2026-08-04 PX5 正式发布编排

- 新增准备证据验证器和 `release-marketplace.ps1`；正式发布必须精确确认 PX5C-1 Release ID，并重新计算目标、Registry、报告哈希和完整文件计划。
- 上传前验证既有公开基线并再次复核准备证据；部署后公开包集合必须与批准候选精确一致。失败时自动回滚同一 Release ID，并要求公开状态恢复到发布前基线；成功时保留新 Registry。
- 正式入口不支持没有恢复点的首次市场引导发布，也不改变 unsigned 应用分发策略。
- 最终门禁为 Release 0 警告、0 错误，全量自动化 1142 / 1142，发布与部署专项 21 / 21，目录生成器 `-Check` 与 PowerShell 语法检查通过。
- 接手后先审计 PX5 四项完成标准是否全部关闭，再决定进入 PX6 的独立进程评估或插件健康度诊断。

## 2026-08-04 PX5 收口与 PX6 健康度基础

- PX5 收口审计确认：插件集合差异可由权威目录解释，Manifest 与派生目录具备防漂移门禁，安装审查使用验证后的包事实，市场发布失败可恢复并复核原 Registry；PX5 工程完成标准已关闭，真实环境验收继续独立保留。
- PX6A-1 新增线程安全的 `PluginRuntimeHealthMonitor`，由 `CommandExecutor` 聚合插件实际执行的成功、失败、取消、异常、连续失败和耗时，并形成 `idle`、`healthy`、`degraded`、`unhealthy` 稳定状态。
- 健康快照不保存命令输入、输出、异常文本或堆栈，不增加采样线程，也不改变插件协议和执行返回语义。
- 最终门禁为 Release 0 警告、0 错误，全量自动化 1146 / 1146，健康度、命令合同与运行时协调专项 56 / 56，目录生成器 `-Check` 通过。
- 接手后进入 PX6A-2：覆盖插件加载、启动、停止和后台任务生命周期，再为管理中心提供只读诊断投影；独立进程隔离保持评估优先。

## 2026-08-05 PX6 插件生命周期健康度

- PX6A-2 已把注册、启动、恢复、后台、停止、卸载和资源释放接入统一健康模型；激活失败、停止拒绝、后台切换失败与释放异常使用稳定分类，正常停止和用户取消不作为插件故障。
- 命令触发的启动失败只由 Registry 生命周期边界记录一次；成功重新进入运行态才清除连续失败，进入后台或停止不会掩盖既有故障。
- 新增 `PluginRuntimeDiagnostics` 只读投影，按插件 ID 稳定排序并合并 Manifest 身份、Registry 状态和健康快照；卸载后不保留旧名称、版本或运行时文本。
- 最终门禁为 Release 0 警告、0 错误，全量自动化 1150 / 1150，生命周期、命令、协调器、主界面启动和单文件插件专项 67 / 67，目录生成器 `-Check` 通过。
- 接手后进入 PX6A-3：把只读投影接入管理中心诊断模块，完成手动刷新、空态、状态排序、键盘和辅助功能；继续禁止无依据的高频后台采样。

## 2026-08-06 PX6 管理中心插件健康诊断

- 平台诊断现可读取插件命令与生命周期健康度，按异常、降级、健康、空闲排序；展示运行身份、Registry 状态和不含输入、输出、异常正文的摘要。
- 健康数据仅在进入页面、切换语言或点击刷新时读取，不接入每秒性能刷新；列表支持键盘、读屏名称、明暗主题、高对比度和独立滚动。
- 视觉矩阵覆盖 1120 / 840 两种宽度、明暗主题及高对比度，共 8 / 8 张通过；工程截图不替代真实读屏器、物理 DPI 和故障插件人工验证。
- Release 构建 0 警告、0 错误，全量自动化 1152 / 1152，目录生成器 `-Check` 与 PowerShell 语法检查通过。
- 接手后先检查真实失败插件是否排在顶部、刷新后焦点是否稳定、读屏摘要是否清楚；随后进入 PX6B-1，先形成 DLL / Script / Hybrid 独立进程兼容与迁移矩阵，再决定隔离运行器边界。

## 2026-08-06 PX6 插件进程隔离兼容性审计

- 权威目录当前为 7 个 Native DLL、0 个正式 Script、19 个纯 WebView、1 个 WebView + Native Background；现有 Native/Script/Hybrid 均直接依赖宿主对象，不能无兼容合同地迁出进程。
- 新增版本化 Schema 与机器可读兼容矩阵，固定源码证据、库存数量、迁移风险、共享 Worker 约束和单插件资源归因规则；自动化会阻止库存、协议常量和实现锚点漂移。
- 纯 WebView 保留 WebView2 现有进程模型；WPF 与 WebView UI 不进入插件 Worker。现有 27 项权威资产本批没有迁移，默认运行行为不变。
- Release 构建 0 警告、0 错误，隔离合同专项 3 / 3、全量自动化 1155 / 1155，目录生成器 `-Check` 通过。
- 接手后进入 PX6B-2：只建立无 UI、无宿主能力的合成 Native Worker，先验证握手、命令、取消、超时、崩溃、清理和专属进程资源快照，再讨论真实插件迁移。

## 2026-08-06 PX6 合成插件 Worker 实验

- 新增不引用 Host/WPF 的 `LongBetterWindows.PluginWorker`，宿主实验会话创建当前用户专属命名管道、拉起独立进程，并同时校验实际客户端 PID 与随机 256-bit nonce。
- 内部实验协议覆盖初始化、启动、停止、后台、恢复、资源释放、语言变化及 `echo/delay/burn/crash` 合成命令；复用 LPWP 帧限制和 deadline，但不进入 `PluginIpc` 公开导出 API。
- 真实子进程门禁覆盖取消、100 ms deadline 超时、退出码 91 崩溃、优雅关闭、PID 消失、专属工作集/CPU 快照，以及取消/超时/崩溃到现有健康模型的差异映射。
- Release 构建 0 警告、0 错误，全量自动化 1161 / 1161，目录生成器 `-Check` 与 IPC 1.1.0 打包通过；测试后无 Worker 进程残留。
- 生产开关仍为关闭，真实插件迁移数为 0，Host 不引用实验 Worker，宿主默认加载路径和 27 项权威资产均未改变。接手后进入 PX6B-3：先设计最小只读能力代理、宿主访问上下文恢复和崩溃资源租约，不直接迁移 ClipboardHistory 后台。

## 2026-08-06 PX6 Worker 只读权限代理与资源租约

- 内部双向协议只新增 `host.capability.query`：Worker 只能查询会话绑定插件是否声明某项能力，不能传入插件身份、读取用户数据或调用任何 `IHostApi` 服务。
- Host 桥接器在每次请求时恢复 `PluginAccessContext`，再复用 `HostProvider.HasCapability`；无效能力名、未知反向方法和错误响应信封均会被稳定拒绝，反向请求使用独立 deadline。
- 会话资源采用 Host 所有的租约作用域，支持单项释放、后进先出统一释放、释放失败后继续清理，以及 Worker 正常关闭或崩溃时幂等回收。
- Release 构建 0 警告、0 错误，全量自动化 1166 / 1166，Worker/隔离专项 14 / 14，IPC 1.1.0 打包通过；测试后无 Worker 进程残留。
- 生产开关仍关闭，真实插件迁移数为 0，默认加载路径和公开 `PluginIpc` 1.1.0 API 不变。接手后先做 PX6B-4 迁移就绪复审：明确首个无 UI、低副作用候选及退出条件；在所需能力 DTO 与真实资源租约门禁齐备前不迁移 ClipboardHistory 后台。

## 2026-08-06 PX6 Worker 迁移就绪复审

- 新增独立版本化 Schema 与机器可读候选矩阵，对 7 个 Native 和 1 个 WebView + Native Background 的 UI、`IHostApi` 对象耦合、能力、副作用、所缺 DTO 与资源租约逐项对账。
- 结论为现有生产候选 8 项、可迁移 0 项：7 个 Native 都返回宿主 WPF UI；ClipboardHistory 后台虽无 UI，但处理私密剪贴板、CAS 持久化、热键和页面打开事件，不能用当前只读代理承载。
- Sample 仅作为未来“拆出参考工作负载”的来源，不迁移其生产插件；其通知副作用和 WPF UI 仍留在宿主。生产开关关闭、真实迁移数为 0、权威目录和默认加载路径不变。
- Release 构建 0 警告、0 错误，全量自动化 1169 / 1169，Worker 迁移就绪专项 3 / 3；目录派生无漂移，测试后无 Worker 进程残留。
- GitHub 首轮复核发现 Broker 超长帧连接在特定任务收割时序下会于服务释放时再次抛出 `InvalidDataException`；协议读取边界现将超长和非法 JSON 帧统一视为客户端断开，连续 10 轮、每轮 5 个连接回归通过。
- 接手后进入 PX6B-5：建立不进入权威目录、无用户数据和系统副作用的 headless reference workload，使 Worker 加载有界插件合同而不是硬编码命令；完成 DTO、崩溃租约和发布退出条件前不接生产插件。

## 2026-08-06 PX6B-5 Headless Reference Workload

- 新增不进入权威目录的 `reference.headless.native` 参考程序集；它只引用内部 PluginIpc 合同，不引用 Host/WPF，不读取环境、文件、网络或用户数据，也不产生系统副作用。
- Worker 新增可选程序集加载入口，要求程序集只有一个工作负载实现且身份与已认证会话一致；reference 模式只转发声明命令，不回退到 `echo/crash/query-capability` 等 synthetic 命令。
- 真实子进程门禁覆盖生命周期、确定性 SHA-256、主动取消、deadline、会话恢复和目录/源码边界。生产开关仍关闭，真实插件迁移数仍为 0，默认插件加载路径不变。
- 本批 Release 构建 0 警告、0 错误，全量自动化 1172 / 1172，Worker/迁移聚焦门禁 17 / 17，目录派生与 IPC 1.1.0 打包通过，测试后无 Worker 进程残留。
- 首轮 GitHub CI 发现既有 Broker 本地取消测试依赖 20ms 线程池计时器，在并行负载下可晚于 100ms 响应触发；现改为确认服务端开始后显式取消、再放行迟到响应，保留原连接复用语义并移除时序竞态。
- 第二轮 GitHub CI 发现宏录制旧会话回调测试用线程池启动阻塞回调，在并行负载下可能 2 秒内未获调度；键盘与鼠标用例现使用专用长任务线程，继续验证重启录制后旧回调不会写入新会话。

## 2026-08-06 PX6B-6 Worker 包与能力策略

- Worker 启动策略现绑定包根目录、DLL 路径、64 MiB 上限、预期 SHA-256 和批准的 Host 方法集；越出包根、路径链含 reparse point、哈希或方法无效时在拉起前拒绝。
- Worker 只在 PID + nonce 握手成功后读取程序集，复核 SHA-256，并从已校验的同一批内存字节加载，避免校验后重新打开路径；工作负载声明的 `RequiredHostMethods` 必须与批准集精确一致。
- reference workload 的反向 Host 方法集为空；实验协议当前唯一可批准方法仍是显式 DTO `host.capability.query`。生产开关保持关闭、真实迁移数仍为 0，默认插件加载与公开 PluginIpc 1.1.0 API 不变。
- 当前哈希信任锚要求来自“已验证包文件总账”，但生产验证结果尚未桥接为不可伪造的内部启动描述符。下一步 PX6B-7 建立该描述符桥接和独立发布开关门禁，再复审是否存在可拆分的低风险生产候选。
- 本批 Release 构建 0 警告、0 错误，全量自动化 1175 / 1175，Worker/迁移聚焦门禁 20 / 20；三类失败关闭路径连续 5 轮共 15 / 15 通过。
- 下一步 PX6B-6：审计生产 Worker 程序集来源、哈希/签名身份、能力 DTO 白名单和发布开关；这些门禁关闭前不迁移 ClipboardHistory 或其他生产插件。
