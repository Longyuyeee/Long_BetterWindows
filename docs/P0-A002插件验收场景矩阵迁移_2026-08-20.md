# P0-A002 插件验收场景矩阵迁移

> 日期：2026-08-20
> 范围：仅迁移插件正向矩阵的场景声明，不删除历史批准回执和兼容脚本
> 开发提交：`d753812fb192a324b988908378040aa7bc3264f3`
> 远端 CI：`32329364282`，Build and test / Plugin platform 均通过

## 开发结果

- `docs/plugin-positive-function-matrix.json` 已将 25 个 `manual_checks` 迁移为 25 个 `acceptance_scenarios`。
- 场景继续完整覆盖 25 个插件和 42 条唯一命令。
- 场景定义只保留稳定 ID、命令映射、描述和发布必要性，不再保存 `status`、`evidence_path` 或 `evidence_sha256`。
- 验证器、规划器和旧批准入口已兼容读取新字段；历史回执的删除属于 A003。
- 真实进程/TCP 测试已加入仓库既有的非并行性能敏感集合，避免全量并发造成启动假超时，原 5 秒判定标准保持不变。

## 预期与实际

| 检查 | 预期 | 首次实际 | 修正后实际 |
|---|---:|---:|---:|
| 矩阵结构 | 25 插件、42 命令、0 旧字段 | 符合 | 符合 |
| 定向合同与脚本测试 | 17 / 17 | 16 / 17；旧源码测试仍要求矩阵内嵌证据分支 | 17 / 17 |
| 真实进程/TCP 测试 | 6 / 6 | 全量并行时一次性监听器启动超时 | 隔离后 6 / 6 |
| Release 构建 | 0 警告、0 错误 | 0 / 0 | 0 / 0 |
| 全量自动测试 | 1478 / 1478 | 1477 / 1478 | 1478 / 1478 |

## 审计结论

A002 已完成本地和远端闭环，没有丢失插件或命令覆盖，也没有把旧人工状态换名后继续留在矩阵中。旧批准回执目前仅作为兼容覆盖路径存在；A003 必须删除其索引、SHA 审阅和发布资格影响，并增加“回执缺失、损坏或被篡改均不改变自动结果”的测试。
