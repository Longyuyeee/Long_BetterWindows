# Long助手 v1.11.0-rc.16 候选资产与桌面验收审计

> 审计日期：2026-08-14
>
> 候选源码：`d5fc9b2095691a3da9c8c5e95b75052f503caffb`
>
> 版本身份：`v1.11.0-rc.16`
>
> 分发政策：公开 prerelease、unsigned、当前用户安装、不请求提升权限

## 结论

RC16 已从精确干净的主线提交 `d5fc9b2` 生成全套候选实物。两种 ZIP、unsigned 安装器、Release Manifest 和 SHA256SUMS 均已落盘；源码绑定、结构、数量、真实命令、WebView2 清理、独立哈希、完整桌面 UI 自动化和亮/暗物理 250% DPI 视觉复核全部通过。

本阶段关闭“RC16 候选资产生成、工程门禁、候选桌面自动化与核心入口视觉复核”。它不关闭 25 项插件人工回执、真实读屏、托盘/任务栏人工签字、管理员 WPR、SmartScreen/杀毒软件或隔离 Windows 验收，因此仍不得冻结或发布正式 `v1.11.0`。

RC16 尚未创建 Git 标签或 GitHub Release。候选与运行证据位于被排除的 `artifacts/**`，不得提交到源码仓库。

## 构建、测试与源码绑定

- `release.ps1 -Version 1.11.0-rc.16 -PackageKind All` 在不存在的新目录上执行，没有覆盖 RC2–RC15 历史候选。
- Release 全量自动化为 `1467 / 1467`；PR #54 与合并后主线 Repository CI 均通过 Build/test 和 Plugin platform。
- Manifest 记录 `commit=d5fc9b2095691a3da9c8c5e95b75052f503caffb`、`source_dirty=false`、`release_eligible=true`。
- 两种候选均执行 `com.long.base64:base64.encode` 真实命令，退出码为 0。
- self-contained：启动约 4988 ms、命令约 2145 ms、WebView2 清理约 6499 ms。
- framework-dependent：启动约 3086 ms、命令约 2138 ms、WebView2 清理约 6703 ms。
- 两种候选新增 WebView2 残留均为 0；所有候选验证结束后宿主残留为 0。

## 资产总账

| 文件 | 字节数 | SHA-256 |
|---|---:|---|
| `LongBetterWindows-v1.11.0-rc.16-win-x64-self-contained.zip` | 83,800,393 | `7f8a517277201bd5235b3cc2bd49fcefb23577e97dcc25f8509248df81b45e7d` |
| `LongBetterWindows-v1.11.0-rc.16-win-x64-framework-dependent.zip` | 13,945,018 | `36557545d20c3a1ccae93ffffa17af836e522e1116fad66239448966cfbd24e6` |
| `LongAssistant-Setup-v1.11.0-rc.16.exe` | 59,709,326 | `8143cd1a5a34c078aa35c22de09272c3d6d7edbfba9369fe28a5da3d19dc0f8f` |

三项实物哈希均已独立重算并与 `SHA256SUMS.txt` 和 Manifest 逐件一致。`release-manifest.json` SHA-256 为 `71190d4f15d5766c02f8c5677e4501bff8d49855240193b5a67f3067a10bc1b9`。

## 结构与发布政策

两种 ZIP 的独立条目复核均确认：

- 宿主 EXE 恰好 1 个；
- 插件 Manifest 25 个、唯一插件 ID 25 个、命令 42 条；
- 反斜杠 ZIP 条目为 0；
- WebView2 运行态目录条目为 0。

安装器 Authenticode 实测为 `NotSigned`，无签名证书主体；这与既定 unsigned 稳定版政策一致。Manifest 同时记录当前用户安装、`lowest` 权限、不请求提升、发布者身份未验证、必须披露 SmartScreen、必须核对 SHA-256，以及更新清单必须使用 RSA-SHA256 签名。

## 完整机器聚合

`verify-final-closure.ps1` 从同一精确提交完整复跑，报告确认：

- 源码干净，Restore、Release 构建和全量测试通过；
- 25 个插件、42 条命令、87 项自动证据的正向矩阵有效；
- LPWP 1.0 的 .NET 合同、Web SDK、四运行时矩阵和 6 个 Fixture 有效；
- 机器阻断为 0，`ready_for_human_validation=true`；
- 25 项正式人工回执仍为 0 / 25，管理员 WPR 和外部环境项仍待处理，因此最终 `release_eligible=false`。

最终聚合报告 SHA-256 为 `67b36a80a3d7949852983f021ed2940051ae5be99ad9a62dc04c348b06f24a8b`。候选 Manifest 的 `release_eligible=true` 只表示打包实物工程合格；最终聚合的 `false` 表示稳定版尚未取得完整发布资格，两者语义不冲突。

## 候选桌面与视觉验收

完整桌面 UI 冒烟明确指向候选 self-contained 目录，不使用开发输出。结果为 `passed=true`、`error=null`，报告 SHA-256 为 `02357d9b283ee58ca58e2ab929836ffd5a64ecec30ae0a45bf64862af6b246a9`，退出后宿主残留为 0。

覆盖范围包括 Command Palette、超级面板上下文矩阵、焦点恢复、工作区连续性、模块关闭、管理中心、插件市场与命令管理、工作流宽窄布局/参数/长输出、插件嵌入与分离、高对比度、减少动效和组合辅助模式。UIA 播报继续为“快捷启动, 2/12”和“快捷启动, 2/6”，首字徽标未产生重复语义。

候选实物另生成亮/暗主题 × Command Palette/超级面板共 4 张物理 250% DPI 截图并逐张目视复核。真实管理图标继续优先，无图插件显示清晰首字徽标，中文和拉丁字母、选中态、边框、层级和缩放均无打包退化。多个同插件命令共享徽标属于插件身份语义，符合设计目标。

## 下一步

1. 对 RC16 安装器复跑本机当前用户安装、安装后桌面自动化、卸载、升级、回退和数据保留；不复用 RC15 结论。
2. 若安装器生命周期无代码问题，继续 25 / 25 插件人工回执、真实读屏、托盘/任务栏和可获得的最高强度隔离验收。
3. 没有第二台电脑时使用可获得的 Sandbox、VM 或新本地用户，并如实披露隔离强度；不得把同用户开发目录验证表述为干净 Windows。
4. 没有 Authenticode 证书时继续 unsigned 政策；不得伪造签名或把更新清单 RSA 签名要求降级。
5. 任何代码、脚本、资源或普通文档变更都会使 RC16 失效并要求新的候选身份；所有门禁关闭后才冻结正式 `1.11.0`。

本机原始路径：

- `artifacts/releases/v1.11.0-rc.16/`
- `artifacts/quality/final-closure-d5fc9b2-rc16/`
- `artifacts/quality/desktop-ui-smoke-d5fc9b2-rc16-candidate/`
- `artifacts/quality/rc16-candidate-visual-d5fc9b2-20260814/`

上述路径均不得提交 Git。
