# 当前开发对齐审计

> 审计日期：2026-08-21
> 基线：`26f07317357fc750d2cdb117e1559051170b75f1`

## 当前结论

- `master` 与 `origin/master` 为 `0 / 0`，受跟踪工作区干净；最新 Repository CI `32471303649` 通过。
- 当前保持 25 个插件、42 条命令；A009 本地全量 Release 自动化为 1457 / 1457，构建 0 警告、0 错误。
- 产品仍对齐最初的“Windows 本地效率中枢”方向：统一入口、上下文、超级面板、管理中心、插件运行时与可信市场是主链路，不做 uTools 品牌或插件协议复制。
- 宿主、SDK、Web/Script/Native/Hybrid 运行时、插件与发布证据保持模块化边界，没有重新退化为单体工具集合。

## 阶段对齐

| 阶段 | 当前状态 | 审计判断 |
|---|---|---|
| P0 自动验收收口 | A001-A009 completed；A010-A025 待执行 | 当前唯一开发主线，尚未完成 |
| P1 统一入口与搜索 | 已有命令中心、管理入口和搜索基础 | 需按 B001-B004 新门禁重新关闭 |
| P2 上下文匹配 | 已有剪贴板、文件和窗口上下文基础 | 声明式 `matchers` 合同尚未完成 |
| P3 动态指令与结果流 | 已有命令协议、Web 桥和工作流输出 | 动态注册、分批结果与生命周期矩阵尚未完成 |
| P4 超级面板 | 已有上下文面板、固定项和键盘路径 | 文件夹、拖拽、分页和即时结果仍需深化 |
| P5 开发者与市场 | 已有诊断、四运行时 SDK、市场和可信安装 | 新模板、诊断包与市场信息架构仍需统一验收 |
| P6 首方能力 | 现有插件覆盖常用文本与 Windows 工具 | 图片、二维码、PDF、增强搜索和 OCR 尚未按新计划完成 |
| P7 在线生态 | 非本地稳定版阻断项 | 继续独立决策，不反向阻断本地核心 |

因此，方向与架构对齐，但不能声明已经完全对标 uTools，也不能从现有 UI 数量推导阶段完成度。

## 分支审计

除主线外，没有比 `master` 更先进的功能分支。`origin/agent/release-candidate-handoff` 只有提交 `6aef0cb` 不在主线历史中，但它生成于 2026-07-20、已落后主线 580 个提交，并包含现已退役的人工 DPI 批准入口。其交互平台、市场和搜索能力已在后续主线重构中演进，不应直接合并或摘取该旧提交。

## 下一步

1. A010：建立唯一的一键自动收口入口，让本地、`release.ps1` 和 GitHub CI 调用同一套 Release 构建、全量测试、插件平台与最终聚合逻辑。
2. A011-A014：为 25 插件/42 命令补齐场景 ID、WebView/DLL/Script 执行器和高风险事务验证。
3. A015-A020：完成视觉基准、感知差异、布局、DPI×主题×语言、动效和 UIA 焦点图门禁。
4. A021-A025：完成安装事务、隔离用户、非管理员性能、稳定性矩阵和新 RC。
5. P0 完成后再按 B/C/D/E/F/G 顺序继续产品对标，不提前新增无验收闭环的插件。

A010 当前已确认的差距是：`build_test.ps1` 默认 Debug，`release.ps1` 重复维护构建/测试流程，CI 又单独维护插件平台步骤，三者没有消费同一个自动收口入口。这是下一批应先解决的问题。
