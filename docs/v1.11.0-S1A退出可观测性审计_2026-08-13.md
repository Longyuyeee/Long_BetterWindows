# v1.11.0 S1A 退出可观测性开发审计

> 日期：2026-08-13
> 阶段：S1A 结构化退出可观测性
> 状态：已完成

## 1. 本批目标

本批只处理稳定版总计划的 S1A，不提前进入 S1B 的插件整体退出预算，也不修改 UI、版本号或发布候选。目标是让宿主退出能够回答：执行了哪些步骤、每步耗时、是否失败或超时、超时任务后来成功还是失败，以及证据是否绑定本次源码和宿主二进制。

## 2. 实现结果

1. `BestEffortShutdownSequence` 现在返回全部步骤结果，不再只返回失败列表。
2. 每项结果包含名称、`passed` / `failed` / `timed_out` 状态、耗时、超时预算和稳定错误分类。
3. 超时任务保留独立迟到完成任务；最终成功或失败会写入结构化日志，不改变原始超时决策。公开 JSON 若写入时已经观察到迟到结果则记录最终状态，否则如实记录 `pending`，不写入异常正文。
4. 宿主退出固定产生 `broker`、`plugins`、`plugin_runtime`、`host_services` 四个顶层步骤；组件未启动时执行安全空操作，因此证据集合不随启动路径漂移。
5. 新增 `--quality-shutdown-report` 和 `--quality-source-commit` 参数。报告通过 `FileMode.CreateNew` 创建，拒绝覆盖已有证据。
6. shutdown JSON 绑定源码提交、宿主路径和 SHA-256、PID、退出码及全部步骤结果。
7. `verify-host-process-cleanup.ps1` 升级到 Schema 3，必须读取并核对 shutdown JSON、四步骤顺序、成功状态、错误分类、提交、EXE 哈希、PID 和退出码。

## 3. 需求对齐审计

| S1A 要求 | 落地状态 | 审计结论 |
|---|---:|---|
| 返回全部退出步骤 | 已完成 | 成功、失败、超时均进入结果集合，后续步骤继续执行 |
| 稳定状态、耗时、预算和错误分类 | 已完成 | 公开字段不依赖异常消息 |
| 迟到成功或失败 | 已完成 | 内部结果任务和结构化日志记录最终结果；公开报告保持不可变 |
| 不公开敏感异常正文 | 已完成 | JSON DTO 不含 `Exception` 或异常消息字段 |
| 不可覆盖 shutdown JSON | 已完成 | 第二次写入抛出 `IOException`，原文件保持不变 |
| 四个顶层步骤恒定出现 | 已完成 | 宿主接线固定为四项且顺序稳定 |
| 证据绑定源码、EXE、PID、退出码 | 已完成 | Writer 单测与清理脚本双重校验 |
| 清理脚本强制验证报告 | 已完成 | 缺失、无效 JSON、身份错配或步骤失败都会使探针失败 |

本批没有改变产品交互、插件协议、权限边界、版本身份或 unsigned 稳定版政策，符合“先收退出链、后收发布政策”的既定顺序。

## 4. 自动化验收

- S1A 相关定向测试：14 / 14 通过。
- 全量自动化测试：1415 / 1415 通过，0 失败，0 跳过。
- Release 构建：0 警告，0 错误。
- 全量回归同时发现既有 Worker CPU 采样在并发负载下存在统计刷新延迟；测试改为保持同一“必须观察到 CPU 增长”条件的限时最终一致性检查，并将合成负载延长至 1 秒，未降低验收标准。

## 5. 实机验收结果

安装版 Long助手已通过托盘自身“退出”入口正常关闭，没有使用 `Stop-Process`、`taskkill /F` 或其他强制终止。随后从干净提交 `1422fee35a77bc38930fbff57d177f5d761d539b` 重新构建 Release，并运行真实清理探针。

验收结果：

- `source_dirty=false`；
- 宿主 PID `40144`，退出码 `0`；
- 宿主 EXE SHA-256：`d210df42082e8c5c2e3a34537da179496b703cc08d138a8006353929655e39bb`；
- `broker`：`passed`，4.2 ms；
- `plugins`：`passed`，117.3 ms；
- `plugin_runtime`：`passed`，0.6 ms；
- `host_services`：`passed`，1.4 ms；
- 本次宿主观察到 3 个 WebView2 子进程、0 个 Worker 子进程；
- 清理等待结束后本次宿主派生进程残留为 `0`；
- shutdown 报告验证结果为 `true`，问题集合为空；
- 清理探针总耗时 5386 ms，低于 S1B 之前定义的 15 秒真实宿主目标。

原始证据仅保留本机，不推送到 Git：

| 文件 | SHA-256 |
|---|---|
| `artifacts/quality/s1a-shutdown-1422fee/host-shutdown.json` | `f55433cb44f17c92036b68c16e3ab4f1da0428f0efff129b90a357005a29f25f` |
| `artifacts/quality/s1a-shutdown-1422fee/host-process-cleanup.json` | `01f5bfa7687698bfdb97a17a2f58e73ef33bb76cac296b90ec01981cc731e56a` |
| `artifacts/quality/s1a-shutdown-1422fee/host-startup.json` | `2efdf26594325b666a01ab9a63ce0bd9b358066f05c02e749b80c8091fe84e96` |

机器上另有其他应用所属的 WebView2 进程。探针按 PID、创建时间和父子进程关系限定本次宿主派生树，因此没有把无关进程误判为 Long助手残留。

## 6. 阶段判定与下一步

S1A 的代码、自动化测试、Release 构建、不可覆盖证据和真实进程清理验收全部通过，阶段正式关闭。下一阶段固定为 S1B 插件关闭整体预算；S1B 不改变 UI、版本号、插件协议或 unsigned 发布政策。
