# P0-A003 移除插件人工回执判定

> 日期：2026-08-20
> 范围：仅移除插件矩阵验证器对人工批准回执的读取和判定；报告字段迁移与新发布资格算法属于 A004
> 开发提交：`705e55ff153325e9198be4e29e3b27c0bb40552e`
> 远端 CI：`32341732230`，Build and test / Plugin platform 均通过

## 开发结果

- `verify-plugin-positive-matrix.ps1` 不再接收或读取批准回执目录。
- 删除候选包绑定、审阅人、Manifest/EXE/证据文件 SHA-256 和回执新旧状态校验，共移除约 300 行旧门禁代码。
- 旧人工验证规划器不再向矩阵验证器转发已经删除的 `-ApprovalDirectory` 参数；规划器本体留待 A008 归档。
- 25 个插件、42 条命令、自动证据绑定和验收场景覆盖检查保持不变。
- 为兼容尚未迁移的最终收口消费者，旧报告字段暂时固定为 0；场景计数和发布资格字段由 A004 统一迁移。
- 新增真实扰动测试：在历史目录写入损坏 JSON 和伪造 passed 回执，验证器退出码及稳定报告字段与扰动前完全一致。

## 预期与实际

| 检查 | 预期 | 首次实际 | 修正后实际 |
|---|---:|---:|---:|
| 定向测试 | 全部通过 | 新测试缺少 `System.IO`，15 个编译错误 | 16 / 16 |
| 回执扰动 | 前后结果一致 | 未执行 | 退出码与 15 个稳定字段一致 |
| Release 构建 | 0 警告、0 错误 | 0 / 0 | 0 / 0 |
| 全量测试 | 1479 / 1479 | 1479 / 1479 | 1479 / 1479 |

## 审计结论

A003 已完成本地和远端闭环：历史回执存在、损坏或伪造均不能改变插件矩阵结果，验证器源码也不再包含回执入口和 SHA 审阅逻辑。下一步 A004 应迁移报告字段和发布资格算法，使自动证据与场景合同直接决定结果。
