# Long助手 v1.11.0 RC17 正式人工验收重基线审计

> 审计日期：2026-08-15
>
> 前一候选：`v1.11.0-rc.16` / `d5fc9b2095691a3da9c8c5e95b75052f503caffb`
>
> 目标：为 25 插件及最终人工验收生成最新、不可变、可被规划器接受的候选身份

## 1. 触发原因

RC16 已通过候选结构、完整桌面、亮/暗物理 250% DPI、安装升级卸载、任务栏 25 / 25 和托盘恢复 8 / 8 自动门禁。准备启动第一项正式插件回执时，`plan-next-plugin-validation.ps1` 拒绝 RC16，原因是候选生成后新增了候选资产、安装器生命周期、任务栏/托盘审计以及当前状态文档。

这些文件不是批准回执，按既定策略必须使候选过期。该拒绝属于预期安全行为，证明人工回执不能绑定到与当前仓库审计状态不一致的旧候选。

## 2. 决策

- 不放宽规划器的候选陈旧检查；
- 不删除、隐藏或改写候选后的审计文档；
- 不覆盖 `artifacts/releases/v1.11.0-rc.16/`；
- RC16 继续作为完整自动探索候选保留；
- 提升到 `1.11.0-rc.17`，从最新干净合并提交重新生成全部资产；
- RC17 产品功能、UI、插件协议、生产 Worker 和外部生态策略均与 RC16 保持一致。

## 3. 变更范围

本阶段只同步：

- WPF 宿主 Version 与 InformationalVersion；
- `release.ps1`、`build-installer.ps1` 与 Inno Setup 默认版本；
- IPC Fixture、一键入口和质量门禁测试；
- README、CHANGELOG、安装/签名/插件人工验收说明；
- 当前状态、稳定版总计划、候选检查表与收口顺序。

Windows 数字 FileVersion 继续保持 `1.11.0.0`，不改变升级兼容策略。

## 4. 本地验证

- 版本与一键入口专项：`74 / 74` 通过；
- Release 全量自动化：`1467 / 1467` 通过；
- `git diff --check` 必须通过；
- 当前阶段不生成 RC17 候选资产，必须先经过提交、PR CI、合并和主线 CI；
- 不提交 `artifacts/**` 或 `local-secrets/**`。

## 5. 后续严格顺序

1. 合并 RC17 版本重基线并确认主线 CI；
2. 从精确干净合并提交生成 RC17 双 ZIP、unsigned 安装器、Manifest 和 SHA256SUMS；
3. 复跑最终机器聚合、候选完整桌面、任务栏身份与托盘恢复；
4. 使用 `plan-next-plugin-validation.ps1` 确认 RC17 候选不再 stale；
5. 按低风险→中风险→高风险→关键桌面副作用顺序启动 25 项正式插件回执；
6. 自动截图与证据整理由工程流程完成，真实功能体验和风险操作结果由用户签字；
7. 同步推进真实辅助功能、管理员 WPR 和最高可用隔离环境验收。

当前结论：**RC17 重基线代码已通过本地自动化，但在合并、重新打包和候选复验前仍不是可用于正式人工回执的有效候选。**
