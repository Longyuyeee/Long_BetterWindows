# Changelog

本项目使用语义化版本。候选版本在正式发布前仍可能包含兼容性修正。

## 1.11.0-rc.3 — 2026-08-01

### 交互与插件平台

- 完成 Launcher / Workspace 双模式统一工作区，统一管理中心、市场、插件设置、插件运行、工作流与诊断导航。
- 保持 25 个内置插件、42 条命令和 27 项宿主能力，完善三运行时脚手架、SDK、确定性打包和人工正向回执矩阵。
- 完成 Long助手侧 LPWP 1.0 Widget、生命周期、布局、IPC Broker、Golden Fixtures、参考 Widget 与 Long Grid 接入工具。
- Long Web UI Kit 升至 `1.3.0`，在语言与 viewport 环境桥基础上新增内容区 Toast、串行确认框及焦点恢复，并保持 `1.1.x`、`1.2.x` 插件兼容。
- 修复 Web 确认框按 `Esc` 时同时关闭宿主模块的问题，嵌入与分离窗口现在均优先交由活动模态层处理。
- 建立 27 项权威插件目录与 JSON Schema，分离分发分类、市场展示元数据和 Manifest 技术事实，并增加防漏项与边界回归测试。
- 宿主 8 项市场目录和 Markdown 清单改由权威目录与 Manifest 确定性生成，增加只读漂移检查，并移除陈旧的手写文档 Registry。
- 市场分类使用稳定机器值与中英文展示映射；修复内置目录显示无效生成日期及截图插件漏报 `system.screenshot` 权限的问题。
- 安装与升级审查改用已验证包事实，展示版本迁移、发布者与签名身份、兼容要求、权限差异和事务恢复语义，并修复高 DPI 下确认操作被裁切的问题。
- 补齐宏、截图和窗口管理插件缺失的 `system.notification` 声明，同步代码与 Manifest 版本，并增加原生插件能力和版本一致性门禁。
- 新增不可覆盖的市场发布准备编排，一次完成插件包签名、独立 Bundle 验证、部署 Dry Run 和哈希绑定交接报告，不接触线上目标或 Windows 代码签名证书。
- 新增显式批准的市场正式发布编排，重新验证准备证据与公开基线，要求线上包集合匹配候选，并在上传或验收失败时回滚和复核基线。
- 建立隐私安全的插件运行健康度聚合，记录命令成功、失败、取消、异常、连续失败和耗时，为 PX6 诊断与隔离评估提供稳定合同。
- 扩展插件健康度到加载、启动、后台、停止、卸载和资源释放生命周期，并提供不含日志正文的只读诊断投影。
- 管理中心平台诊断新增插件健康视图，按故障优先展示运行身份、Registry 状态与隐私安全摘要，支持手动刷新、键盘访问、空状态和紧凑宽度。
- 完成 PX6 插件进程隔离兼容性审计，以版本化 Schema、机器可读矩阵和源码防漂移门禁固定 Native、Script、WebView 与 Hybrid 的迁移边界及首个合成 Worker 实验范围。
- 完成 PX6B-2 合成无界面 Native Worker 实验，验证 PID + nonce 握手、生命周期、命令、取消、超时、崩溃、清理、专属进程资源快照和健康映射；生产插件迁移仍保持关闭。
- 完成 PX6B-3 只读权限查询代理与会话资源租约，反向请求恢复握手绑定的插件访问上下文，未知方法被拒绝，关闭或崩溃时按后进先出回收资源；仍未开放宿主服务调用或迁移真实插件。
- 完成 PX6B-4 Worker 迁移就绪复审，以独立 Schema、8 项候选矩阵和源码门禁确认现有生产插件均不具备首迁条件；下一步改为非目录、无副作用的 headless reference workload。
- 完成 PX6B-5 headless reference workload：Worker 可加载身份绑定的内部有界合同并转发生命周期与命令，reference 模式禁止回退到 synthetic 命令；真实插件迁移和生产开关继续保持关闭。
- 修复 Broker 本地等待取消测试对 20ms 线程池计时器的竞态，改为服务端开始后显式取消并放行迟到响应，稳定验证连接不会被本地取消污染。
- 宏录制会话重启并发测试改用专用回调线程，避免 CI 线程池繁忙时回调未在 2 秒内启动造成伪失败，同时继续验证旧会话键鼠回调会被丢弃。
- 完成 PX6B-6 Worker 包与能力策略：Host 启动前校验包内 DLL 路径、reparse point、64 MiB 上限和 SHA-256，Worker 握手后复核并从同一批字节加载；工作负载所需 Host DTO 方法必须与批准集精确一致。生产描述符桥接和生产开关仍未开放。
- 完成 PX6B-7 可信包描述符桥：包验证结果密封逐文件 SHA-256 事实，安装事务在提交前复核解压目录，只有真实验证证据可构造内部 Worker 候选；独立生产门禁继续固定关闭，真实插件迁移仍为 0。

### 质量与发布

- 修复 Broker 拒绝超长或非法 JSON 帧后的连接任务清理竞态，协议级无效数据现在只关闭对应连接，不会在服务释放时再次冒泡。
- 修复真实用户测试发现的主题、对比度、交互性能、插件副作用生命周期和任务栏身份问题。
- 发布门禁统一消费插件人工回执、任务栏、原生性能、Long Grid E2E、Widget 桌面行为与签名参考包证据。
- 新增冻结候选提交、不可覆盖批准回执和可移植 `.sources/` 证据链，避免提交漂移或手改摘要绕过验收。
- `v1.11.0-rc.2` 保持不可变历史候选；RC3 使用新的版本号、提交和二进制哈希，不覆盖旧 Release。

## 1.11.0-rc.2 — 2026-07-27

### 安装与发布

- 新增面向普通用户的 `LongAssistant-Setup-*.exe`，采用当前用户安装，不需要管理员权限。
- 安装器提供中文向导、Long助手图标、开始菜单、可选桌面快捷方式、覆盖升级和标准卸载。
- ZIP 继续作为便携版与应用内安全更新载荷。

### 修复

- 修复发布冒烟后 WebView2 运行时用户数据被误收入 ZIP 的问题。
- 发布脚本会在封包前清理 WebView2 临时状态，并在 ZIP 结构检查中强制拒绝此类文件。
- Setup.exe、两种 ZIP、校验和与签名更新清单统一绑定同一干净源码提交。

## 1.11.0-rc.1 — 2026-07-27

### 品牌

- 产品显示名由“Long窗口”统一升级为“Long助手”，英文显示名统一为 “Long Assistant”。
- 新增以几何 `L`、对话引导和助手星芒为核心的极简扁平图标。
- 图标覆盖 EXE、任务栏、托盘、主窗口、欢迎区、关于卡片、Sparse Package 与 README。

### 更新与验证

- 内置 GitHub Release 自动发现、RSA/SHA-256 签名清单验证和更新包完整性校验。
- README 重构为正式产品主页，并同步品牌、下载通道与构建说明。
- 品牌一致性和九档 Windows 图标尺寸进入自动化质量门禁。

## 1.10.0 — 2026-07-26

### 新增

- 命令参数 Schema 与工作流 v3：统一参数声明、编辑、校验、绑定、执行审查、长输出和报告导出。
- Win11 Sparse Package 工程链路：提供一级右键菜单候选构建、状态检查、安装、卸载及独立证据门禁。
- 宿主与 25 个内置插件完成中英文国际化，统一命令中心、超级面板、市场和插件工作台使用同一语言状态。

### 优化

- 收口统一命令入口、超级面板和插件内置 UI 的响应式布局、键盘交互、可访问语义与减少动画行为。
- 延续 Long Design System 的优雅、华丽、扁平和动效化视觉语言，并保持高对比度与多 DPI 适配。
- 发布工程升级为 25 个唯一插件、42 条命令和双分发包的机器可读校验。

### 验证

- Release 构建保持 0 警告、0 错误，自动化测试扩展至 640 项。
- 候选包必须从干净源码提交生成，并校验插件清单、真实 Base64 命令、WebView2 进程差量、ZIP 路径与 SHA-256。
- 当前采用公开 unsigned 分发通道；代码签名是未来可选增强，正式发布仍需完成干净 Windows、物理 DPI、读屏器与安全软件人工门禁。

### 修复

- 将物理 DPI 连续采集的单视图超时从固定 30 秒改为默认 90 秒且允许显式配置，避免高 DPI 下 WebView2 冷启动被误判为渲染失败。

## 1.9.0 — 2026-07-20

### 新增

- 合并 Long Design System、统一命令中心、超级面板、上下文搜索与鼠标手势。
- 合并可信插件市场、签名包校验、事务安装恢复和发布工具链。
- 内置插件集合扩展为 25 个，保留 v1.8 全部系统能力并新增 4 个开发工具插件。

### 工程

- 主线版本统一为 v1.9.0，候选功能不再维护独立开发基线。
- 当前自动化测试为 296 项，发布门禁按 25 个插件、42 条声明式命令执行。
- 市场交互已提取可取消、防重入的会话协调器，覆盖加载竞态、确认态和事务结果刷新。
- 发布脚本会验证唯一插件 ID、命令总数、真实 Base64 命令退出码和 WebView2 进程差集，并写入 Release Manifest。
- 本地市场覆盖部署会保留历史不可变包与 Release 恢复点，确保 Registry 回滚后旧包仍可读取。
- 新增一次性 RSA 密钥驱动的本地市场全链演练，覆盖签名发布、基线部署、候选预演、覆盖、回滚与私钥清理。
- 生产 HTTPS 演练在部署前强制完成候选 Dry Run 和现网公开基线验收，并校验实际 Release ID 与预演计划一致。
- 生产演练增加无需上传令牌的只读 `PreflightOnly` 模式，可在任何线上写入前独立归档候选与基线证据。
- 补齐市场、命令中心、超级面板、主窗口、插件管理和通知的屏幕阅读器名称与动态状态 Live Region。
- 桌面 UI 门禁开始验证核心控件的可访问名称、Window/Edit/List/Button 类型、可用状态和键盘焦点能力。
- 建立真实 Windows 辅助功能三段式门禁：采集系统状态与桌面 UI 哈希、独立人工批准、汇总高对比度/减少动画/组合配置并要求至少一次 Narrator 或 NVDA 朗读签核。
- 建立干净 Windows 发布验收三段式门禁，直接针对候选 ZIP 校验首启、桌面 UI/WebView2、升级、回滚和卸载证据，并拒绝未经人工批准或被篡改的证据。
- 建立 Windows Authenticode 签名与独立复验流水线，只从受保护证书存储区调用私钥，强制 SHA-256 RFC 3161 时间戳，并对用户实际下载的 ZIP 逐文件复验。

## 0.5.0-rc.1 — 2026-07-18

### 新增

- 统一 Command Palette，支持上下文输入、别名搜索、键盘执行与错误反馈。
- Long Design System：WPF/Web 语义令牌、亮暗主题、组件、图标和动效体系。
- ToolCenter 管理中心与统一插件窗口。
- 16 个内置插件全部接入命令协议和统一视觉体系。
- Per-Monitor V2、高对比度、减少动画、焦点可见与自动化名称支持。
- 发布质量参数、隔离插件扫描、性能测量与视觉回归基线。

### 优化

- WebView2 延迟初始化，插件列表回收虚拟化，非活动页面按需构建。
- Command Palette 搜索增加取消和防抖。
- 主窗口支持 720×560 最小逻辑视口与窄窗口响应式布局。
- Command Palette 移除高成本透明窗口，同时保留圆角和主题视觉。
- 调整亮暗主题对比度至 WCAG AA 基线。

### 修复

- 修复 WebView2 控件进入视觉树前初始化造成的等待。
- 修复 Command Palette 执行上下文相关命令时的窗口状态问题。
- 修复原生插件构建产物和 `win-x64` 发布包缺失问题。
- 修复原生插件热键冲突回退、截图区域选择和剪贴板数据链路。
- 修复 `Ctrl+Shift+Tab` 页面反向切换。

### 验证

- 自动化测试 78 / 78 通过，构建 0 警告、0 错误。
- 正式发布目录插件扫描 16 / 16。
- 20 插件等效样本空闲工作集 188.2MB。
- 250% DPI 亮暗主题与 720×560 最小视口视觉回归通过。

### 已知限制

- 候选包尚未进行 Windows 代码签名。
- 100%、125%、150%、200% DPI 的完整设备矩阵仍待人工签核。
- 20 个互不重复真实插件的内存数据仍待发布设备复核。
- WebView2 进程树内存独立于宿主进程统计，打开 Web 插件时会明显增加总内存。
